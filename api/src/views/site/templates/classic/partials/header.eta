<%
/* Shim — supporting pages (locations.eta, menu.eta, menu_hub.eta, page.eta)
   still include ./partials/header for chrome. The header is now a block
   type rendered via the central block renderer; we just delegate here. */
%>
<%~ include('../../../blocks/renderer', { ...it, block: { type: 'header', data: {} } }) %>

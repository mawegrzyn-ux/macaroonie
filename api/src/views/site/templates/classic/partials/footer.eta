<%
/* Shim — see partials/header.eta for the resolution rules. */
const chromeBlocks = it.page ? (it.page.blocks || []) : []
const showFooter   = it.page ? it.page.show_footer : true
%>
<%~ include('../../../shared/chrome_footer', { ...it, chromeBlocks, showFooter }) %>
</body>
</html>

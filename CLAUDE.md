# Macaroonie — Claude Code Context

> ⚠️ DEVELOPER ONLY — contains infra details, env var names, auth config.

**Multitenant restaurant table booking + website CMS for the F&B / QSR franchise sector.**

Each master-franchisee registers as a tenant (Auth0 org). They configure venues, tables,
schedules, booking rules, deposits, and per-venue websites via an admin portal.
Guests book through an embeddable widget (iframe / Ember.js) and browse venue sites
at `{slug}.macaroonie.com`.

Owner: Obscure Kitty. Stack chosen for familiarity with existing plugin work (Node.js, PostgreSQL, React).

---

## ⚠️ Project status — pre-production

**Macaroonie is in active pre-prod development and testing.** No real customers, no real
booking traffic depend on it yet. Treat the codebase accordingly:

- **Backwards compatibility is NOT a concern.** Don't add fallback paths for legacy
  data shapes, deprecated field names, or schema-version-N rendering. Drop columns,
  rename without migrations, replace whole subsystems if cleaner. Simpler code beats
  defensive code at this stage.
- **Don't preserve "legacy fallback" rendering for old layouts** when shipping new
  ones — pick the new path and let it be the only path.
- **No need for feature flags or staged rollouts** for new features. Ship them on,
  iterate on the live deploy.
- **Migrations don't need data-preservation routines** beyond what naturally falls
  out of the SQL. Drop tables, drop columns, drop legacy enums when they stop being
  the source of truth.

This stops applying once we onboard the first real franchise. At that point flip
this note and revisit any places it's been quoted as justification.

---

## Table of Contents

1. [SOS Checklist](#sos-checklist)
2. [EOS Protocol](#eos-protocol)
3. [Standard Design Rules](#standard-design-rules)
4. [Repo structure](#repo-structure)
5. [Tech stack](#tech-stack)
6. [Admin portal design principles](#admin-portal-design-principles)
7. [Multitenancy](#multitenancy--the-most-important-thing-to-understand)
8. [Auth flow](#auth-flow)
9. [Key files](#key-files)
10. [Database — critical patterns](#database--critical-patterns)
11. [Completed items](#completed-items-implemented)
12. [Outstanding items](#outstanding-items)
13. [Environment variables](#environment-variables)
14. [Common mistakes to avoid](#common-mistakes-to-avoid)
15. [Deployment — CI/CD](#deployment--cicd-via-github-actions)
16. [Running locally](#running-locally)
17. [Doc maintenance](#doc-maintenance)

---

## SOS Checklist

When a new session begins:

**1. Orient:**
- Read CLAUDE.md (loaded automatically).
- If the conversation has a compacted summary, verify claims against the actual codebase before referencing past items as "pending".

**2. Verify codebase state:**
- `git log --oneline -5` — what changed since last session?
- `git status` — pick up uncommitted WIP.
- `git branch -a` — confirm you're on the right branch.
- If the user mentions earlier work, grep to confirm it exists before assuming it's still pending.

**3. Establish session context:**
- Ask the user what they want to work on; don't assume.
- For a multi-step task, create a TodoWrite immediately.
- Note today's date for changelog entries.

**Critical rule:** Never reference backlog items, bugs, or features from a session summary as "pending" without verifying. Session summaries go stale. Always grep / check live code.

---

## EOS Protocol

When the user signals end of session ("wrap up", "eos", "that's all", or naturally winds down):

**1. Update all docs** affected by this session:
- CLAUDE.md — new completed items, new gotchas, key-file references.
- In-app Help.jsx — **always** add/update the operator-facing section for any new or changed feature. This is the most user-visible doc and must never lag behind shipped code.
- In-app Docs.jsx — **always** add/update the technical section for any new API routes, DB tables, services, or architectural changes.
- CHANGE.md — prepend a changelog entry for shipped features.

**2. Impact analysis:**
- Files modified / created this session.
- Existing features that may be affected by changes.
- Cross-cutting concerns flagged (e.g. RLS implications, template include paths, CSS variable additions).

**3. Risk + gap assessment:**
- Untested edge cases.
- Incomplete bits left behind (mark them clearly in Outstanding items).
- Recommended next actions for the user.

**4. Hand off for commit.** The **user commits and pushes via GitHub Desktop**. Claude must NOT print `git add` / `git commit` / `git push` command blocks for the user to run — they don't use the CLI for this. The GitHub Actions workflow on the server auto-deploys on push. After making code changes, just say "ready to commit" with a short list of files changed and a suggested commit-message subject line — let the user drive the rest in GitHub Desktop. Same goes for any time you'd otherwise tell them to "push from your laptop" — drop that phrase, the user knows their own workflow.

---

## Standard Design Rules

App-wide defaults — assume these on every new screen unless documented otherwise.

1. **Touch-first.** Admin portal targets tablet-sized touch screens at host stands. Minimum 48×48 px touch targets. `touch-manipulation` on every interactive element.
2. **No hover-only affordances.** Every interactive element must be discoverable and usable by tap alone.
3. **Drag-drop is the default for any orderable list.** If it has a `sort_order` column, the UI must support drag-reorder. Always-visible grip handles; `@dnd-kit` with both PointerSensor and TouchSensor.
4. **No native browser popups** (`window.confirm` / `prompt` / `alert`). Use inline confirmation patterns (double-confirm, typed-confirmation for destructive ops).
5. **Every save is explicit.** Each section has its own Save button — no auto-save, no global form state. Changes are only persisted when the user clicks Save.
6. **Destructive operations require double confirmation.** Delete, anonymise, and data-clearing actions show an inline "Are you sure?" before executing.
7. **No emojis in committed code or files** unless explicitly requested by the user.
8. **Mobile-responsive from 1015 px wide.** Sidebar + main content must remain usable at that width without horizontal scroll.
9. **All modals scrollable.** Use `max-h-[85vh] overflow-y-auto` on modal content areas so content does not clip on smaller tablet screens.
10. **Date as a styled button with invisible `<input type="date">` overlay.** Tapping opens the OS date picker on mobile. No custom calendar components.
11. **`type="tel"` / `inputMode="tel"` on phone inputs.** Triggers numeric keypad on tablets without custom code.

---

## Repo structure

```
/
├── api/              Node.js API (Fastify)
├── admin/            React admin portal (Vite)
├── migrations/       PostgreSQL migration files (run in order 001–021)
├── setup.sh          One-shot Lightsail server setup script
├── deploy.sh         Subsequent deployment script
└── CLAUDE.md         This file
```

---

## Tech stack

| Layer | Choice | Why |
|---|---|---|
| API | Fastify 4, Node.js 22, ESM | Performance, native schema validation |
| DB client | postgres.js (raw SQL) | Matches migrations exactly, no ORM magic |
| Auth | Auth0 (JWT + JWKS) | Offloads auth entirely, fits multitenancy via Orgs |
| Payments | Stripe Connect | Each restaurant is a Connect account, platform takes fee |
| Queue | BullMQ + Redis | Email jobs, hold sweep fallback |
| Validation | Zod | Both API and admin portal |
| Admin UI | React 18, Vite, TanStack Query, @dnd-kit | |
| Realtime | Native WebSocket (ws package) | Timeline live updates |
| Deployment | Lightsail Ubuntu 24.04, Nginx, PM2 | Single instance |

---

## Admin portal design principles

The admin portal is **optimised for tablet-sized touch screens** used by front-of-house staff at a host stand or service counter. Design decisions must reflect this:

- **Touch targets minimum 48 × 48 px** — buttons, covers selectors, slot tiles, grip handles
- **`touch-manipulation` CSS on every interactive element** — eliminates 300 ms tap delay on iOS/Android
- **No hover-only affordances** — every interactive element must be discoverable and usable by tap
- **`type="tel"` / `inputMode="tel"`** on phone number inputs — triggers numeric keypad on iOS/Android tablets without custom code
- **Custom numeric keypad** for covers/number inputs on touch devices — `inputMode="none"` suppresses native keyboard; a 3×4 grid overlay is rendered instead. Detection: `navigator.maxTouchPoints > 0` evaluated once at module load as `IS_TOUCH`
- **Date as a styled button with invisible `<input type="date">` overlay** — tapping the label opens the OS date picker on mobile, and a native datepicker on desktop; avoids custom calendar components
- **Minimum 1015 px wide layout assumed** — sidebar + timeline must remain usable at that width without horizontal scroll inside panels
- **All modals scrollable** — use `max-h-[85vh] overflow-y-auto` on modal content areas so content does not clip on smaller tablet screens in landscape

When adding new UI: always verify interactive elements are finger-sized, add `touch-manipulation`, and test at 1015 px width.

---

## Multitenancy — the most important thing to understand

Every table in the DB (except `tenants`) has:
- A `tenant_id uuid` column
- Row-level security (RLS) enabled
- A policy: `USING (tenant_id = current_setting('app.tenant_id', true)::uuid)`

**Every DB query that touches tenant data MUST go through `withTenant()`:**

```js
// src/config/db.js
import { withTenant } from '../config/db.js'

// CORRECT — sets RLS context before every query
const rows = await withTenant(req.tenantId, tx =>
  tx`SELECT * FROM venues WHERE id = ${venueId}`
)

// WRONG — RLS will block all rows (returns empty, not an error)
const rows = await sql`SELECT * FROM venues WHERE id = ${venueId}`
```

`withTenant()` wraps a transaction with `SET LOCAL app.tenant_id = '${id}'`.
Use `withTx()` only for tenant-resolution queries (slug → tenant_id lookups).

**Connection pooling**: must be transaction-mode (not session-mode) or tenant context leaks between requests. PgBouncer transaction mode is safe.

---

## Auth flow

**Login is identity-only — tenant selection happens on our side, not via a second Auth0 org login.**
This superseded the original org-scoped-login design; the description below is what's actually
live. See the "Tenant selection" Completed item and Docs.jsx's Authentication section for the
full client-side mechanics (`TenantGate`, `TenantSwitcherModal`, `X-Tenant-Id`).

1. User logs in via Auth0 with no `organization` param (`RequireAuth` in `main.jsx`) — except an
   invitation link, which forwards its `invitation`/`organization` query params as-is.
2. API middleware (`src/middleware/auth.js`) validates the JWT via JWKS and checks `platform_admins`.
3. The active tenant is resolved **per request** from the `X-Tenant-Id` header (sent by every
   admin-portal call via `useApi()`), falling back to the JWT's `org_id` claim only when no header
   is sent. A header naming a tenant the caller isn't a member of is ignored (logged), never 401'd.
4. `req.tenantId` and `req.user.role` are attached to every request.
5. Every route handler passes `req.tenantId` to `withTenant()`.

Roles: `owner` > `admin` > `operator` > `viewer`
Use `requireRole('admin', 'owner')` for destructive/config operations.

**Auth0 Dashboard setting this depends on:** Applications → Macaroonie Admin → Login Experience →
**Types of Users = "Business Users"**. This is what actually makes the app invitation-only — see
the Common-mistakes entry below before ever touching this setting.

---

## Key files

### API
- `src/config/db.js` — `withTenant()` and `withTx()` helpers. Touch this carefully.
- `src/config/env.js` — Zod-validated env. App won't start with missing vars.
- `src/middleware/auth.js` — JWT validation + tenant resolution. Auth0 claim namespace is `https://${AUTH0_DOMAIN}/claims/`.
- `src/middleware/error.js` — Global error handler. PG error codes mapped to HTTP codes here.
- `src/routes/bookings.js` — Hold creation, free booking confirm, list/detail, status updates. `POST /bookings/admin-override` — admin direct booking that bypasses all slot/capacity/window rules; handles single table, multi-table (auto-creates combination), or unallocated row.
- `src/routes/customers.js` — Customer profiles. GET /customers?q= (search), GET /customers/:id (detail + bookings), PATCH /customers/:id, POST /customers/:id/anonymise (GDPR erasure), GET /customers/:id/export (GDPR JSON download). Exports upsertCustomer() helper called by bookings.js on every confirm.
- `src/routes/payments.js` — Payment intent creation + Stripe webhook. `handlePaymentSucceeded` copies `combination_id` and `guest_notes` from hold to booking INSERT.
- `src/routes/schedules.js` — Full schedule CRUD (templates, sittings, slot caps, overrides).
- `src/routes/website.js` — Website builder admin CRUD: config singleton, gallery (with reorder), custom pages, menu PDFs, opening hours, allergens, multipart upload, slug availability, DNS verify.
- `src/routes/menus.js` — Structured menu manager (migration 048; `calories`/section `image_url` added in 094/095). One bulk-upsert `PATCH /:id` rewrites the whole `menus` → `menu_sections` → `menu_items` tree per save (see Docs.jsx's Menus section for the full list of touch points a new item/section field needs). Also: `POST /:id/duplicate` (copy a menu), `GET /public/:menuId` + `GET /:id/print` (unauthenticated), variant-groups + dietary-tag CRUD.
- `src/routes/publicSite.js` — Public JSON: `GET /api/site/:slug` bundles config + gallery + pages + menus + hours + allergens + venue. Also serves `sitemap.xml` / `robots.txt`.
- `src/routes/siteRenderer.js` — Subdomain + custom-domain SSR. `resolveSiteHost(host)` returns `{ slug, customDomain }` or null. Renders `views/site/templates/{template_key}/{view}.eta`. Reserved subdomains (api, www, mail, …) bypass.
- `src/services/siteDataSvc.js` — `loadSiteBundle({ slug, customDomain })`. Global lookup → `withTenant(tenant_id, …)` for scoped rows. Checks `is_published` AND `custom_domain_verified` on the custom-domain path.
- `src/services/storageSvc.js` — Pluggable uploads. `getStorage().put(tenantId, kind, ext, mimetype, buffer)`. Drivers: `LocalStorage` (writes to `UPLOAD_DIR`, served at `/uploads/*` by `@fastify/static`), `S3Storage` (lazy-imports `@aws-sdk/client-s3`, compatible with AWS S3, DO Spaces via `S3_ENDPOINT`, Cloudflare R2). Selected via `STORAGE_DRIVER` env.
- `src/views/site/shared/head.eta` — Central theme renderer. Converts `website_config.theme` JSONB into CSS custom properties (`--c-*` for colours, `--f-*` for fonts, `--r-*` for radii, etc) and auto-loads Google Fonts. Every template includes it.
- `src/views/site/templates/classic/`, `src/views/site/templates/modern/` — the two shipping public-site templates. Each has `index.eta`, `menu.eta`, `page.eta`, `partials/{header,footer}.eta`.
- `src/routes/slots.js` — Calls `get_available_slots()` PG function. Thin wrapper.
- `src/routes/emailTemplates.js` — Email template CRUD + venue email settings + merge-fields endpoint + preview renderer + delivery log query + `POST /send-test` (sends a real email through the venue's configured provider against sample merge fields, used by the Widget Test page to verify setup without creating a booking). Mounted at `/api/email-templates`.
- `src/routes/widgetApi.js` — Public booking-widget API (`/widget-api/*`). No auth, CORS-allow-all, scoped per `venueId`. Mirrors `/api/bookings` but resolves tenant from venue lookup (`resolveTenant(venueId)` returns `{ id, tenant_id, ... }`). Free-booking only for v1 — deposit-required venues throw 422 on confirm. Rate limit shared with the global `@fastify/rate-limit` config.
- `src/views/site/widget.eta` — Standalone iframe-friendly booking widget page. Vanilla JS, ~400 lines, 5-step flow (covers/date/slot/details/confirmed). Reads `?theme=light|dark&accent=hex` URL params. Posts `macaroonie:booking-confirmed` message to parent on success. Uses `X-Frame-Options: ALLOWALL` + CSP `frame-ancestors *` so any tenant site can embed.
- `src/routes/media.js` — Per-tenant media library API. Mounted at `/api/media`. Routes: `/categories` CRUD; `/items` (list with `category_id`/`scope`/`search` filters), `/items/scopes` (DISTINCT scope values for dropdown), `/items/check-duplicate` (filename + hash within scope), `/items/upload` (multipart, validates MIME, hashes content, optionally extracts dimensions via `sharp`), `/items/:id/replace` (multipart, **swaps file behind an existing item** — used by the image editor's "Replace original" path; keeps row id, updates url/storage_key/bytes/dimensions/hash, deletes old file best-effort), `/items/:id` PATCH/DELETE, `/items/bulk` (delete / move-category / move-scope). Storage goes through existing `storageSvc` with kind `'media'`. `sharp` is lazy-loaded — listed as `optionalDependencies` so a broken native build doesn't crash the API; missing it just means no width/height capture.
- `src/routes/emailMonitoring.js` — SendGrid monitoring API. `GET /summary` returns stats + suppression lists + local email_log totals in one round-trip. `GET /stats` daily breakdown. `GET /suppressions/:type` (bounces / blocks / spam_reports / invalid_emails). `DELETE /suppressions/:type/:email` (owner-only — clears a specific entry so retries can flow). `GET /log` paginates local email_log. Uses `loadSendgridKey(tenantId, venueId)` to resolve the per-venue key with env fallback. Mounted at `/api/email-monitoring`.
- `src/services/sendgridSvc.js` — SendGrid Web API wrapper. `getStats({ apiKey, startDate, endDate })`, `getSuppressions({ apiKey, type, limit })`, `removeSuppression({ apiKey, type, email })`, `pingApiKey({ apiKey })`. All raise `Error.status` from the HTTP response so callers can distinguish auth failures from rate limits.
- `src/routes/manageBooking.js` — Public guest-facing booking management. SSR page at `/manage/{token}`. No auth — manage_token UUID IS the auth. GET shows details, POST /modify changes date/time/covers, POST /cancel cancels. Queues modification/cancellation emails.
- `src/services/emailSvc.js` — Pluggable email delivery: `sendEmail({ provider, credentials, from, to, subject, html })` with 5 drivers (sendgrid/postmark/mailgun/ses/smtp). Also exports `renderTemplate()` (mustache-style {{field}} replacer with HTML escaping), `buildMergeFields()` (booking+venue+customer→fields object), and `MERGE_FIELDS` (16 available fields with examples).
- `src/services/emailTemplateDefaults.js` — Built-in default HTML email templates (confirmation/reminder/modification/cancellation). Mobile-responsive, professional layout. Used when a tenant hasn't customised their own.
- `src/jobs/emailWorker.js` — BullMQ job processor for booking emails. Loads booking+venue+customer, resolves template (venue→tenant→built-in), resolves provider+credentials, renders, sends, logs to email_log, marks reminder_sent_at.
- `src/routes/platform.js` — Platform admin routes (`/api/me`, `/api/platform/tenants` CRUD + stats). `requirePlatformAdmin` guard. `/api/me` returns user profile + available tenants for org switcher.
- `src/routes/team.js` — In-app team management (`/api/team`). List members, invite (sends Auth0 invitation email), role change (syncs `app_metadata.role` to Auth0), deactivate (removes from Auth0 org), `POST /:userId/reset-password` (Auth0 change-password email). Owner-only for mutations; admin can view. Self-protection guards. Falls back to local-only invites with a warning if `AUTH0_MGMT_*` env vars are unset.
- `src/services/auth0MgmtSvc.js` — Auth0 Management API wrapper. In-memory M2M token cache (refreshes ~5min before expiry), `inviteUserToOrg`, `removeUserFromOrg`, `updateUserAppMetadata` (role sync), `sendPasswordResetEmail`, `createOrganization`, `enableOrgConnection`, `provisionTenantOrg`, `getConnectionByName`. `isConfigured()` / `canInvite()` feature checks let callers degrade gracefully when creds are missing.
- `src/routes/access.js` — Access management (`/api/access`). `GET /modules` lists all known modules merged with the tenant's `tenant_modules` rows. `PATCH /modules/:key` toggles a module on/off (owner only). `GET /roles` returns tenant_roles with permissions. `POST/PATCH/DELETE /roles[/:id]` for custom-role CRUD; built-in roles can be edited but not deleted; custom roles can't be deleted while users reference them.
- `src/config/modules.js` — Single source of truth for the module registry (key, label, description, per-built-in default permissions). 17 modules currently: bookings, venues, tables, schedule, rules, customers, website, email_templates, cash_recon, team, settings, dashboard, widget_test, documentation, issue_log, feature_requests, changelog. Export `MODULES`, `MODULE_KEYS`, `PERMISSION_LEVELS`, `permissionAtLeast()`.
- `src/routes/reviews.js` — Customer reviews CRUD. Tenant-scoped (RLS). Three ingestion paths: Apify scraper (`POST /scrape` → BullMQ), manual entry, CSV upload. Moderation via `is_approved` + `is_featured`. Mounted at `/api/reviews`.
- `src/routes/backlog.js` — Platform-admin-only dev backlog. No RLS (global table). Kanban-column `PATCH /:id/move`. Mounted at `/api/backlog`.
- `src/routes/issueLog.js` — Tenant-scoped ITIL issue log. Auto-derives P1–P4 priority from impact × urgency. Platform admin can update status and promote to backlog. Mounted at `/api/issues`.
- `src/routes/featureRequests.js` — Cross-tenant feature request wishlist with upvote toggle (`feature_request_upvotes`). Platform admin sets lifecycle status and can promote. Mounted at `/api/feature-requests`.
- `src/routes/changelog.js` — Changelog entries. Tenants see published only. Platform admin CRUD + publish/unpublish. Mounted at `/api/changelog`.
- `src/routes/orderSheets.js` — Supplier order management (no prior CLAUDE.md coverage — see Docs.jsx's Order Sheets section for the full write-up). Two layers: reusable **templates** (`order-sheets/templates*`, `order_sheet_setup` module — named per-supplier order form, items with unit/price, assigned venues, optional weekly `delivery_days`) and **orders** (`order-sheets/orders*`, `order_sheets` module — one instance of a template for a venue + delivery date, with per-item quantities; status `ordering → ready → placed`; autosaves quantities). Items live under tenant-wide reorderable `order_sheet_categories`. Both modules belong to the same `order_sheets` module group (one tenant-level on/off switch), gated separately for role permissions (a role can fill in orders without editing templates). Mounted at `/api/order-sheets`.
- `src/routes/testData.js` — Pre-prod QA tooling. `POST /:venueId/test-data/seed` generates synthetic bookings ("Test Guest N") across a date range at a target per-sitting occupancy %, using the same exception→override→template sitting-resolution cascade as `schedules.js`; bypasses holds/Stripe/slot-cap checks like `admin-override`. `POST /:venueId/test-data/clear` bulk-deletes bookings by date range or the whole venue (`dry_run: true` previews the count; `confirm: true` required to actually delete; clears dependent `payments` rows first since that FK is `ON DELETE RESTRICT`). Mounted at `/api/venues`. Gated by the `test_data` module (owner/admin only by default).
- `src/jobs/reviewScrapeWorker.js` — BullMQ worker for Apify Google review scraping jobs. Polls Apify dataset, upserts reviews, marks job complete.
- `src/services/apifySvc.js` — Apify HTTP API wrapper. `startRun(actorId, input)`, `getDataset(datasetId)`, `waitForRun(runId)`. Requires `APIFY_API_TOKEN` env var.
- `src/config/ws.js` — WebSocket server. Rooms keyed by venue_id. Auth via JWT query param.
- `src/services/broadcastSvc.js` — Call `broadcastBooking(type, booking)` after any booking mutation.
- `src/jobs/queues.js` — BullMQ queues. `notificationQueue` for emails, `holdSweepQueue` for hold cleanup.
- `src/routes/foodSafety.js` — SFBB food-safety logging (migrations 076, 077, 090). Equipment (fridge/freezer) temp checks + capture times, deliveries, hot/cold hold checks + stations + capture times, cooking/reheat checks + sessions, plus `GET /defaults` for built-in target/min/max per equipment type. Mounted at `/api/food-safety`, gated by the `food_safety` module.
- `src/routes/checklists.js` — Operator-defined recurring checklists (migration 083): template CRUD + item CRUD/reorder, `GET /due` (what's due for a venue/date), `GET`/`PUT /instance` (lazy-created tick state per template per date). Mounted at `/api/checklists`, gated by the `checklists` module.
- `src/routes/hsDashboards.js` — Customisable H&S dashboards (migrations 086–088): dashboard CRUD + reorder, widget CRUD + reorder, per-widget `col_span`/`height_px` resize. A widget is a thin pointer (`widget_type` + optional `checklist_template_id`) at the food-safety/checklist data — no separate per-widget data model. Mounted at `/api/hs-dashboards`, gated by the `hs_dashboard` module.
- `src/routes/hsActionLog.js` — H&S Action Log (migration 092): a general facilities/compliance to-do list per venue, migrated from the legacy spreadsheet's "ActionLog" tab. Category CRUD + reorder (`hs_action_categories` — tenant-managed reorderable list, same pattern as `fs_hold_stations`, not a fixed enum), entry CRUD, dedicated `PATCH /entries/:id/complete` that sets `completed_by`/`completed_at` server-side from the caller's JWT email. Mounted at `/api/hs-action-log`, gated by the `hs_action_log` module.
- `src/routes/dashboardTiles.js` — Overview page tile system (migration 093). `TILE_CATALOG` (static, same "only real known tiles" idea as nav.js's ROUTE_CATALOG) + CRUD + `PATCH /reorder` on `dashboard_tiles`, gated by `requirePermission('dashboard', …)`. `GET /hs-status?from=&to=&today=` computes the combined Checklists + Food safety status per venue per day for the `hs_today_status`/`hs_week_status` tiles — see Docs.jsx's Overview Tiles section for the full expected/completed/unresolved derivation and the status-colour rules.
- `src/config/defaultDashboardTiles.js` — `DEFAULT_DASHBOARD_TILES` + `seedDefaultDashboardTiles(tx, tenantId)`, used for brand-new tenants (`POST /api/platform/tenants`), same "one seed function, called from two places" pattern as `defaultNav.js`.
- `src/utils/checklistPeriod.js` — `periodStartFor()`/`mondayOf()`/`monthStartOf()`, factored out of `checklists.js` so `dashboardTiles.js`'s hs-status aggregation can never drift from what `checklists.js` itself considers a template's current period.
- `src/routes/nav.js` — Admin-configurable navigation (migration 091). `ROUTE_CATALOG` (static, so the designer can only point items at real pages) + CRUD on `nav_items` + `PATCH /items/:id/reorder` (siblings) + `PATCH /items/:id/reparent` (indent/outdent — moves to a new parent at an index, re-sequencing both the old and new sibling lists) + `POST /reset` (re-seed via `defaultNav.js`) + `PATCH /style` (tenant-wide `sidebar`/`launcher` toggle). Mounted at `/api/nav`, gated by the `nav_designer` module (owner-only `manage` by default).
- `src/config/defaultNav.js` — `DEFAULT_NAV_TREE` + `seedDefaultNav(tx, tenantId)`. Used for brand-new tenants (`POST /api/platform/tenants`) and the nav designer's "reset to defaults". Migration 091 seeds the same tree once in raw SQL for tenants that already existed — that seed never re-runs, so this file is the only copy that matters going forward; keep them in sync by hand if the default tree changes.

### Admin portal
- `src/lib/api.js` — `useApi()` hook. Injects Auth0 token automatically. All API calls go through here. `download(path, filename)` method fetches with auth and triggers browser Save dialog.
- `src/hooks/useRealtimeBookings.js` — WS hook. Connects to `/ws?venue=&token=`. Invalidates TanStack Query on push.
- `src/components/TenantGate.jsx` — sits inside `RequireAuth`, after Auth0 identity login. Auto-selects a single-tenant user's only tenant (no picker flash — the loading state is held until the auto-select effect fires); silently re-enters a returning multi-tenant user's last-picked tenant if it's still valid (`localStorage` key `maca_selected_tenant`, checked against `/me`'s `current_tenant`); shows the mandatory `TenantSwitcherModal` (no close/dismiss) only for a genuine "which one?" case — multiple tenants, no valid stored pick.
- `src/components/TenantSwitcherModal.jsx` — shared tenant-picker modal. `TenantGate` renders it mandatory (no `onClose`) for first-choice; `AppShell`'s sidebar header renders it dismissible for voluntary switching. Replaced the old sidebar `<select>` dropdown.
- `src/components/MobileSuggestModal.jsx` — mounted at the bottom of `AppShell` (desktop shell only, never rendered under `MobileShell`). Detects a touch device in portrait at ≤700px (`IS_TOUCH && matchMedia('(max-width: 700px) and (orientation: portrait)')`, checked on mount and on an `orientationchange`-driven media-query listener so a mid-session rotation into portrait also triggers it) and offers a big modal pointing at `/mobile`. Dismissing either button (Open mobile view / Stay on this page) sets `maca_mobile_prompt_dismissed` in localStorage — offered once per browser, never nags again.
- `src/components/MobileViewToggle.jsx` — persistent floating pill button, bottom-left, shown any time the viewport matches `IS_TOUCH && matchMedia('(max-width: 700px)')` (no portrait requirement, no dismissal — unlike `MobileSuggestModal` above, this is an always-on toggle rather than a one-time nudge). Takes a `target` prop (`'mobile'` or `'standard'`) and navigates to `/mobile` or `/` respectively. Mounted twice: `<MobileViewToggle target="mobile" />` in `AppShell.jsx` (next to `MobileSuggestModal`), `<MobileViewToggle target="standard" />` in `MobileShell.jsx` (after `<Outlet />`) — the latter is the only way to get from `/mobile/*` back to the full desktop admin without editing the URL by hand.
- `src/pages/Dashboard.jsx` — "Overview" (home page, `/` route). Tile-based layout (migration 093) — quick_access / stats_today / upcoming_bookings / venues_status / hs_today_status / hs_week_status, rendered via a shared `TileCard` shell mirroring `HSDashboard.jsx`'s `WidgetCard` (header with icon/title, edit-mode reorder/resize/remove controls, scrollable body). Non-managers see `me.dashboard_tiles` (server-filtered by role); an owner/admin's "Customise layout" mode fetches the full unfiltered list from `GET /api/dashboard-tiles` and can additionally set per-tile `hidden_role_ids` via a small "Visible to" modal. The `Shortcuts` component (per-user quick-access picker, `maca_dashboard_shortcuts` localStorage) is unchanged — it's nested inside the `quick_access` tile, a separate customisation axis from tile layout.
- `src/pages/Timeline.jsx` — Gantt view. @dnd-kit for drag. Booking cards positioned by pixel-accurate time offset. Current-time red line indicator (today only, updates every 30 s).
- `src/pages/Schedule.jsx` — 7-day grid + sitting editor + slot caps grid.
- `src/pages/Tables.jsx` — table list with drag-to-reorder (grip handles always visible), combinations, disallowed pairs section.
- `src/pages/Rules.jsx` — booking rules + smart allocation toggles (allow_cross_section_combo, allow_non_adjacent_combo). enable_reconfirmed_status toggle (re-confirmed status for operator phone-call workflow).
- `src/pages/Bookings.jsx` — Guestplan-style time-grouped list. Stats bar (reservations/tables/guests — active only). Inline status dropdown. Phone visible. Permanent resizable right panel (280–700 px). BookingDrawer in inlineMode.
- `src/pages/Customers.jsx` — Customer list + resizable detail panel. GDPR anonymise (double-confirm inline) + export (JSON download via api.download()).
- `src/pages/Website.jsx` — Website builder admin page (`/website` route). Left-rail nav is grouped by job, not table — `TENANT_NAV` (tenant-site level: Home, Pages & modals, Header, Footer, Locations index, Reservations widget, Brand & theme, Domain & publish, SEO, Legal & cookies, Analytics, Emergency banner) and `VENUE_NAV` (per-location: Location page, Pages & modals, Opening hours, Find us, Contact, Gallery, PDF menus, Allergens, Booking, Online ordering, Delivery); `SINGLE_VENUE_NAV` collapses the two for one-venue tenants. There is no per-venue Identity page any more (removed) — `tenant-brand` renders `BrandSection`, which shows a "Default"-plus-per-venue-pill selector (multi-venue tenants only) over `BrandIdentitySection` + `BrandThemeSection` + `BrandLayoutSection`, each taking an optional `venueId` prop (see the Brand & theme override Completed item). Note `Website.jsx` also has an unreferenced, dead `ThemeSection({config})` function from an earlier refactor, unrelated to any of the above — don't confuse the two when adding a theme field (see Common mistakes). Onboarding card for tenants with no `website_config`/`tenant_site` row yet. Shared primitives: `SectionCard`, `FormRow`, `Toggle`, `SaveBar`, `FileUpload`, `ImageField`, `useConfigFields()` helper. Theme sections save the full `theme` JSONB in one PATCH; each content section saves its own subset.
- `src/pages/Docs.jsx` — in-app technical documentation (auto-synced with codebase).
- `src/pages/Help.jsx` — operator user guide.
- `src/pages/Settings.jsx` — theme colour picker (live CSS var update) + timeline view defaults.
- `src/contexts/SettingsContext.jsx` — `themeHex` state + `hexToHsl`/`fgForHex`/`applyTheme` helpers. Default `#630812`. Persisted to `maca_settings` localStorage key.
- `src/contexts/TimelineSettingsContext.jsx` — `hideInactive`, `groupBySections`, `panelMode`, `venueId`, `refetchTrigger`. All three toggles persisted to `maca_timeline_prefs` localStorage.
- `src/components/bookings/BookingDrawer.jsx` — booking detail side-panel. Save button in header (contextual per edit mode). Table override: individual checkboxes only, pre-populated from member_table_ids; **single-table → `PATCH /relocate`** (cascade displacement), **multi-table → `PATCH /tables`** (direct), **unallocated → `PATCH /tables`**; "Unallocated" checkbox at top of picker (orange); unallocated bookings show orange badge in view mode. **Capacity warning** (amber) shown permanently in table section when `booking.covers` exceeds table/combination max. **End time editor**: "End time" button in Date & time section; `PATCH /bookings/:id/duration`; midnight crossover handled.
- `src/components/bookings/NewBookingModal.jsx` — admin new booking. Touch-optimised: 48px cover buttons, date-as-button (OS picker), tel input for phone, custom numeric keypad for covers on touch devices. Accepts `openManual`/`prefillTime`/`prefillTableId`. **Canvas click flow** opens ManualAllocModal directly. **Manual allocation** button bypasses slot resolver. Customer search: debounced suggestions panel (desktop: side panel; mobile: inline below phone field with × dismiss). Walk In button. autoFocus suppressed on touch. **Slot warnings**: amber when booking end > `sitting_closes_at`; red when > `sitting_doors_close`.
- `src/components/widget/BookingWidget.jsx` — Self-contained widget component. This is the reference implementation for the Ember.js widget.
- `src/pages/EmailTemplates.jsx` — Email template admin page (`/email-templates` route). 3 tabs: Templates (per-type editor with merge field pills, preview iframe, reset-to-default), Settings (provider picker, credentials, sender identity, reminder toggle + hours slider, guest modify/cancel permissions), Sent emails (audit log). Venue selector in header. TipTap visual WYSIWYG editor with toolbar + HTML mode toggle.
- `src/pages/Media.jsx` — Standalone media library page (`/media` route). Renders the modal in `manager` mode. Browse/organise/upload tenant assets without being mid-task.
- `src/components/media/MediaLibrary.jsx` — Reusable per-tenant media library modal. Two modes: `picker` (returns URL via `onPick` callback, "Insert selected" footer button) and `manager` (no insert action). Features: grid + list views, search, category sidebar, scope (form) filter, upload (button + drag-drop), duplicate detection (filename + SHA-256 hash), upload progress chips, single + multi select, single-item detail panel (preview, rename, change category, change scope, delete, **edit**), bulk actions (move category, move scope, delete), resizable detail panel, ESC to close, fullscreen toggle.
- `src/components/media/ImageEditor.jsx` — Crop / rotate / flip editor. Uses `react-easy-crop` for the crop UI; rotation (90° increments) and flip (H/V) applied via `<canvas>` at export. Aspect-ratio presets (Free, 1:1, 4:3, 3:2, 16:9, 9:16). Two save modes: **Save as new** (POST `/media/items/upload` with the cropped Blob → fresh media_items row) or **Replace original** (POST `/media/items/:id/replace` → keeps the row id, swaps file content + url + storage_key + dimensions + hash, deletes old file best-effort). Output MIME matches input for JPEG/WebP, falls back to PNG for everything else. `crossOrigin='anonymous'` on the source `<Image>` so canvas isn't tainted (works for same-origin and CORS-enabled S3 URLs).
- `src/pages/EmailMonitoring.jsx` — SendGrid monitoring (`/email-monitoring` route). Two stacked sections: **What went out** (4 stat cards from SendGrid + 4 from local log + daily breakdown table + recent sends from email_log) and **What's in (suppressions)** (4 cards — bounces/blocks/spam_reports/invalid_emails — with inline removal). Venue picker in header (uses that venue's SendGrid API key). Date range selector (7/30/90 days). Falls back to a clear "this venue is using {provider}" banner when the venue isn't on SendGrid; falls back to a "no API key" banner when the key is missing/invalid. Owner-only on suppression removal.
- `src/pages/Team.jsx` — Team management page (`/team` route). Invite users, set roles (inline dropdown), deactivate/reactivate. RBAC reference table. Owner-only mutations.
- `src/pages/Platform.jsx` — Platform admin dashboard (`/platform` route). Tenant list with venue/user counts, create/edit tenant cards, active/inactive sections, stats overview. Platform admin only.
- `src/pages/Access.jsx` — Access management (`/access` route). Two tabs: **Modules** (master on/off toggles per module, owner-only) and **Roles** (list of tenant_roles + inline permission matrix editor with `none/view/manage` toggle pills per module). Built-in roles show a "Lock" badge, can be edited but not deleted. Custom roles can be added/edited/deleted (only when no user references them). Available to anyone for viewing, mutations are owner-only (API-enforced).
- `src/pages/Reviews.jsx` — Reviews management page (`/reviews` route). Scrape/import/manual-add reviews. Filter, approve, feature. Stats bar.
- `src/pages/Menus.jsx` — Structured menu builder (`/menus` route). Sections (drag-reorder, collapsible) containing dishes; per-dish price, calories (optional kcal), description, photo, variant groups, dietary tags. `SectionImagePicker` gives each section a small category icon (deliberately compact — the image always renders no bigger than the section heading font on the live site). The save payload builder whitelists fields explicitly rather than spreading the item/section object — a new field must be added there or it silently never saves.
- `src/pages/Backlog.jsx` — Kanban backlog (`/backlog` route, platform admin only). 5-column board with @dnd-kit drag between columns.
- `src/pages/IssueLog.jsx` — ITIL issue tracker (`/issues` route). Filter by priority/status/category. Inline detail panel. Platform admin can update status and promote.
- `src/pages/FeatureRequests.jsx` — Feature request wishlist (`/feature-requests` route). Upvote toggle. Platform admin lifecycle controls and promotion to backlog.
- `src/pages/Changelog.jsx` — Changelog timeline (`/changelog` route). Platform admin editor with publish/unpublish. Tenants see published entries only.
- `src/pages/OrderSheets.jsx` — Order Sheets day-to-day page (`/order-sheets` route, `order_sheets` module). List/detail pattern: order list on the left, detail panel on the right, collapsing to full-page-on-select at phone width (`hidden md:flex` on the list column — same responsive shape as `Bookings.jsx`'s `inlineMode`). Exports `OrderCard`, `OrderDetail`, `NewOrderModal`, `FilterModal`, `ALL_STATUSES`, `DEFAULT_STATUSES` for reuse by `MobileOrderSheets.jsx` — same "export sub-components rather than duplicate them" pattern `HSDashboard.jsx` uses for its mobile counterpart. `OrderDetail` handles the item-quantity autosave, status transitions, and delete.
- `src/pages/OrderSheetTemplates.jsx` — Template CRUD (`/order-sheets/templates`, `order_sheet_setup` module, manage-only). Item list per template with categories, prices, venue assignment, delivery-days pattern.
- `src/pages/OrderSheetCategories.jsx` — Tenant-wide reorderable item categories (`/order-sheets/categories`, `order_sheet_setup` module).
- `src/pages/TestData.jsx` — Pre-prod QA tooling (`/test-data` route, owner/admin only). "Generate dummy bookings" (date range + occupancy % + status) and "Clear bookings" (date range or all, typed-`DELETE`-to-confirm with a live dry-run count preview). Not for use once real tenants onboard.
- `src/pages/FoodSafety.jsx` — SFBB food-safety logs (`/food-safety` route). Tabs: Today (overview), Equipment (fridge/freezer grid, drag-reorderable), Deliveries (entry form + current-week list, click any item to load it back into the form for editing), Holds (hot/cold hold stations, drag-reorderable), Cooking (till-style category tabs + dish buttons; `bg-muted/60` fill so tiles read as distinct from the page background). "Corrective actions" button cycles every out-of-range reading for the day still missing a note.
- `src/pages/Checklists.jsx` — Operator-defined recurring checklists (`/checklists` route). Two tabs: Today (tick off what's due) and Checklists (template builder — name, department, frequency daily/weekly/monthly, ordered tasks).
- `src/pages/HSDashboard.jsx` — Customisable H&S dashboards (`/hs-dashboard` route). Any number of named dashboards (tabs), each holding any number of widgets (checklist / temp_checks / delivery_checks / hold_checks / cooking_checks) behind one date navigator. "Edit layout" adds/removes/reorders/resizes (width in grid columns, height in px) widgets. Checklist widget's "Complete" action lives in the widget's own card header. Container is `w-[90%] mx-auto`, not a fixed `max-w` cap. Date navigator: fixed-width label (doesn't reflow between "Today" and a full date string), 44px outlined prev/next arrows, native `input[type=date]` overlay for the calendar picker. The dashboard-name tabs render in the same `flex flex-wrap` row as the date navigator (tabs to its right, wrapping onto their own line only if the row runs out of width) rather than on a separate row below — the tabs' JSX lives once, in that combined row; the `dashboards.length === 0` empty-state branch doesn't render it at all (guarded by `dashboards.length > 0`), and the `editing`-mode dashboard-management strip (rename/reorder/columns) still renders on its own row below, since it's edit-mode-only chrome not part of the everyday navigation row.
- `src/components/foodSafety/shared.jsx` — Shared panels reused by both `FoodSafety.jsx` tabs and `HSDashboard.jsx` widgets: `TempChecksTable`, `DeliveryChecksPanel`/`DeliveryChecksBoard`, `HoldChecksTable`, `CookingChecksPanel`, `EndOfDayReview`. One implementation of each check type, never two. `TempKeypadModal` is the shared IS_TOUCH-gated custom keypad used by `TempCell`, `HoldCell`, and `CookingEntryModal`'s core-temp field in place of the native keyboard.
- `src/pages/HSActionLog.jsx` — Standalone H&S Action Log page (`/hs-action-log` route) — venue picker + `HSActionLogPanel`.
- `src/components/hsActionLog/shared.jsx` — `HSActionLogPanel`, reused by `HSActionLog.jsx` and the H&S Dashboard's `action_log` widget. Open/Completed/All status tabs, category filter, add/edit modal (task, category, priority, assigned-to, due date, notes, photo attachment via the Media library picker), category-management modal (drag-reorder, same pattern as Food Safety's hold-stations manager).
- `src/components/checklists/shared.jsx` — `ChecklistRunPanel`, the tick-list UI shared by `Checklists.jsx` and the H&S Dashboard's checklist widget. Accepts `hideHeader` + `onStateChange` so the host can render its own "Complete" affordance instead of the panel's own footer button.
- `src/pages/NavDesigner.jsx` — Navigation designer (`/nav-designer` route, gated on `nav_designer` module). Outline-editor tree (move up/down/indent/outdent buttons, not drag-and-drop reparenting) over `nav_items`; add section/link with route picked from `GET /api/nav/route-catalog`; per-item module gate, per-role hide (`hidden_role_ids`, checkboxes sourced from `/api/access/roles`), "Show as a quick-access tile" toggle; typed-confirmation "Reset to defaults"; tenant-wide Sidebar/Launcher style toggle.
- `src/pages/Launcher.jsx` — Quick-access tile grid (`/launcher` route) — the alternative to the sidebar when `tenants.nav_style = 'launcher'`. Tiles come from `/api/me`'s `launcher_tiles` (admin-defined via Nav Designer); each viewer can drag-reorder (`@dnd-kit`, `rectSortingStrategy`) and hide tiles for themselves, persisted to `localStorage` (`maca_launcher_prefs_{tenantId}`) — never a DB table. "Reset to admin defaults" clears the override. Empty state explains where tiles come from and offers a "Switch back to sidebar" button when the tenant is stuck in launcher mode with nothing flagged yet.
- `src/components/mobile/MobileShell.jsx` — layout for the `/mobile/*` route tree (a sibling of `AppShell`'s tree in `main.jsx`, both inside `RequireAuth`/`TenantGate`). Minimal safe-area-aware header (back arrow / title / sign out), no sidebar. `useMobileManifest()` swaps `<link rel="manifest">`, `<link rel="apple-touch-icon">`, `<meta name="theme-color">`, and `<meta name="apple-mobile-web-app-title">` to the mobile-only "Macaroonie Ops" identity on mount and restores them on unmount. Also exports `useHideMobileHeader()` — a hook a nested full-page-detail view (e.g. `MobileCashUp.jsx`'s day declaration) calls to suppress the shell's own header for as long as it's mounted, when it needs to render one unified header of its own instead of stacking a second header underneath the shell's — and `MobileLogoutButton`, the same sign-out button the shell's header uses, for that replacement header to fold sign-out into itself. Both are backed by a small context (`HeaderVisibilityContext`) internal to this file; the visibility toggle is a plain boolean (mount sets it, unmount clears it) — no page content is passed through context, avoiding any render-loop risk from passing JSX/objects through it.
- `src/mobile/registry.js` — `MOBILE_MODULES` array (key/label/description/icon/path), the single source of truth for tiles on the `/mobile` hub. Add an entry here + a route in `main.jsx` to ship a new mobile-optimised module.
- `src/pages/mobile/MobileHub.jsx` — `/mobile` index route; renders one tile per `MOBILE_MODULES` entry.
- `src/pages/mobile/MobileHSDashboard.jsx` — phone-portrait, single-column rebuild of `HSDashboard.jsx` with full parity (same dashboards-as-tabs, widget CRUD/reorder, same six widget types, same `/api/hs-dashboards/*` endpoints and shared body components); ignores `col_span`/`height_px` and always renders full-width/natural-height so the page is the only scroll region. Imports `DashboardModal`/`AddWidgetModal`/`WIDGET_TYPE_BY_KEY` (exported from `HSDashboard.jsx`) rather than duplicating them.
- `src/pages/mobile/MobileExpenses.jsx` — second mobile module (`/mobile/expenses`). Venue/date pickers, running total, tap-to-edit expense list, add/edit modal with category chips + camera-capture receipt photo. Reuses the exact same `/venues/:id/cash-recon/*` endpoints as `CashRecon.jsx`'s `ExpensesSection` — no new backend surface for this feature beyond the `POST /expenses` bug fix (see Common mistakes).
- `src/pages/mobile/MobileOrderSheets.jsx` — third mobile module (`/mobile/order-sheets`). Unlike the first two, needed no body-component rework at all — `OrderSheets.jsx`'s list/detail pattern already collapses to single-column-full-page-on-select at phone width. Reuses `OrderCard`/`OrderDetail`/`NewOrderModal`/`FilterModal` exported from `OrderSheets.jsx` verbatim; only the shell differs (no page-level title bar or `pl-14` AppShell-hamburger spacing — `MobileShell`'s own header already supplies both — and no resizable desktop list-panel width, since mobile is always strict either/or, never side-by-side).
- `src/pages/mobile/MobileCashUp.jsx` — fourth mobile module (`/mobile/cash-up`). Venue picker + week nav over a plain vertical list of the week's 7 days (day name/date, `StatusBadge`, income, variance), each row tapping into a day-detail view for the complete daily declaration — same "list, then full-page detail" shape as `MobileOrderSheets.jsx`. A "Week total" tile below the list sums income/takings/variance across days that have a report. Reuses `GET /venues/:id/cash-recon/week/:week_start` (the same endpoint `WeekView`'s day cards use) — no new backend surface. The day-detail view is `MobileDayDeclaration` (module-local): it calls `useHideMobileHeader()` (from `MobileShell.jsx`) to suppress the shell's own header, then renders one unified header of its own — back arrow (to the day list), `EEE, dd MMM` date (e.g. "Mon, 21 Sep") + `SaveIndicator`, a Submit/Unsubmit button, and `MobileLogoutButton` (also from `MobileShell.jsx`) — followed by `DayView` (exported from `CashRecon.jsx`) rendered with `hideHeader` + `onStateChange={setDayState}`, so DayView's own sticky header is skipped and its status/save/submit state flows up into the page's single header instead. No `StatusBadge` in this header — the Submit/Unsubmit button's own label/colour already conveys submitted-vs-not, so the separate status pill was redundant here (unlike the week-list view above, which keeps `StatusBadge` since there's no per-day submit button to read the state off). `fmt`/`getMonday`/`isoWeekDates`/`StatusBadge`/`SaveIndicator`/`DayView` are exported from `CashRecon.jsx` for this.
- `src/pages/mobile/MobileWages.jsx` — fifth mobile module (`/mobile/wages`). A deliberately narrowed rebuild of `CashRecon.jsx`'s `WagesView` — desktop shows Staff/Type/Hours/Rate/Total/Cash paid/Notes/Remove in a dense 8-column grid (already horizontal-scroll-safe via `overflow-x-auto`, but not a good mobile shape); mobile instead renders a single-row-per-staff-entry table (one bordered card, `divide-y` rows) showing only Name, "To be paid" (`total`) and a "Paid" **checkbox** (`PaidToggle`) — not a second amount field. Column labels ("Staff" / "To be paid" / "Paid") render once in a header row above the list rather than per-entry. Ticking "Paid" counts the entry's full `total` toward cash reconciliation; unticked counts nothing — `cash_amount` (the field the reconciliation totals actually sum, see the `cash_wage_entries.total`/`cash_amount` gotcha) is derived from this boolean in `buildPayload()`, never edited as a raw amount on this page. Before the week's report is submitted, toggling "Paid" goes through the normal debounced whole-tree `PUT`, same as any other field; once submitted, that `PUT` is rejected server-side (amounts are locked), so the toggle instead calls a dedicated `PATCH /venues/:id/cash-recon/wages/:week_start/entries/:entryId/paid` (new route, `cashRecon.js`) that updates just that one entry's `cash_amount` — set to its own `total` when paid, `0` when not — regardless of submitted status, since marking staff paid is a real-world step that happens after the report is finalised, not before. "To be paid" edits and add/remove staff are disabled (and hidden, for remove/add) while submitted, with a short inline note explaining why, so an edit there can no longer look silent and fail — see the "Save failed" gotcha below. Hours/rate/entry_type/notes are read from the server and passed straight back through on every save exactly as loaded; this page never edits them. Same auto-populate-from-`wage_defaults`-or-active-staff logic as `WagesView` (duplicated rather than shared, since the two pages needed different field sets from the same state shape), same submit/unsubmit/set-as-default actions, same `/venues/:id/cash-recon/wages/:week_start[/submit|/unsubmit|/set-default]` endpoints. `fmt`/`parseNum`/`getMonday`/`StatusBadge`/`SaveIndicator` exported from `CashRecon.jsx` for this.
- `src/routes/cashRecon.js` — `PATCH /:venueId/cash-recon/wages/:week_start/entries/:entryId/paid` (`{ paid: boolean }`) sets a single `cash_wage_entries` row's `cash_amount` to its own `total` (paid) or `0` (not paid), bypassing the whole-tree `PUT`'s submitted-report lock on purpose — see Mobile Wages above and the "Save failed" gotcha below. Added alongside the Mobile Wages "Paid" checkbox; desktop `WagesView` does not use it yet (still edits `cash_amount` as a raw amount via the whole-tree `PUT`, which is still blocked once submitted — see Outstanding items).

---

## Database — critical patterns

### Slot generation
Slots are **never stored**. They are computed at request time by the `get_available_slots(venue_id, date, covers)` PG function (current definition in `020_slot_start_filter.sql`). The function:
1. Checks for named schedule exceptions (highest priority) → date override → weekly template
2. Iterates sittings, generates candidate times at `slot_interval_mins` while `slot_time < closes_at`
3. **Slot is generated as long as `slot_time < closes_at`** — the booking may run past `closes_at`. Contrast with the old rule which required `slot_time + duration ≤ closes_at`.
4. Looks up per-slot cover cap (sparse — only stored if different from sitting default)
5. Subtracts active bookings + non-expired holds from the cap
6. Applies `zero_cap_display` logic (hidden vs unavailable)
7. Returns `sitting_closes_at` and `sitting_doors_close` per slot for frontend warnings

### Double-booking prevention
Two layers:
1. `UNIQUE (table_id, starts_at)` on `booking_holds` — DB-level race condition guard
2. `confirm_hold(hold_id, tenant_id)` PG function uses `FOR UPDATE NOWAIT` at booking confirm time

### Hold lifecycle
```
Guest presses Book
  → POST /bookings/holds → INSERT booking_holds (TTL = hold_ttl_secs, default 300)
  → Widget shows countdown timer

Guest cancels
  → DELETE /bookings/holds/:id → slot immediately free
  → Also cancels Stripe PI if one was attached

Payment received (Stripe webhook)
  → confirm_hold() → INSERT bookings → DELETE hold
  → broadcastBooking() → WS push to timeline

Hold expires (no action)
  → pg_cron sweep or BullMQ holdSweepQueue deletes expired rows
```

---

## Completed items (implemented)

Items 1–6 from the original bootstrap checklist are done:
- ✅ `auth0_org_id` added to migration 001
- ✅ `PATCH /bookings/:id/move`, `GET|PATCH /venues/:id/rules`, `GET|PATCH /venues/:id/deposit-rules` added
- ✅ `Rules.jsx` ESM issue fixed (`useVenueId` hook)
- ✅ `table_id` + `combination_id` added to slot resolver response
- ✅ pg_cron sweep activated
- ✅ `broadcastBooking()` wired into all booking mutations

Additionally implemented across development sessions:
- ✅ **Smart allocation** — `PATCH /bookings/:id/relocate` (cross-table drag, adjacency expansion, cascade displacement, Unallocated row)
- ✅ **Table sort order** — `PATCH /venues/:id/tables/reorder` + drag-to-reorder on Tables page (always-visible grip handles)
- ✅ **Allocation rules** — `allow_cross_section_combo`, `allow_non_adjacent_combo` toggles on `booking_rules`; `disallowed_table_pairs` table; all enforced in `/relocate`
- ✅ **Disallowed pairs UI** — Tables page shows "Disallowed pairs" section with add/remove
- ✅ **combination_id fix** — `POST /bookings` (free booking confirm) now copies `combination_id` from the hold record
- ✅ **Timeline canvas click** — clicking empty slot on Timeline opens New Booking modal pre-filled with that time
- ✅ **Booking drawer save UX** — save button moved to drawer header, contextual label per active edit mode
- ✅ **Table override simplified** — drawer override picker is now individual-table checkboxes only (no preset combos radio section); pre-populated from `member_table_ids`
- ✅ **Schedule sitting edit** — pencil button on each sitting opens inline edit form for times + default covers
- ✅ **Schedule slot_time fix** — PostgreSQL `TIME` columns return `HH:MM:SS`; normalised to `HH:MM` on load and save to match API validation regex
- ✅ **Docs + Help pages** wired into AppShell nav and main.jsx routes
- ✅ **Doors close time** — `venue_schedule_templates.doors_close_time` column (migration 011); "Last order time" label on sitting closes_at; "Doors close" time picker per day in Schedule; widget slot filtering enforced in `GET /slots` for unauthenticated callers
- ✅ **Widget booking hours rule** — `allow_widget_bookings_after_doors_close` on `booking_rules`; "Opening hours enforcement" section in Rules page
- ✅ **Stripe webhook booking fix** — `combination_id` and `guest_notes` now copied from hold into booking INSERT in `handlePaymentSucceeded` (payments.js)
- ✅ **Doors close time moved per sitting** — `doors_close_time` column on `venue_sittings` and `override_sittings` (migration 013); removed from day-template level
- ✅ **Schedule exceptions** — `schedule_exceptions`, `exception_day_templates`, `exception_sittings`, `exception_sitting_slot_caps` tables (migration 014); `get_available_slots()` updated with Priority 1 exception resolution; full CRUD in `schedules.js`; `ExceptionsSection` / `ExceptionCard` / `ExceptionDayCard` components in `Schedule.jsx`
- ✅ **get_available_slots() bug fix** — `v_exc_template_id uuid` scalar variable replaces direct record field access to prevent "record not assigned yet" PG 55000 error (migration 015)
- ✅ **Admin manual allocation** — `POST /bookings/admin-override` API endpoint; ManualAllocModal in NewBookingModal with free date/time/table selection, "Booked" indicators from day bookings query, unallocated option
- ✅ **Timeline grey column overlay** — `GET /slots?covers=1` used to compute unavailable time strips; grey = outside sittings or cap=0 (reason='unavailable'); fully-booked (reason='full') stays white; strips clipped in secondary combo rows to avoid bleed-through the spanning card
- ✅ **New Booking Modal touch optimisation** — 48px cover buttons, date-as-button overlay, `type="tel"` phone, custom numeric keypad on touch devices
- ✅ **enable_reconfirmed_status** — added to `booking_rules` (migration 012), `BookingRulesBody` schema in venues.js, `BookingRulesSchema` and UI toggle in Rules.jsx
- ✅ **New booking statuses** — `arrived` (cyan, between confirmed and seated), `seated` (renamed from `completed`), `checked_out` (muted grey-green, after seated); migration 017; all capacity queries exclude `checked_out` same as `cancelled`/`no_show`
- ✅ **Bookings page redesign** — Guestplan-style time-grouped list; stats bar; inline status dropdown; phone visible; permanent resizable right panel (BookingDrawer in inlineMode)
- ✅ **Walk In button** — in NewBookingModal guest step; skips all details; books immediately as "Walk In" with dummy email
- ✅ **Timeline canvas click → ManualAllocModal** — clicking empty canvas now opens ManualAllocModal directly with time + table pre-populated (openManual + initialTableIds props)
- ✅ **Timeline liveBooking fix** — drawer in Timeline derives `liveBooking = bookingsRes.find(b => b.id === selected.id) ?? selected` so status updates reflect immediately without stale snapshot
- ✅ **notificationQueue fire-and-forget** — all three `notificationQueue.add()` calls wrapped as `.catch()` promises; API process no longer crashes when Redis is unavailable
- ✅ **Customer database** — `customers` table with RLS (migration 018); `customer_id` FK on `bookings`; auto-upsert on every booking confirm (fire-and-forget); `upsertCustomer()` exported from customers.js; walk-in/TBC emails skipped
- ✅ **Customer GDPR** — `POST /customers/:id/anonymise` replaces all PII with placeholder values, anonymises all linked bookings, never deletes the row; `GET /customers/:id/export` returns JSON download
- ✅ **Customer search in booking modal** — debounced search as operator types in name/email/phone; suggestions panel appears to the right of the modal; clicking a result pre-fills the form
- ✅ **Customers page** — `/customers` route; searchable list; resizable detail panel; GDPR anonymise with double confirmation; GDPR export download
- ✅ **Timeline current-time indicator** — red vertical line across all table rows + dot and label in header; today-only; updates every 30 s
- ✅ **iOS autoFocus suppressed** — `autoFocus={!IS_TOUCH}` on guest name field prevents iOS keyboard popping on modal open
- ✅ **deploy.sh always builds as ubuntu** — both `npm install` and `npm run build` in `deploy_admin()` and `deploy_api()` now run via `sudo -u ubuntu bash -c ...` so dist/ files are never owned by root
- ✅ **table_combination_members tenant_id bug** — admin-override INSERT was passing `tenant_id` to `table_combination_members` which has no such column; removed
- ✅ **checked_out tile colour** — changed from light green to grey (`#e5e7eb` / `#9ca3af`)
- ✅ **New booking FAB** — `+ New booking` moved from toolbar to round floating action button (`absolute bottom-6 right-6 z-30 w-14 h-14 rounded-full`) on timeline canvas
- ✅ **Booking tile shape** — `clip-path` arrow polygon removed; tiles are plain rounded rectangles
- ✅ **Timeline controls → sidebar** — venue selector, inactive toggle, sections toggle, panel toggle, refresh, fullscreen all moved to AppShell sidebar above logout; shown only when route is `/timeline`; icon-only variants for collapsed sidebar
- ✅ **"Inactive" label** — renamed from "Cancelled and no-show" throughout
- ✅ **Settings page** — new `/settings` route; `SettingsContext` for `themeHex` (default `#630812`); `hexToHsl` + `fgForHex` helpers; `--primary`/`--primary-foreground` CSS vars applied at runtime; timeline defaults toggles delegate to `TimelineSettingsContext`; persisted to `localStorage` as `maca_settings`
- ✅ **panelMode to sidebar + localStorage** — `panelMode` moved from local Timeline state into `TimelineSettingsContext`; persisted alongside `hideInactive` and `groupBySections` in `maca_timeline_prefs`
- ✅ **Mobile customer search inline** — on small screens (`< sm`) suggestions panel moved inline below phone field with × dismiss; desktop side-panel unchanged; pure CSS responsive classes
- ✅ **Slot start-time filter** — `get_available_slots()` now generates slots where `slot_time < closes_at` only (not `slot_time + duration ≤ closes_at`); migration 020 drops/recreates `slot_result` type adding `sitting_closes_at` and `sitting_doors_close` fields; `GET /slots` passes both fields through; `NewBookingModal` shows amber/red warning when booking end exceeds last-order/doors-close time
- ✅ **BookingDrawer end-time editor** — "End time" button added to Date & time section; native `<input type="time">` on OS picker; calls existing `PATCH /bookings/:id/duration`; midnight crossover handled; Save in drawer header
- ✅ **enable_arrived_status flag** — added to `booking_rules` (migration 021); controls whether "Arrived" status is shown in booking drawer; defaults to `true`; included in `BookingRulesBody` Zod schema and Rules page toggle
- ✅ **BookingDrawer redesign (UX polish)** — new full-panel layout; unified `editMode` string (`null | 'guest' | 'datetime' | 'table' | 'notes'`) replaces multiple boolean flags; guest count supports direct typing (`type="tel"`) in addition to ± buttons; date/time unified editor: clicking either the start-time pill or the end-time pill opens a single inline card with date + start + end pickers (replaced separate Reschedule/End-time modes); customer search panel rendered as `fixed right-[420px] top-14` sibling outside `overflow-hidden` drawer div (fixes previous clipping); `handleCustSearch(changedValue)` called per-field (not multi-field priority) so editing any of name/email/phone independently triggers search
- ✅ **Timeline grey strips in secondary combo rows** — secondary row canvas now rendered at `z-index: 4` (above primary row's z=3 stacking context) with `pointer-events: none` (passes clicks through to the primary's spanning card); clipped `linear-gradient` background makes grey strips visible outside the booking time while remaining transparent where the spanning card sits
- ✅ **Grey strip architecture redesign** — per-row grey strip rendering removed entirely; single CSS `linear-gradient` as `backgroundImage` on the rows wrapper div; `backgroundColor` for custom timeline bg; decoupled from per-row z-index stacking; booking tiles cover grey naturally via their own opaque backgrounds
- ✅ **Status colours** — 9 booking statuses each have a customisable hex bg colour; `DEFAULT_STATUS_COLOURS` in `SettingsContext`; `deriveBorderFromBg()` (HSL lightness −30pp) auto-derives border; `applyStatusColours()` writes `--status-{name}-bg/bd` CSS vars on `:root`; `index.css` uses `var(--status-confirmed-bg, #bfdbfe)` etc.; `setStatusColour(status, hex)` + `resetStatusColours()` exposed from context; `StatusColourEditor` in Settings with 12 pastel swatches + colour picker + Done + Reset all
- ✅ **Timeline background colour** — `timelineBg` state in `SettingsContext` (default `#ffffff`); applied as `backgroundColor` on the Timeline rows wrapper div; 6 predefined swatches + `ColourPickerRow` in Settings
- ✅ **Closed/unavailable area colour** — `greyColour` state in `SettingsContext` (default `#8c8c8c`); `hexToRgba(greyColour, 0.38)` used in `greyBackground` useMemo gradient stops; 6 grey swatches + `ColourPickerRow` in Settings
- ✅ **Tile modes (compact / extensive)** — `tileMode` + `compactFontSize` in `TimelineSettingsContext`; persisted to `maca_timeline_prefs`; `ROW_HEIGHT_MAP = { compact: { sm:36, md:44, lg:52 }, extensive: 72 }`; `BookingCard` renders single-row (compact) or 3-line flex-col (extensive); extensive shows phone + table allocation via `tableById` Map; `Detail` toggle button in AppShell sidebar; default tile mode + compact size configurable in Settings
- ✅ **Wide time columns** — `wideColumns` boolean in `TimelineSettingsContext` (default `false`); `const hourWidth = wideColumns ? 120 : HOUR_WIDTH`; all three utility functions (`timeToX`, `sittingTimeToX`, `durationToWidth`) accept optional `hw` param defaulting to `HOUR_WIDTH`; all resize/drag/canvas-click/grey-strip calculations use `hourWidth`; `TableRow`, `BookingCard`, `TimelineHeader` all accept `hourWidth` prop; toggle in Settings → Timeline defaults
- ✅ **ColourPickerRow sync fix** — `useEffect(() => setHexInput(value), [value])` added so local picker state syncs when an external swatch button updates the context value (previously only colour wheel/hex input worked; swatches appeared to do nothing)
- ✅ **Resize drag no-drawer-open** — `wasResizingRef` boolean ref set to `true` in `handleUp` after resize mutation fires; `handleBookingClick` checks ref and returns early (suppressing drawer open) then resets ref; prevents the `click` event that fires after `pointerup` from opening the booking drawer
- ✅ **Timeline TDZ fix** — `hourWidth`, `totalWidth`, `rowHeight` declarations (and their `tileMode`/`compactFontSize`/`wideColumns` destructure) moved to before the `nowX` useMemo in `Timeline.jsx`. Previously they were declared ~50 lines after `nowX`, causing a `ReferenceError: Cannot access '...' before initialization` crash (Temporal Dead Zone) that made the entire Timeline render blank. Also added `hourWidth, totalWidth` to the `nowX` dep array.
- ✅ **Compact tile vertical padding** — `py-[3px]` added to the compact tile content div (`flex items-center h-full ...`) so there is a small top/bottom gap around text inside each booking tile in compact mode.
- ✅ **Opening hour line** — `showStartLine` (boolean, default `true`) and `startLineColour` (hex, default `#630812`) added to `SettingsContext`; `firstOpenX` useMemo in Timeline computes x-position of first sitting's `opens_at` for the selected date; a `position:absolute` 3px-wide full-height overlay div is rendered inside the Timeline wrapper at `LABEL_WIDTH + firstOpenX` (z=2, `pointer-events:none`) when `showStartLine` is true; Settings → Appearance shows toggle + colour picker + 6 swatches (only visible when line is enabled).
- ✅ **Shade header row** — `headerBgStrips` boolean (default `false`) added to `SettingsContext`; when true the same `backgroundStyle` object (grey strips + diagonal stripes) is applied as inline style to the `TimelineHeader` outer div, replacing `bg-background`; sticky label cell retains `bg-background` regardless; toggle in Settings → Appearance.
- ✅ **Last order time inclusive** — migration `022_slot_inclusive_last_order.sql` changes `WHILE v_slot_time < v_sitting.closes_at` to `<=` so the last-order slot itself is bookable (e.g. 22:00 last orders → 22:00 slot offered). Uses `CREATE OR REPLACE FUNCTION` so no type drop needed.
- ✅ **Section headers in grouped view (bug fix)** — grouped-section `TableRow` call was missing `rowHeight`, `tileMode`, `compactFontSize`, `tableById`, `hourWidth` props; now matches flat-view `TableRow` exactly.
- ✅ **Timeline hour range control** — `timelineStart` (default 9) and `timelineEnd` (default 24) added to `TimelineSettingsContext`; persisted to `maca_timeline_prefs`; derived `startHour`, `endHour`, `totalHours` in Timeline component; `timeToX` and `sittingTimeToX` accept optional `sh = START_HOUR` third param; `startHour` threaded to `TableRow`, `BookingCard`, `TimelineHeader`, all useMemos and drag/resize/canvas-click handlers; Settings → Timeline defaults shows two hour dropdowns.
- ✅ **Drawer table override uses /relocate** — single-table selection in `BookingDrawer` now calls `PATCH /bookings/:id/relocate` (triggers cascade displacement), multi-table still calls `PATCH /bookings/:id/tables`.
- ✅ **Overlap detection (stop sign)** — `overlappingIds` useMemo O(n²) pairwise check on active bookings for shared tables and overlapping time windows; overlapping `BookingCard` tiles get red `ring-2 ring-red-500 ring-inset` border and `⛔` badge top-right.
- ✅ **Date display as full word** — Timeline top bar replaced with styled-button + invisible `<input type="date">` overlay (matching Bookings pattern); both Timeline and Bookings now show `EEEE d MMMM yyyy` format (e.g. "Monday 22 March 2026"); "Today" shown when on current date; Today button only visible when not on today.
- ✅ **Opening hour line per sitting** — `firstOpenX` (single value) replaced with `sessionStartXs` (array); one 3px line rendered per sitting so both lunch and dinner get their own line; `TimelineHeader` receives `sessionStartXs`/`showStartLine`/`startLineColour` props and renders lines inside its canvas when `headerBgStrips` is on.
- ✅ **Header shade toggle fix** — `TimelineHeader` outer div was missing `bg-background` class so it appeared shaded by default (parent wrapper's `backgroundStyle` bled through); `bg-background` added to the `className` — `backgroundStyle` inline style overrides it when `headerBgStrips` is on.
- ✅ **Session names on sittings** — migration `023_sitting_names.sql` adds `name text` (nullable) to `venue_sittings`, `override_sittings`, `exception_sittings`; `SittingBody` Zod schema updated; `name` returned in `sittings-for-date` and all schedule GET endpoints; Schedule.jsx sitting editor has a "Session name" text input (placeholder: e.g. Lunch, Dinner); name displayed next to time range in sitting list.
- ✅ **Per-sitting stats bar** — `sittingStats` useMemo in Timeline maps active bookings to sittings by HH:MM comparison; shows total bk/covers + per-sitting breakdown in Timeline and Bookings top bars (`hidden sm:flex`); uses sitting `name` if set, falls back to `opens_at–closes_at` time range.
- ✅ **Capacity warning always visible in drawer** — `hasCapacityIssue = booking.covers > (combination_max_covers ?? table_max_covers)` computed on drawer open; amber warning shown permanently in table section (not just when editMode === 'table').
- ✅ **Sidebar expanded-by-default setting** — `sidebarExpandedDefault` boolean (default `true`) added to `SettingsContext`; AppShell uses it to initialise the `open` state on desktop (mobile always starts collapsed); Settings → Interface toggle.
- ✅ **Zero cap greying fix** — `staleTime: 0` added to `slots-overlay` query; `if (slots.length > 1)` changed to `>= 1` with `intervalMs` defaulting to 15 min so single-slot venues now get cap=0 greying.
- ✅ **Unallocated option in drawer** — `tables` query made non-lazy (always fetches); `unallocatedTable` derived from `tables.find(t => t.is_unallocated)`; "Unallocated" checkbox added at top of table picker (orange); currently-unallocated bookings show orange "Unallocated" badge in view mode; `handleTableSave` routes unallocated selection to `PATCH /tables` (not `/relocate`); `toggleTable` clears unallocated when a real table is picked.
- ✅ **Table lock** — `table_locked boolean DEFAULT false` on `bookings` (migration 024); `PATCH /bookings/:id/lock` toggles the flag; `/relocate` conflict scan now selects `table_locked` and throws 422 before displacing if any conflicting booking is locked; `BookingDrawer` shows a lock/unlock icon button inline next to the table pill (amber ring when locked, muted when unlocked); `Lock`/`Unlock` icons from lucide-react.
- ✅ **Tenant Website Builder / CMS — one site per tenant + location pages** *(migrations 025 + 026 + 027 + 041 + 042 + 043)*. Each master franchisee has ONE site at `{tenant_site.subdomain_slug}.macaroonie.com` (or a verified custom domain). Each venue under the tenant becomes a `/locations/{venue.slug}` page driven by `website_config` + the venue row, plus a `/locations` index. Booking widget gained a tenant mode (`/widget/tenant/:tenantId`) with a location-picker step 0, and a `?venue=<id>` deep-link param. Migration 043 (May 2026) flipped the architecture from one-site-per-venue to this model — the `tenant_brand_defaults` table was renamed to `tenant_site` and absorbed `subdomain_slug`, `custom_domain[_verified]`, `home_blocks`, `is_published`, `meta_title/description`, `default_widget_venue_id`, locations-index settings (`hide_locations_index`, `locations_heading`, `locations_intro`); `website_config.home_blocks` → `page_blocks` (drives the location page layout); `website_pages` got a nullable `venue_id` (NULL = tenant-level page, non-NULL = location page). **Pre-flip background:** Venue sites inherit brand defaults and override per-location (address, hours, gallery, menus, hero, about). New tables: `website_config` (per-venue, UNIQUE venue_id), `website_opening_hours`, `website_gallery_images`, `website_pages`, `website_menu_documents`, `website_allergen_info` (migration 025). Migration 026 adds `custom_domain`, `custom_domain_verified`, `template_key` ('classic' | 'modern'), and `theme` JSONB. Migration 027 shifts from UNIQUE tenant_id to UNIQUE venue_id, adds `tenant_brand_defaults` with emergency banner. Pluggable storage service (`STORAGE_DRIVER=local|s3`). Subdomain + custom-domain SSR renderer with `@fastify/view` + Eta. Two shipping templates (classic, modern) sharing theme CSS variables. Admin page at `/website` with venue picker + brand defaults mode (4 sections: identity, theme, analytics, emergency banner) + per-venue mode (18 sections: setup, template, theme overrides, branding, hero, about, gallery with @dnd-kit reorder, PDF menus, allergens, opening hours, find us, contact + socials, online ordering, delivery providers, booking widget, custom pages, SEO, analytics). Deployment pieces (wildcard Nginx, DNS, SSL) pending — see Outstanding items.
- ✅ **Cash recon — SC included_in_income flag** *(migrations 033 + 034)* — independent `included_in_income` boolean on `cash_sc_sources`. Works alongside existing `included_in_takings` with XOR variance adjustment: both on = no change (SC in income AND till); income only = subtract (SC bundled in income but not in till); takings only = add (SC not in income but in till); both off = no change. Toggle in admin SC source editor. Summary shows signed "SC adjustment" line. Weekly grid variance also uses XOR logic.
- ✅ **Booking email system** *(migration 035)* — end-to-end: `manage_token` UUID on bookings (guest auth for self-service). Pluggable email service (`emailSvc.js`) with 4 providers (SendGrid, Mailgun, SES, SMTP) selectable per venue. Default HTML email templates (confirmation, reminder, modification, cancellation) with 16 `{{merge_fields}}`. Email worker (`emailWorker.js`) processes BullMQ jobs: loads booking + venue + template, renders fields, sends, logs to `email_log`. Reminder scheduling via delayed BullMQ jobs (configurable hours-before per venue). Guest manage page at `/manage/{token}` (SSR, themed, modify date/time/covers + cancel with double-confirm). Triggers wired into bookings.js (confirmation on create, cancellation on status change, reminder on confirm). Admin page at `/email-templates` with 3 tabs: template editor (per-type, merge field pills, preview iframe, reset-to-default), settings (provider, credentials, sender, reminders, guest permissions), sent emails log.
- ✅ **Auto-deploy migrations** — `api/scripts/migrate.js` runner with `schema_migrations` tracking table. Runs automatically in GitHub Actions workflow + deploy.sh. Auto-baseline (`AUTO_BASELINE_UP_TO=024` env) for servers whose schema predates the runner. Manual baseline workflow (`.github/workflows/db-baseline.yml`).
- ✅ **CLAUDE.md session protocols** — adopted SOS Checklist, EOS Protocol, Standard Design Rules, Doc maintenance section from the COGS template. TOC added. Gotchas section marked append-only.
- ✅ **Visual email editor** — TipTap WYSIWYG with toolbar (bold/italic/underline, headings, lists, alignment, link, image, divider, colour picker, CTA button insert). Visual / HTML mode toggle. Merge field pills insert at cursor in visual mode. `<a class="btn">` preserved via extended Link extension with class attribute round-trip. URL sanitisation (`isSafeUrl`) prevents `javascript:`/`data:` injection.
- ✅ **Platform admin + tenant/user management** *(migration 036)* — `platform_admins` table (global, no RLS). Auth middleware detects platform admins and bypasses all role gates. `GET /api/me` returns user profile + available tenants (for org switcher). Platform admin routes: `GET/POST/PATCH /api/platform/tenants` + stats. Team routes: `GET/POST/PATCH/DELETE /api/team` for invite, role change, deactivate (owner-only, self-protected). Org switcher in AppShell sidebar (re-authenticates with different Auth0 org). `VITE_AUTH0_ORG_ID` now optional. Team page at `/team`. Platform page at `/platform`.
- ✅ **Auth0 Management API integration** — full team lifecycle in-app, no Auth0 dashboard required for tenant operators. New service `api/src/services/auth0MgmtSvc.js` (token caching via client-credentials grant, `inviteUserToOrg`, `removeUserFromOrg`, `updateUserAppMetadata`, `sendPasswordResetEmail`). `POST /api/team/invite` now sends an Auth0 organization invitation email and rolls back the local row if Auth0 fails. `PATCH /api/team/:userId` syncs role to Auth0 `app_metadata.role` whenever role changes. `DELETE /api/team/:userId` removes the user from the Auth0 org membership in addition to setting `is_active=false` locally. New endpoint `POST /api/team/:userId/reset-password` (owner-only) sends an Auth0 password change email — no need to touch the Auth0 dashboard. Auth middleware reconciles Auth0 sub ↔ local user on first login (links by email when invited row has `auth0_user_id IS NULL`), uses local `users.role` as source of truth for `req.user.role`, and rejects auth when local `is_active=false`. New env vars `AUTH0_MGMT_CLIENT_ID`, `AUTH0_MGMT_CLIENT_SECRET`, `AUTH0_INVITE_CLIENT_ID` (all optional — when missing, system degrades to local-only invites with a warning banner in the Team page). Migration 037 seeds the initial platform admin (Michal) so `/platform` works on first deploy.
- ✅ **Auto-provision Auth0 organisations on tenant creation** — `POST /api/platform/tenants` with `auto_provision: true` (UI default) creates the Auth0 organisation via `provisionTenantOrg()` and enables Username-Password + Google connections with `assign_membership_on_login: true` baked in. New helpers in `auth0MgmtSvc.js`: `createOrganization`, `getConnectionByName`, `enableOrgConnection`, `provisionTenantOrg`. The tenant row is inserted in the DB FIRST (with the provisioned `auth0_org_id`), so a partial Auth0 failure leaves the platform admin a clear edit path. Response includes `auth0_provisioning: { attempted, org_created, enabled_connections, error }` so the UI can surface partial failures with an inline warning. Requires extra M2M scopes (`create:organizations`, `read:connections`, `create:organization_connections`) on top of the team-invite scopes — listed in `api/.env.example`.
- ✅ **Configurable RBAC: tenant module switches + custom roles** *(migration 038)* — extends RBAC beyond the booking-focused enum. Two new tables: `tenant_modules` (per-tenant on/off switch per module — disabled modules are hidden from nav AND rejected at the API) and `tenant_roles` (custom roles per tenant with a `permissions` JSONB mapping `module_key → 'manage' | 'view' | 'none'`). 4 built-in roles seeded (owner/admin/operator/viewer); built-in roles can have permissions edited but not deleted. New `users.custom_role_id` (nullable) takes precedence over the legacy `users.role` enum when set. New module registry at `api/src/config/modules.js` lists all 14 modules + descriptions + per-built-in defaults — single source of truth for migrations, API, UI.
  - **Module groups** (`MODULE_GROUPS` in modules.js) bundle tightly-coupled modules under one master switch. Current groups: `bookings` (bookings, venues, tables, schedule, rules, customers, widget_test), `email_templates`, `website`, `cash_recon`. Core modules (dashboard, team, settings, documentation) are `core: true` with no master switch. The tenant Modules tab in `/access` toggles GROUPS, not individual modules — disabling Bookings hides every booking-related nav entry at once.
  - **Role permissions stay per-module** so a role can have `bookings: manage` + `rules: view` even though both belong to the same group.
  - **Routes**: `/api/access/modules` (per-module list/patch — internal), `/api/access/module-groups` (group-level toggles — what the UI uses), `/api/access/roles` (custom-role CRUD).
  - New `requirePermission(moduleKey, level)` middleware for route gating. `/api/me` extended with `enabled_modules: string[]`, `permissions: { module: level }`, `effective_role`. AppShell filters nav by `me.permissions[item.module]`. New admin page at `/access` with Modules (group toggles) + Roles tabs.
- ✅ **Reservations widget — full rebuild** *(migrations 050 + 051 + 052, May 2026)*. The legacy `widget.eta` + `booking_widget` block are gone — replaced with a clean ground-up rebuild at `views/site/reservations_widget.eta` and `views/site/blocks/reservations_widget.eta`. Routes renamed: `/widget/:id` → `/reservations/:id`, `/widget/tenant/:id` → `/reservations/tenant/:id`. Admin page renamed: `/widget-settings` → `/reservations-widget` with a Live preview iframe + Share & embed card (Direct URL + iframe code). Sidebar nav entry "Reservations widget". The old `widget-api/*` API endpoints are unchanged (still the source of truth for slots/holds/confirms). Migration 052 rewrites existing live `booking_widget` block instances to `reservations_widget` in-place. The old `widget.eta` had a broken slot filter (`s.available_seats` field that doesn't exist), buggy date input, and dim-cell calendar — all rebuilt fresh.
- ✅ **Widget customisation — full control surface**. Tenant-level defaults at `/reservations-widget` and per-block overrides in the page builder, both with parity:
  - **Header** controls: tri-state show/hide, custom header text, sub-header text
  - **Button**: theme-role bg + fg colour pickers, corner radius slider
  - **Borders + radii**: theme-role border colour, card/chip radius slider
  - **Three independent typography tracks** — Body / Calendar / Time slots, each with its own FontPicker + size slider (px). Each can be a different Google Font; the widget builds a single deduped Google Fonts URL covering all picked fonts with per-font weight specs (no v2 400s)
  - **Calendar day colours** — Open day (bg/fg/border) and Closed day (bg/fg/border), all theme-role pickers
  - **Per-block anchor ID** — set `data.anchor_id` to anything URL-fragment-safe; section renders `<section id="<your-anchor>">` so any `<a href="#book-now">` link works
  - **Debug toggle** — tri-state on the block, boolean on the tenant defaults; surfaces a fixed black bar at top of widget showing live state + last action + last API URL/status. Also accepts `?debug=1` / `?debug=0` URL override
  - **Messages** — "Larger party" copy override (set to a single space to hide entirely)
  - All colour fields store theme role names (`'primary' | 'accent' | 'background' | 'surface' | 'text' | 'muted' | 'border'`) via `ThemeColourPicker`. SSR resolves role → hex against the tenant theme before reaching the widget. URL params carry hex directly.
- ✅ **`ThemeColourPicker` component** (`admin/src/components/website-builder/ThemeColourPicker.jsx`) — pill-strip showing the 7 theme roles as swatches rendered with their actual hex colours from `tenant_site.theme.colors`. Stores the role name (not hex). Optional "Inherit" swatch. Includes `resolveRole(value, theme)` helper that maps name → hex. Used by all colour controls on widget settings + block editor.
- ✅ **Reservations widget calendar — month-view date picker**. Replaced the native `<input type="date">` with a full month-grid calendar. Prev/next month arrows, today highlighted with an inset accent ring, day-of-week headers (Mon-first ordering, `(getDay() + 6) % 7`). Click a date → fires `pickDate(iso)` → calls `loadSlots()` → advances to slots step. "Jump to date" native input below the grid for fast multi-month navigation. Calendar pre-fetches a `GET /widget-api/venues/:id/schedule-summary` (returns `openDaysOfWeek: number[]` from the venue's weekly template) and greys out closed days with diagonal hatching — operators can't waste clicks on closed days.
- ✅ **Scrolling text block** — clean replacement for the broken `ticker` block. Loads its own Google Font directly via a self-contained `<link>` in the block markup (NOT relying on head.eta's font collector) and applies it with `!important` so no parent style can override. No legacy `font_style: 'sans'` branch. Default font Caveat. Six font sizes (sm to 3xl as multipliers of `--fs-base`). Theme-role bg picker. Optional text-colour role override. Per-instance unique class scoping. Migration 051 rewrites `ticker` → `scrolling_text` in existing live data.
- ✅ **Gallery block — Media library integration**. Replaced the disconnected `website_gallery_images`-based gallery with a `media_items`-driven block. Two source modes: "By category" (with `category_id`, blank = all images) and "Pick specific images" (chip-style multi-select preserving order). Three layouts: Grid (configurable columns + aspect ratio), Masonry (CSS multi-column flow), Horizontal (snap scroll). Per-block aspect ratio (1:1 / 4:3 / 16:9 / natural), gap, max-images cap, captions toggle, lightbox toggle. Built-in vanilla-JS lightbox with prev/next + ESC/click-out close, scoped per block. New backend resolver `loadGalleryItemsForBlocks(tenantId, blocks)` in `siteDataSvc.js` walks all gallery blocks pre-render, batches one DB query, and returns `{ [blockId]: items[] }`. Editor shows a live thumbnail preview of the resolved set.
- ✅ **Dynamic menu (`menu_inline`) block — full control surface**. Section filter (chip-style multi-select of menu sections, blank = all), specific dish filter (chip-style grouped by section), hide-prices toggle, column count (1-4 with Auto = use menu's print_columns), flow direction (Columns / Snake CSS-multi-column vs Rows / Wrap CSS-grid), show/hide section headers toggle, subheader text override with show/hide toggle. Section deletion / hide-prices changes preserve variant labels (joined with " · ").
- ✅ **Pre-prod block cleanup**. Dropped legacy blocks entirely: `ticker` (→ `scrolling_text`), `two_column` (→ `story_with_stamp`), `booking_widget` (→ `reservations_widget`). Each has an idempotent data migration that rewrites JSONB in `tenant_site.home_blocks` and `website_config.page_blocks`. PageBuilder templates updated. Block partial files deleted from disk. Total block roster: 22.
- ✅ **Per-tenant Widget settings JSONB column** *(migration 050)* — `tenant_site.widget_settings jsonb` stores tenant-level booking widget defaults. PATCH on `/website/tenant-site` accepts it; widget renderer fetches it and merges into `tenantWidgetDefaults` before rendering. Single column keeps the field set malleable.
- ✅ **Per-font Google Fonts weight map**. Earlier the Google Fonts URL builder requested every font with `wght@300;400;500;600;700;800` which v2 rejected as 400 the moment any picky font (Caveat 400-700, DM Serif Display 400-only, Libre Baskerville 400/700) ended up in the URL. Now both `head.eta` and `FontPicker.jsx` maintain matching `FONT_WEIGHTS` maps that specify the actual supported weight set per font. Widget partial has its own copy of the same map (so the embed widget can build its own URL deduplicated across body/calendar/slots tracks).
- ✅ **Columns block 1-4** — was 2-4. Default still 2.
- ✅ **Mobile responsive blocks**. Added scoped `_blockId` classes + `@media` collapse rules to `menu_inline`, `dish_list`, `reviews_band`, `story_with_stamp`. Header block now has per-element mobile visibility flags (`mobile_show_logo`, `mobile_show_brand`, `mobile_show_subtitle`, `mobile_show_links`, `mobile_show_cta`, `mobile_breakpoint`). `head.eta` body has `overflow-x: hidden` defensive guard.
- ✅ **Theme-only colour picker for all blocks**. Replaced hex colour inputs across `BookingWidgetEditor` → `ReservationsWidgetEditor`, `ScrollingTextEditor`, `WidgetSettings.jsx` → `ReservationsWidget.jsx` with `ThemeColourPicker`. Operators can no longer pick "off-palette" hex values; only the 7 theme roles. Backwards-compat: legacy hex values in existing data are detected (regex `^#[0-9a-fA-F]{6}$`) and passed through unchanged at render time.
- ✅ **SEO + AI discoverability** *(migration 055)*. Schema.org `Restaurant` JSON-LD on every tenant site page (address, geo, phone, hours, menu link, `servesCuisine`, `priceRange`); `BreadcrumbList` on location pages. Open Graph + Twitter Card meta. Canonical URL per page. `llms.txt` at root of every tenant domain (generated from published data; referenced in `robots.txt`). `cuisine` + `price_range` columns on `website_config`; both wired to the admin Website builder SEO section and to the JSON-LD emit.
- ✅ **Widget combo-to-combo displacement**. `tryDisplaceCombo` in `widgetApi.js` now tries free existing combinations as displacement destinations after individual tables fail. Slot-availability SQL subquery extended to match. Fixes false-positive availability and "Conflict" errors when conflicts can only be moved to a combo destination (e.g. T8+T9) rather than individual tables.
- ✅ **Admin relocate chain-displacement (second cascade level)**. `/relocate` step 6c: when a conflict has no free single table and no fully-free combo, the system tries combos whose every occupant can be moved to a free individual table — all in the same atomic transaction. Covers scenarios where e.g. T8+T9 is needed for a 4-person but T8 has a 2-person who can move to T11 once the primary booking vacates it.
- ✅ **Pre-configured combos bypass non-adjacent rule in `/relocate`**. Step 3b (find explicit combo containing target table) no longer applies the adjacency check. An operator-configured combination is always eligible as a relocation target regardless of sort-order gaps. The adjacency rule still governs step 3c (automatic expansion). Fixes 422 errors on venues with T3A/T3B-style split tables.
- ✅ **Timeline Manual Mode**. Amber `Hand` toggle in the timeline sidebar. When on, `/relocate` is called with `manual_mode: true`: allocation logic still runs (picks right table/combo) but conflict detection and cascade are skipped — booking placed directly, overlaps allowed. Persisted to `maca_timeline_prefs`. Overlap badges still show.
- ✅ **Long-press to access stacked booking** (5 s hold). `BookingCard` merges a 5 s pointer timer alongside dnd-kit's drag listener. If the finger drifts > 8 px, timer cancels and drag takes over. On fire, opens the drawer for the overlapping peer booking. `overlapPeerMap` computed in the `overlappingIds` useMemo; bridged via `overlapPeerMapRef` to avoid TDZ crash (memo is ~600 lines below the callback).
- ✅ **Widget confirmation page customization** — configurable heading text, rich-body HTML (TipTap WYSIWYG in the block editor), and up to N CTA buttons each with label + URL. Stored as `confirmHeading`, `confirmBodyHtml`, `cta_buttons[]` on the block/tenant-defaults data shape. SSR renders from `_confirmHeading`/`_confirmBodyHtml`/`_ctaButtons` vars resolved via `_pickStr()` (block → tenant-default → empty string). `CONFIRM_HEADING` / `CONFIRM_BODY_HTML` / `CONFIRM_CTAS` vars baked into the widget JS at render time.
- ✅ **Widget booking hold fixes** *(migrations 053 + 054)*. Migration 053 makes `booking_holds.table_id` nullable — the public widget creates holds at venue-level (no per-table selection) so the NOT NULL constraint was rejecting every widget hold. Migration 054 rewrites `confirm_hold()` to fix two bugs: (1) status check now excludes `no_show` + `checked_out` matching the slot resolver; (2) for combination holds the function now checks every member table is free, not just the first.
- ✅ **Reviews system** *(migration 056)*. `reviews` table with RLS (tenant_id). Three ingestion paths: Apify Google scraper (`POST /api/reviews/scrape` enqueues a BullMQ job via `reviewScrapeWorker.js` + `apifySvc.js`), manual entry (`POST /api/reviews`), CSV import (`POST /api/reviews/import-csv`). Moderation flags: `is_approved` + `is_featured`. Admin page at `/reviews` (sidebar nav). Stats summary (`GET /api/reviews/stats`). `google_place_id` column added to `venues` to tell the scraper what to fetch. New `reviews_band` block updated for media-library integration (images from actual reviews). Requires `APIFY_API_TOKEN` env var for scraping.
- ✅ **Dev tools suite** *(migration 057)*. Four platform/tenant tools:
  - **Backlog** (`/backlog`, platform-admin only) — 5-column Kanban (backlog/todo/in_progress/in_review/done). @dnd-kit drag between columns. Item types: epic/story/task/bug/spike. Priority + story points. Items can be promoted from issue log or feature requests.
  - **Issue log** (`/issues`, tenant-facing) — ITIL-style tickets. Impact × urgency matrix auto-derives P1–P4 priority. Categories: incident/problem/change_request/service_request. Platform admin can update status, add resolution notes, promote to backlog.
  - **Feature requests** (`/feature-requests`, tenant-facing) — upvote toggle (cross-tenant `feature_request_upvotes` table). Platform admin can set status (submitted/under_review/planned/in_progress/shipped/declined) and promote to backlog.
  - **Changelog** (`/changelog`, all users) — timeline list of published entries. Platform admin editor with publish/unpublish toggle. Tenants see only published entries.
  - New modules: `issue_log` (group: support), `feature_requests` (group: support), `changelog` (core: true). `support` group added to `MODULE_GROUPS`.
- ✅ **Curly-quote contamination fix in `reservations_widget.eta`** — the Edit tool introduced 153 U+2018/U+2019 typographic quotes into the template during confirmation-page development. Four of these were inside the `<% %>` server-side JS block (lines 105, 106, 115, 116), causing Eta to pass invalid token characters to V8 → "Invalid or unexpected token" 500 on every widget request. The remaining 149 were in the `<script>` HTML section where they would produce client-side JS errors. Fixed via Python byte-level global replacement (`\xe2\x80\x98`/`\xe2\x80\x99` → `\x27`). Diagnostic: `python3 -c "data=open('f','rb').read(); [(print(i,l)) for i,l in enumerate(data.split(b'\n'),1) if b'\xe2\x80\x98' in l or b'\xe2\x80\x99' in l]"`.
- ✅ **Venue auto-select fix (`EmailMonitoring.jsx`)** — `useQuery`'s `onSuccess` callback was removed in `@tanstack/react-query` v5, so the auto-select-first-venue logic silently never ran; `venueId` stayed `''` forever while the native `<select>` (no blank option) visually defaulted to the first venue, producing a dropdown/body-text mismatch. Fixed by deriving the default venue at render time (`selectedVenueId || venues[0]?.id || ''`), matching the existing `useVenueId` pattern in `Rules.jsx`. `WidgetTest.jsx` had the same dead `onSuccess` but was left as manual-select-only since it has an intentional blank "Select venue…" state.
- ✅ **SES Secret Access Key field added (`EmailTemplates.jsx`)** — the AWS SES provider settings only exposed an Access Key ID (mislabelled "API key") and AWS region; there was no input for the Secret Access Key the backend already expected in `provider_domain`, so per-venue SES credentials could never be fully configured. Added the missing field and relabelled the shared key input to "Access key ID" for SES.
- ✅ **Test data tool** *(migration 067)* — `/test-data` admin page (owner/admin only) for pre-prod QA: generate synthetic bookings across a date range at a target per-sitting occupancy %, or bulk-clear bookings by date range / venue-wide with a typed-`DELETE` confirmation and a dry-run count preview. New `test_data` module (group: `bookings`). See `src/routes/testData.js` in Key files.
- ✅ **Food safety logs** *(migrations 076, 077, 090)* — SFBB-style due-diligence logging per venue/day: equipment (fridge/freezer) temp checks, deliveries, hot/cold hold checks, cooking/reheat checks. Equipment and hold stations are drag-reorderable named entities (migration 090), not fixed enums. `/food-safety` admin page, `food_safety` module. See `src/routes/foodSafety.js` + `src/pages/FoodSafety.jsx` + `src/components/foodSafety/shared.jsx` in Key files.
- ✅ **Checklists** *(migration 083)* — fully operator-defined recurring checklists (opening/closing/cleaning/anything), any number of tasks, daily/weekly/monthly due schedule. `/checklists` admin page, `checklists` module. See `src/routes/checklists.js` + `src/pages/Checklists.jsx` + `src/components/checklists/shared.jsx` in Key files.
- ✅ **H&S Dashboard** *(migrations 086–088)* — customisable dashboards combining checklist + food-safety check widgets behind one date navigator; a widget is a thin pointer at the existing check data, not a separate model. `/hs-dashboard` admin page, `hs_dashboard` module. See `src/pages/HSDashboard.jsx` in Key files.
- ✅ **H&S Action Log** *(migration 092)* — general facilities/compliance to-do list per venue (repairs, records, training, cleaning), migrated field-for-field from the legacy spreadsheet's "ActionLog" tab. Categories are a tenant-managed reorderable list (seeded with the spreadsheet's four: Repairs, Records, Training, Cleaning), not a fixed enum. Not date-scoped — an entry stays open across days until ticked complete (records who/when, same convention as checklists). Photo attachments reuse the existing Media library rather than a new upload path. Also available as an `action_log` widget on the H&S Dashboard. `/hs-action-log` admin page, `hs_action_log` module. See `src/routes/hsActionLog.js` + `src/pages/HSActionLog.jsx` + `src/components/hsActionLog/shared.jsx` in Key files.
- ✅ **Dashboard → Overview tile system** *(migration 093)* — the home page ("Dashboard") is renamed "Overview" and rebuilt as a tile layout, same layout-control pattern as `hs_dashboard_widgets` (`col_span`/`height_px`/`sort_order`) but simpler — one implicit layout per tenant (Overview is tenant-wide, not per-venue, so no multiple-named-dashboards concept), plus per-role visibility (`hidden_role_ids`) mirroring `nav_items`. The four elements that used to be fixed page sections (quick access shortcuts, today's stats, upcoming bookings, venues status) are now tiles, in their original order, for every existing tenant. Two new tile types ship (not seeded by default — an owner/admin adds them via "Customise layout"): `hs_today_status` (combined Checklists + Food safety status for today, across venues, red/amber/green/grey) and `hs_week_status` (same, as a Monday–Sunday 7-day strip). Gated by the existing `dashboard` module (relabelled "Overview") — no new module. See `src/routes/dashboardTiles.js` + `src/config/defaultDashboardTiles.js` + `src/pages/Dashboard.jsx` in Key files.
- ✅ **Food safety / checklists polish** — drag-to-reorder added to the Equipment and Hold-stations management lists; the H&S Dashboard checklist widget's "Complete" action moved into the widget's own card header (was a full-width button at the bottom); Deliveries tab redesigned to an inline entry-form + current-week list with click-to-edit; H&S Dashboard container widened to `w-[90%] mx-auto` (was a fixed `max-w-6xl` cap, inconsistent with other wide admin pages); Cooking tab's category tabs and dish buttons restyled like till/POS buttons (fixed height, tighter width, `bg-muted/60` fill so tiles read as distinct tappable tiles instead of blending into the page background); H&S Dashboard date navigator made touch-friendly (fixed-width label so the toolbar doesn't reflow between "Today" and a full date string, 44px outlined prev/next arrows, confirmed the native `input[type=date]` overlay still opens the OS calendar picker).
- ✅ **Navigation designer + quick-access launcher** *(migration 091)* — replaces the previously-hardcoded `NAV_SECTIONS` array in `AppShell.jsx` with an admin-configurable, arbitrary-depth nav tree per tenant (`nav_items`, self-referencing via `parent_id`), with per-role visibility (`hidden_role_ids`, on top of the existing module-permission gating) and a tenant-wide `tenants.nav_style` toggle (`sidebar` / `launcher`). `/nav-designer` admin page is an outline editor (move up/down/indent/outdent buttons — not free-form drag-and-drop reparenting, which is much more fragile to build correctly for arbitrary depth). `GET /api/me` now returns `nav_tree` (pruned/filtered for the caller's effective role) and `launcher_tiles` (flat, filtered, sorted); `AppShell.jsx` renders these directly instead of a static array. Launcher mode (`/launcher`) replaces the whole sidebar with a tile grid built from links flagged "Show as a quick-access tile"; per-user tile order/visibility is personalised via `localStorage` (`maca_launcher_prefs_{tenantId}`), never a DB table, matching the existing `maca_settings`/`maca_timeline_prefs` pattern. New `nav_designer` core module (owner-only `manage` by default). See `src/routes/nav.js` + `src/config/defaultNav.js` + `src/pages/NavDesigner.jsx` + `src/pages/Launcher.jsx` in Key files.
- ✅ **Dashboard quick-access shortcuts kept in sync** — the Dashboard page's own `SHORTCUT_OPTIONS` picker (a separate, older feature from the Navigation designer above — per-user shortcuts from a fixed catalog, always available regardless of `nav_style`) had never been updated to include Food safety, Checklists, or H&S Dashboard since those modules shipped. Added all three.
- ✅ **Wages UX overhaul + cash-only net balance** *(migration 093)* — five changes to `/cash-recon`'s Wages page: (1) each staff entry redesigned from 4 stacked blocks into a single compact grid row (Staff / Type / Hours / Rate / Total / Cash paid / Notes / Remove); (2) new `cash_wage_defaults` table (`tenant_id, venue_id, staff_id, entry_type, sort_order`, `UNIQUE(venue_id, staff_id)`) lets an operator "Set as default" on the current week's staff list (who's included + Fixed/Hourly) so future weeks with no saved entries auto-populate from that instead of the full active-staff roster — falls back to the full roster if no default has been set yet; ad-hoc (staff_id-less) entries are never included in the default since there's no stable identity to match in a future week; (3) removed the "Load template" button and its `window.confirm()` popup (Standard Design Rule #4 violation) — replaced by the non-destructive "Set as default" action; (4) cash recon's net-position calculation (`weekNetPosition()` in `SpreadsheetView`, and the Wages summary card in `WeekView`) now subtracts only `wages_cash_total` (the `cash_amount` column sum) rather than `wages_total` (the full wage cost) — a wage paid partly or fully by bank transfer no longer drags down the cash net position; (5) an amber `AlertTriangle` flag appears wherever wages are shown in the recon view whenever a week's `total_wages !== total_cash_wages`. New route `POST /:venueId/cash-recon/wages/:week_start/set-default`; `GET /:venueId/cash-recon/config` now also returns `wage_defaults` (joined with staff name/rate); both wage-total queries in `cashRecon.js` now select `cash_amount` sums alongside `total`.
- ✅ **Mobile PWA — /mobile section + H&S Dashboard** — a phone-portrait area of the same admin SPA at `/mobile/*`, sharing the existing Auth0 session/API/deploy pipeline (no new Nginx/DNS/Auth0 app). `main.jsx` mounts a second top-level route tree (`<Route path="mobile" element={<MobileShell />}>`) alongside the existing `<AppShell />` tree, both inside the same `RequireAuth`/`TenantGate`. `src/components/mobile/MobileShell.jsx` is a minimal header-only layout (no sidebar) that swaps the page's PWA identity to "Macaroonie Ops" (its own `admin/public/mobile.webmanifest`, `mobile-pwa-192/512.png`, `mobile-apple-touch-icon.png`, teal theme colour) while a `/mobile` route is mounted, by rewriting the existing `<link rel="manifest">` / `<link rel="apple-touch-icon">` / `<meta name="theme-color">` / `<meta name="apple-mobile-web-app-title">` tags on mount and restoring them on unmount — no second `vite-plugin-pwa` instance, since there's only one `index.html` to inject into. `src/mobile/registry.js` (`MOBILE_MODULES`) drives the `/mobile` hub screen (`MobileHub.jsx`) — add an entry + a route to ship a new mobile module, no shell changes needed. First module: `src/pages/mobile/MobileHSDashboard.jsx`, a full-functionality single-column rebuild of `HSDashboard.jsx` (same dashboards-as-tabs, same add/remove/reorder-widget mutations, same six widget types, same `/api/hs-dashboards/*` endpoints, same shared body components) that deliberately ignores `col_span`/`height_px` and renders one full-width column at natural height so the page itself is the only scroll region. `DashboardModal`, `AddWidgetModal`, `WIDGET_TYPE_BY_KEY` are exported from `HSDashboard.jsx` and reused rather than duplicated.
- ✅ **Wages default-list bug fix** — a brand-new week (never saved before) failed to auto-populate from the venue's default staff list, because the frontend's `useEffect` guard (`if (!wagesData || !config) return`) treated a resolved `null` (the API's "confirmed — no wage report yet" response) the same as `undefined` ("query still loading"). Fixed by checking `wagesData === undefined` specifically.
- ✅ **Website theme — tenant-customisable boxed-inset margin steps** — the 5 boxed-inset steps (previously a hardcoded `{1:16, 2:24, 3:40, 4:64, 5:96}` px scale shared verbatim across `head.eta` and all 19 `api/src/views/site/blocks/*.eta` partials) are now redefinable per tenant via `theme.spacing.boxed_steps` — exactly 5 `{ value, unit: 'px' | '%' }` entries, mixing units freely (e.g. step 1 = 20px for mobile portrait, step 3 = 5% for everything else). The step *numbers* (1-5) referenced everywhere — theme default (`boxed_step`), the existing mobile-portrait-only override (`boxed_step_mobile`), and every block's own per-block override — are unchanged; only what each number resolves to is now configurable. Falls back to the standard scale when unset. Admin canvas parity via `boxedLayout.js`'s `setBoxedSteps()`, called once per render by `themeResolver.js`. New "Boxed inset — step values" editor in `BrandLayoutFields.jsx` (Website → Brand & theme → Layout); the existing default-step and mobile-override pickers now show each step's live value. See Website CMS section in Docs.jsx for the full wiring.
- ✅ **Admin portal — website brand accent for section header/footer bars** — `GET /api/me` now also returns `site_theme` (the tenant's public-website brand colours, `tenant_site.theme.colors`, resolved with `head.eta`'s own default fallbacks). `SettingsContext.applySiteTheme()` writes `--site-primary` / `--site-accent` / etc CSS variables — a namespace deliberately separate from the admin's own `--primary` (operator's Settings colour pick) and `--accent` (neutral hover-state grey used everywhere via `hover:bg-accent`). `CashRecon.jsx`'s `SectionCard` (Income, Service Charges & Tips, Takings, Wages, Expenses) now has a coloured header bar plus a new optional `footer` prop rendering a matching coloured footer bar, both using `--site-accent`, so each section reads as a clearly bounded card instead of blending into a flat grey wall of inputs.
- ✅ **Menu inline canvas preview — variant visibility parity fix** — `MenuInlineCanvas` (the page-builder's live preview for the Menu (inline) block, in `dataBlocks.jsx`) ignored a menu's `hide_zero_priced_variants` / `hide_unpriced_variants` toggles, which `menu_inline.eta` (the actual SSR partial) already applied correctly — so an operator editing a page saw variants/prices the published site actually hid. Ported the same filtering + price-formatting logic into the canvas component. `dish_list.eta` (the other menu-shaped block) is unrelated — it's a free-form curated dish list with plain string prices, not backed by the structured menu/variant data model.
- ✅ **Food safety — temp reading pre-fill + touch keypad + cooking-check edit** — three related changes to `foodSafety/shared.jsx`: (1) `TempCell`/`HoldCell` pre-fill an unlogged reading with `min_temp_c` (lower end of that equipment/station's configured range) instead of a blank field with only a ghost placeholder — display-only, nothing saves until the cell is actually touched; (2) new `TempKeypadModal` — a full-screen, IS_TOUCH-gated custom on-screen keypad (digits, one decimal place, a `±` sign toggle for negative freezer readings) replacing the native `<input>` on touch devices, since at least one Linux/Chrome tablet never surfaces its own on-screen keyboard for this input type (a platform limitation `inputMode="decimal"` can't fix — same reasoning as the existing covers-entry custom keypad in `NewBookingModal.jsx`). Applied to `TempCell`, `HoldCell`, and `CookingEntryModal`'s core-temp field; (3) each row in the Cooking tab's "Today's checks" list is now a button — tapping it reopens `CookingEntryModal` pre-filled from that check (`target.existingCheck`) so an already-logged core temp can be corrected; `persist()` already routed to `onUpdate` whenever `checkId` was set, so this only needed the state-initialisation path.
- ✅ **Menu item calories + section category image** *(migrations 094 + 095)* — two additive, nullable fields on the structured menu builder: `menu_items.calories` (int, kcal) shown next to the price wherever price shows; `menu_sections.image_url` (a small category icon/image next to the section heading, deliberately capped at `1.3em`/`1.6em` so it never renders bigger than the heading font regardless of the uploaded image's actual resolution — `SectionImagePicker` in `Menus.jsx` is a compact variant of the existing dish `ItemImagePicker`). Wired through all the places the whole-tree bulk-upsert touches: `ItemBody`/`SectionBody` Zod schemas, `loadMenuFull()`, `upsertMenuTree()`, the `/duplicate` route's re-mapping, and three render surfaces (`menu_inline.eta`, `menu_print.eta`, `MenuInlineCanvas` in `dataBlocks.jsx`) kept in parity. Neither field has a show/hide toggle — they render automatically whenever set, matching how `notes`/dish `image_url` already behave. First-ever Help.jsx/Docs.jsx sections for the Menus feature — none existed before this (a pre-existing documentation gap, not something this change caused).
- ✅ **Cooking check delete** — `DELETE /api/food-safety/cooking/:id` hard-deletes a `fs_cooking_checks` row (no `is_active` column on this log table, nothing else references its id, so a plain DELETE is correct — unlike the config-entity soft-deletes elsewhere in this file). `CookingEntryModal` shows a "Delete this reading" control (inline double-confirm, Standard Design Rule #6) only when editing an existing check (`checkId` set); wired through a new `deleteCheck` mutation in `CookingChecksPanel` that invalidates the day's query and closes the modal on success.
- ✅ **Dashboard widget height enforcement** — `HSDashboard.jsx`'s `WidgetCard` and `Dashboard.jsx`'s `TileCard` both changed their body wrapper from `style={{ maxHeight: heightPx }}` to `style={{ height: heightPx }}` (keeping `overflow-y-auto`). A `maxHeight` let a widget/tile with little content shrink below the size the operator explicitly set with the resize controls; `height` enforces that size in both directions — short content leaves blank space, long content scrolls — matching what "set a height" should mean on a customisable grid.
- ✅ **H&S today tile — full breakdown** — `computeHsStatus()` in `dashboardTiles.js` now returns, per venue per day, `checklists: [{id, name, frequency, completed}]` (one entry per active template — checklists have no "unresolved" concept, just done/not-done) and `categories: [{key, label, expected, completed, unresolved, status}]` (one aggregate entry each for equipment/hold/cooking checks, included only when that category has anything configured for the venue). Computed from data already loaded for the existing expected/completed sums — no new queries. `Dashboard.jsx`'s `HsTodayStatusBody` renders this as a real breakdown list (grouped by venue when there's more than one) instead of the old venue-only rollup that only showed when >1 venue.
- ✅ **Checklist reopen in dashboard widgets** — the H&S Dashboard's checklist widget (and its mobile counterpart) could mark a checklist complete but never reopen it — the full-page `ChecklistRunPanel` already had a working double-confirm reopen flow, it just wasn't exposed in `hideHeader` mode (the mode the widget cards use). Fixed by adding `reopen: () => save.mutate({ markComplete: false })` to the `onStateChange` payload; `HSDashboard.jsx`'s `WidgetCard` and `MobileHSDashboard.jsx`'s `MobileWidgetCard` each hold a local `confirmReopen` boolean and render the same inline double-confirm in their compact header (tap the green "Complete" badge → "Yes, reopen"/"Cancel").
- ✅ **Cash Recon SectionCard — header-only accent, plain footer, spacing fix** — reverted the footer's coloured band back to plain (`bg-muted/30`, no accent) per feedback that two accent bars per card read as too heavy; the header keeps its `--site-accent` colouring. Also fixed zero-visible-gap between stacked section cards on the daily-declaration page: `SectionCard`'s own `mb-4` and the page wrapper's `space-y-0` were two competing spacing mechanisms, and the effective gap was zero — removed `mb-4` from the component, changed both `space-y-0` wrappers to `space-y-4` so the page's own layout container is the single source of spacing.
- ✅ **H&S Dashboard widget headers — brand accent colour** — `WidgetCard` (desktop) and `MobileWidgetCard` (mobile) headers now use the same `--site-accent-soft`/`--site-accent` theming as Cash Recon's `SectionCard`, instead of a generic `bg-muted/30`. Required `MobileShell.jsx` to fetch `GET /me` and call `applySiteTheme()` itself — `/mobile` never mounts `AppShell`, so without this the site-accent CSS variables were never set for a session opening straight into `/mobile` (e.g. the installed PWA's own start URL), and the new accent header would have silently shown the hardcoded fallback colour.
- ✅ **Mobile Expenses module + `POST /cash-recon/expenses` bug fix** — new `/mobile/expenses` page: venue/date pickers, a day's running total, tap-to-edit expense list, and an add/edit modal with category chips + a `capture="environment"` camera input for the receipt photo. Building it surfaced that `POST /:venueId/cash-recon/expenses` had required a `report_id` UUID that its only caller (`CashRecon.jsx`'s `ExpensesSection.handleAdd()`) never had in scope and never sent (it sends `date` instead) — every "Add expense" click on the desktop page has been silently 400-ing since it shipped, swallowed by an empty `catch {}`. Fixed the endpoint to take `date` and find-or-create the daily report row itself (same upsert pattern `PUT /daily/:date` uses for its header), also blocking adds to an already-submitted report. Both the desktop form and the new mobile page call this same, now-working endpoint. See Docs.jsx's Mobile App section for why this wasn't just routed through `PUT /daily/:date` instead (that endpoint replaces income/takings/SC wholesale from the request body — unsafe for a single-expense write).
- ✅ **SC sources — independent add/deduct direction per side** *(migration 096)* — replaced `cash_sc_sources.included_in_takings`/`included_in_income` (booleans, fixed direction each — see the superseded Common-mistakes entry above) with `takings_effect`/`income_effect`, each `'none' | 'add' | 'subtract'`. Lets an operator deduct a service charge from Takings (or add one to Income) — combinations the old fixed-XOR model couldn't express. `scEffectAmount(effect, amount)` in `CashRecon.jsx` is the one place the sign mapping lives; both `variance(date)` (week grid) and the `scAdjustment` useMemo (day view) call it identically, so there's still only one implementation of the adjustment math. Admin editor: two selects ("Effect on Takings" / "Effect on Income") replace the two toggles; the week-grid source row shows a `↳ takings +`/`↳ income −`-style badge per active effect instead of a bare `↳ takings`/`↳ income` tag. Migration maps old booleans onto the new columns so every existing source computes an identical adjustment number before and after — see Docs.jsx for the exact mapping and why it looks asymmetric.
- ✅ **Cash Reconciliation variance sign fixed** — variance was computed as Income − Takings everywhere (`variance(date)` in the week-grid `SpreadsheetView`, the day-view `const variance`, and a third, independent recomputation in `WeekView`'s 7-day cards that additionally referenced a dead field, `d.total_sc_in_takings`, never present in the `/week/:week_start` response). That sign reads backwards from the standard accounting convention (Actual − Expected): taking in MORE than declared showed as a NEGATIVE number. Fixed by flipping to `variance = Takings − (Income + scAdjustment)` in both `SpreadsheetView` and the day view, and by making `WeekView`'s day cards consume the `/week/:week_start` endpoint's own `variance` field directly (`total_takings − total_income`, which was already correctly signed — only the client-side recompute was wrong) instead of recomputing with the dead field. The existing amber/red colour comparisons (`variance > 0` amber, `< 0` red) needed no changes — flipping the underlying number's sign alone makes shortfalls (the more serious case) land on red and surpluses on amber, which they hadn't reliably done before.
- ✅ **Net Cash / Cash to bank — cash-only channels** *(migration 097)* — `cash_payment_channels.counts_as_cash` (boolean, defaults to `true` only for `type = 'cash'`, backfilled from existing `type` values) lets an operator flag which Takings channels actually become physical cash in the till. Card, voucher and online-link channels default off. `Net Cash` and the renamed `Cash to bank` row (see below) in both `SpreadsheetView` (week grid) and the day view now sum only `counts_as_cash` channels (`cashTakingsTotal(date)`/`cashChannels` in `CashRecon.jsx`) — `Total Takings` and `Variance` are unaffected and still sum every active channel. A "Non-cash" badge renders next to any such channel wherever it's listed (week-grid row label, day-view Takings list). Toggle lives in Cash Recon settings → Payment channels, defaulting to the channel's `type` when the operator hasn't touched it yet.
- ✅ **"Net Position" renamed to "Cash to bank"** — the week-grid's final wage-adjusted row (`weekNetPosition()`, unchanged formula: cash takings − expenses − cash wages) now reads "Cash to bank" in the UI. The day-view's per-day "Net Cash Position" summary line (no wages, wages are weekly-only) is unrenamed — it's a different, simpler figure and keeping distinct names avoids implying it's the same "money ready to bank" total.
- ✅ **Boxed inset — per-step mobile values** *(spacing.boxed_steps entries)* — each of the 5 tenant-configurable boxed-inset steps can now carry an optional `mobile_value`/`mobile_unit`, applied only on phone portrait (≤600px). Implemented by resolving every consumer — the theme default (`--boxed-pad`) AND every per-block `boxed_step` override — through 5 shared CSS variables (`--boxed-step-1` … `--boxed-step-5`) emitted once in `head.eta`'s `:root`, with a single `@media (max-width: 600px) and (orientation: portrait)` block overriding whichever steps have a `mobile_value` set. This is a one-source-of-truth improvement over the previous design, where only the theme-level default could differ on mobile (via `boxed_step_mobile`, choosing a different step INDEX) and every per-block override was "fixed at all sizes" — per-block overrides now automatically inherit mobile-responsiveness for free since they resolve through the same vars. `boxed_step_mobile` (swap to a different step index on mobile) still works, composing on top of any per-step mobile values. Mirrored in the admin canvas (`boxedLayout.js`'s `boxedStepVarCss`/`boxedStepVarsCss`/`boxedStepMobileVarsCss`, `themeResolver.js`, `ThemeFrame.jsx`) for canvas/SSR parity, though the canvas's "Approximate mobile preview" toggle narrows a div rather than the real viewport, so it still won't retrigger the media query — only a genuinely narrow browser window does (pre-existing limitation, unrelated to this change). Editor: `BrandLayoutFields.jsx`'s per-step cards gained a "Mobile" checkbox that reveals a second value/unit input, defaulting to the step's own desktop value when first ticked.
- ✅ **Website theme — background image** — new `theme.colors.background_image` (nullable URL, alongside the existing `colors.background` hex). Renders on `html, body` as `background-image` with `cover`/`center`/`no-repeat`/`fixed`, sitting behind the flat background colour. Editable via `ImageField` in `BrandThemeSection`'s Colours card (the actually-rendered tenant-brand theme editor — NOT the same-named but unreferenced `ThemeSection` function earlier in `Website.jsx`, which is dead code left over from a prior refactor). Mirrored in `themeResolver.js`/`ThemeFrame.jsx` for canvas parity. Column-overwrite semantics apply as with the rest of `theme` — see the existing "theme JSONB is column-overwrite" gotcha.
- ✅ **Venue "Hero" nav entry renamed to "Identity"** — `PAGES_VENUE`'s `branding` nav item was labelled "Hero" but its component (`BrandingSection`) has only ever configured site name, tagline, logo and favicon — never hero image/overlay content, which lives entirely in the Hero block and the theme's Hero settings (`theme.hero.*`). The "Hero" label was a stale leftover from before the block-based redesign; relabelled to match what it actually does. The section itself was kept (removing it would have deleted the only way to edit a venue's per-location site name/tagline/logo/favicon). **Superseded the same day** — see the next entry; this section was removed entirely once it turned out (checking the actual render path) that its site_name/tagline fields were dead and its logo/favicon fields are better served by the new full override below.
- ✅ **Brand & theme — per-venue site overrides, replacing the sparse inheritance model + the venue "Identity" page** *(migration 098)* — a multi-venue tenant can now give an individual venue its own complete Brand & theme (identity, colours, typography, layout), separate from a single, simpler "everyone shares the tenant default" state for single-venue tenants. Investigating the user's "why do we need Identity if we have Brand & theme" question surfaced that the venue-level Identity page's `site_name`/`tagline` fields were saved but **never actually read by any renderer** — `mergeLocationConfig()` only ever inherited `logo_url`/`favicon_url` per venue (`BRAND_INHERITABLE`), and separately `brandTheme.js`'s `mergeThemes()` forced tenant `colors`/`typography` to always win over a venue's own theme regardless, specifically to paper over an earlier bug where a stale venue theme silently overrode a template's fonts/colours on the live site. Replaced both mechanisms with one explicit boolean, `website_config.use_brand_override` (default `false`): off, the venue's brand/theme fields (`BRAND_OVERRIDE_FIELDS` in `siteDataSvc.js` — site_name, tagline, logo_url, favicon_url, primary_colour, secondary_colour, font_family, template_key, theme) are ignored entirely and the tenant's apply as-is; on, the venue's own values apply wholesale, no blending. `mergeThemes()`/`brandTheme.js` deleted (zero remaining callers). Admin UI: `BrandSection` (`Website.jsx`, rendered under `tenant-brand`) shows a pill selector — "Default" + one pill per venue with an active override — only when the tenant has more than one venue; **+ Add site override** picks a venue, `POST`s to ensure its `website_config` row exists, then `PATCH`es a snapshot-copy of the tenant's current values plus `use_brand_override: true` (a copy, not a live link — later tenant-default edits don't retroactively change an existing override); **Remove override** (inline double-confirm) just flips the flag back off, leaving the venue's stored values in place in case it's re-added later. `BrandIdentitySection`/`BrandThemeSection`/`BrandLayoutSection` all take an optional `venueId` prop (`null` = tenant default via `/website/brand-defaults`, a venue id = that venue's `website_config` via `/website/config?venue_id=X`) — same form fields either way. The standalone per-venue "Identity" page (`PAGES_VENUE`'s `branding` key, `BrandingSection` component) is deleted — it's fully superseded, and covered strictly less than the new override (no theme/typography/layout, and its site_name/tagline never worked as described above).
- ✅ **Expense VAT amount** *(migration 099)* — `cash_expenses.vat_amount` (numeric, default 0) alongside the existing (gross) `amount`, purely informational: never added to or subtracted from anything in reconciliation — `amount` already covers VAT everywhere it's used (`total_expenses`, `Net Cash`/`Cash to bank`). All three write paths accept it (`ExpenseEntrySchema` in the whole-day `PUT /daily/:date`, plus the individual `POST`/`PUT /expenses[/:id]` routes). `CashRecon.jsx`'s `ExpensesSection` and `MobileExpenses.jsx`'s `ExpenseModal` both gained a second "VAT amount" input next to "Gross amount" (relabelled from plain "Amount"), and show a small "VAT £x" line under the gross figure wherever an expense is listed.
- ✅ **Week-grid day headers — visible "Open" affordance** — `SpreadsheetView`'s day column headers (`Tue 22 Sep` etc) were already a full click target (`onClick={() => onSelectDay(date)}`, hover/active background, `cursor-pointer`) but nothing in the cell visually signalled it was clickable beyond the hover state, which touch devices never see. Added a small always-visible "Open ›" line under the status badge in each header cell so the affordance doesn't depend on hover.
- ✅ **`TempChecksTable`/`HoldChecksTable` mobile layout fix** — on a phone-width viewport (the `/mobile/hs-dashboard` Fridge/Freezer and Holds widgets), the sticky Equipment/Station name column's `bg-muted/50` header background was translucent, so as the table scrolled the next header's text (Range, then the first capture-time column) visibly bled through underneath it — the reported "EquipmentRange" overlap. Fixed by (1) making the sticky header cell's background fully opaque (`bg-muted`, no `/50`), (2) hiding the Range (min…max guideline) column below the `sm:` breakpoint, same treatment already used for the Type column, and (3) letting the equipment/station name wrap (`whitespace-normal break-words max-w-[110px]`) on mobile instead of forcing a single `whitespace-nowrap` line, reverting to the old one-line/no-cap behaviour at `sm:` and above. Net effect: on mobile only the sticky name + capture-time columns render, narrow enough that the temp/hold entry column is visible without horizontal scrolling. Both tables are the single shared implementation (per the note on this file) — fixed once, applies to `FoodSafety.jsx`, desktop `HSDashboard.jsx` widgets, and `MobileHSDashboard.jsx` alike.
- ✅ **Tenant selection — login UX overhaul** — investigating the request surfaced that the codebase had already migrated login to identity-only Auth0 (no `organization` param, `TenantGate` resolving the restaurant client-side via `X-Tenant-Id`) in an earlier session that never updated CLAUDE.md's "Auth flow" section or the Docs.jsx "Authentication" section — both still described the original organisation-scoped-login design. Docs corrected (see "Auth flow" above and Docs.jsx). On top of that pre-existing foundation, three behavioural gaps were closed: (1) a single-tenant user could see a one-frame flash of the "Choose a restaurant" picker before the auto-select effect ran — fixed by computing the auto-select candidate at render time (not just inside the effect) and holding the loading state until it resolves, so the picker only ever renders for a genuine multi-tenant, no-valid-history case; (2) the full-page "Choose a restaurant" list is now a proper modal (`TenantSwitcherModal`, new shared component) rather than a bespoke full-screen layout — mandatory (no close) when it's the first-login picker, dismissible when opened voluntarily; (3) the sidebar's tenant `<select>` dropdown (a Standard Design Rule #2 hover/tap-imprecision concern on a tablet) is replaced by a "Tenant" button that opens the same modal — collapsed-sidebar mode now opens the modal directly instead of first expanding the sidebar. "Check last one and log into it" was already correct behaviour (the `maca_selected_tenant` localStorage pick persists across sessions and is re-validated against `/me` on every load) — no backend change was needed for that part.
- ✅ **Mobile Order Sheets — third `/mobile` module** — `admin/src/pages/mobile/MobileOrderSheets.jsx`, registered in `mobile/registry.js`. Unlike the first two mobile modules, this one needed no body-component rework: the desktop `OrderSheets.jsx` page's list/detail pattern already collapses to a single-column, full-page-detail-on-select layout at phone width (the list is `hidden md:flex` once an order is selected — the same responsive approach `Bookings.jsx`'s `inlineMode` uses), and the item table's horizontal scroll-on-overflow is a fine mobile pattern as-is. `OrderCard`/`OrderDetail`/`NewOrderModal`/`FilterModal`/`ALL_STATUSES`/`DEFAULT_STATUSES` were exported from `OrderSheets.jsx` (previously module-local) and reused verbatim — same `/api/order-sheets/*` endpoints, same autosave/status/delete mutations. Only the shell differs: no page-level title bar or `pl-14` AppShell-hamburger spacing (both irrelevant under `MobileShell`, which already supplies a back-arrow header with the module title), and the list/detail split is strict either/or on mobile (no resizable side-by-side panel, since there's no room for one on a phone). Building this surfaced that the entire Order Sheets feature (templates, categories, orders) had zero coverage anywhere in CLAUDE.md, Help.jsx, or Docs.jsx since it shipped — same "no doc gap flagged ≠ no doc gap exists" pattern as the earlier Menus gap; baseline sections added to all three docs alongside the mobile module itself.
- ✅ **Mobile-view suggestion modal** — `admin/src/components/MobileSuggestModal.jsx`, mounted in `AppShell` (desktop shell only). Detects a touch device in portrait at ≤700px on mount and on a live `matchMedia` orientation-change listener (so rotating into portrait mid-session also triggers it, not just page load), and offers a large centred modal pointing at `/mobile` — "Open mobile view" (navigates there) or "Stay on this page" (dismiss). Either choice sets `maca_mobile_prompt_dismissed` in localStorage so it only ever offers once per browser.
- ✅ **Overview page responsive fix + persistent mobile/standard toggle** — three related fixes surfaced by using the admin at phone width: (1) `Dashboard.jsx`'s page header (`px-6 h-14 border-b`) had no left-side allowance for `AppShell`'s fixed collapsed-sidebar hamburger button (`fixed top-3.5 left-3.5 ... lg:hidden`), so on any viewport below `lg` the "Overview" title/date sat directly under the hamburger icon — fixed by adding `pl-14 lg:pl-6` (the same `pl-N lg:pl-4`-style pattern `OrderSheets.jsx`'s header already used) plus `min-w-0`/`truncate` on the title block and a `hidden sm:inline`/`sm:hidden` pair shortening "Customise layout" to "Customise" on narrow screens so it never wraps against the title. (2) The tile grid was a fixed `style={{ gridTemplateColumns: 'repeat(4, minmax(240px, 1fr))' }}` — at phone width this either forced 4 columns @ 240px+ each (960px+, horizontal scroll) or, if browsers shrank below the minmax floor, badly squeezed tiles; replaced with Tailwind `grid-cols-1 sm:grid-cols-2 lg:grid-cols-4` plus a `SPAN_CLASSES` lookup (`{1: 'col-span-1', 2: 'col-span-1 sm:col-span-2', 3: '... lg:col-span-3', 4: '... lg:col-span-4'}`) on `TileCard` so a tile's configured width (1-4 "quarters") degrades gracefully to half-width or full-width instead of overflowing a grid that doesn't have that many columns yet at that breakpoint. Main content padding also dropped from a flat `p-6` to `p-4 sm:p-6`. (3) New `src/components/MobileViewToggle.jsx` — see Key files — gives every phone-width visitor a persistent way to jump between the desktop admin and `/mobile`, since previously `MobileSuggestModal` only offered a one-time nudge on first detection and `/mobile` itself had no way back to the full admin at all short of editing the URL.
- ✅ **Mobile Cash Up + Mobile Wages — fourth and fifth `/mobile` modules** — see `src/pages/mobile/MobileCashUp.jsx` and `src/pages/mobile/MobileWages.jsx` in Key files for the full write-up. Cash Up gives a plain day-by-day list (status/income/variance) for the current week with tap-through to the full daily declaration; Wages narrows the desktop's 8-column staff grid down to just Name / To be paid / Paid per entry, the two figures that matter when an operator is settling up away from a desk. Both reuse existing `/cash-recon/*` endpoints with no backend changes. Required exporting several previously module-private helpers from `CashRecon.jsx`: `fmt`, `parseNum`, `getMonday`, `isoWeekDates`, `StatusBadge`, `SaveIndicator`, `DayView`.
- ✅ **Sidebar header now shows the tenant name, not the "Macaroonie" wordmark** — `AppShell.jsx`'s expanded-sidebar header used to be a static `MacaroonIcon` + "Macaroonie" label, with a separate "Tenant" block underneath repeating the current tenant's name in its own bordered button. Collapsed into one row: the header now shows the current tenant's name directly next to the logo, with a small `ChevronDown` arrow only when `available_tenants.length > 1` (`canSwitchTenant`) — tapping the name or the arrow opens `TenantSwitcherModal`, same as before. A single-tenant user sees their tenant's name as a plain (non-interactive) label, since there's nothing to switch to; a user with no resolved tenant yet (`available_tenants.length === 0`, e.g. platform admin outside any tenant context) still sees "Macaroonie" as a neutral fallback. The old dedicated "TENANT" strip below the header is gone — its one remaining job, warning when `hasTenants` is true but nothing is currently selected (a rare edge case; `TenantGate`'s mandatory picker normally prevents reaching `AppShell` in this state at all), is now a single amber banner row directly under the header instead of a whole labelled section. The collapsed-sidebar (icon-rail) tenant chevron button is unchanged.
- ✅ **Fixed the fixed hamburger button being completely hidden (and unclickable) on Cash Recon at phone width** — `AppShell.jsx`'s collapsed-sidebar hamburger button was `z-10`, but `CashRecon.jsx`'s own page headers use `sticky top-0 z-10` (four views) or `z-20` (the spreadsheet view) — equal or higher than the hamburger, and a `position: sticky` header creates its own stacking context, so it painted fully over the hamburger rather than just visually overlapping it like Dashboard's title did (see the earlier, milder version of this bug). The practical effect: a phone-width user landing on Cash Recon had no way to open the sidebar at all — the fixed button was there, but invisible and un-tappable underneath the page's own header. Fixed two ways: (1) `AppShell`'s hamburger button raised to `z-40`, safely above every in-page header found anywhere in the app (the highest so far is `z-20`) and below true modals (`z-50`), so it's never coverable by page content again regardless of that page's own z-index choices; (2) all 5 of `CashRecon.jsx`'s page-level sticky headers (`SpreadsheetView`, `WeekView`, `DayView`, `WagesView`, `SettingsView`) given the same `pl-14 lg:pl-4` treatment as `OrderSheets.jsx`/`Dashboard.jsx`, so the button doesn't visually sit on top of the venue selector/title once it's actually visible. **Other pages with the same `sticky top-0 z-10`-or-higher page-level-header shape have NOT been individually audited yet** — grep `sticky top-0 z-1\|sticky top-0 z-2` across `admin/src/pages` for the remaining candidates (`Menus.jsx`, `OrderSheetTemplates.jsx`, `Website.jsx`, `Timeline.jsx` are plausible page-level instances; several other hits are nested `<thead>`/list-group headers that don't sit at the literal top of the page and are not at risk). The z-40 bump makes the button clickable everywhere regardless, but those pages may still want the `pl-14` visual cleanup — see Outstanding items.

---

## Outstanding items

### Build next (larger features)

**1. Booking widget — Stripe deposit flow**
The free-booking widget is shipped (`/widget/:venueId` + `/widget-api/*`).
Deposit-required venues currently throw 422 on `confirm`. Wire the existing
`/payments/intent` Stripe flow into the public widget: Payment Intent created
on hold, public Stripe.js form, webhook → confirm_hold. Same UX, just an
extra step before Confirmed.

**2. GloriaFood sales integration**
*(paused — needs API access details from the user)* Pull order data from GloriaFood
for sales reporting and reconciliation. Architecture decisions pending: Partner API vs
Restaurant API, polling vs webhook, per-venue vs tenant-level credentials, where it
displays in the admin UI.

**3. Header & Footer as page-builder blocks**
The site header and footer are currently template-baked (`templates/{key}/partials/header.eta` + `footer.eta`) and read auto-derived data: tenant_site logo + nav from venues/menus/pages, social_links, address, etc. Operators want them editable AS BLOCKS in the page builder so they can add/remove nav links, swap CTA copy, restructure footer columns, surface custom social handles. Required:
- Two new block types: `header` and `footer`. Each has rich data shape (header: `{ logo_url, brand_text, nav_items: [{label,url}], cta: {text, url} }`; footer: `{ columns: [{title, items: [{label,url}]}], copyright, show_social, show_powered_by }`).
- React canvas components for both, with editor controls (add/remove link, drag-reorder).
- Eta block partials per template (`templates/{key}/blocks/header.eta`, `footer.eta`) so each template renders its own signature chrome.
- Rendering shift: `index.eta` and `location.eta` STOP including `partials/header.eta` / `partials/footer.eta`. Instead, the home_blocks/page_blocks array renders, and we auto-prepend a `header` block + auto-append a `footer` block when missing (so the user always gets working chrome out of the box).
- Page builder UX: header block should be locked at index 0, footer at the last position (no drag). New "+" inserter sits between them.
- Picking a page template seeds blocks with header + footer at the expected positions.

**4. RBAC rollout to existing routes**
The foundation (migration 038, `requirePermission` middleware, `/api/access/*`, `/access` admin page) is in place. Still to do — incrementally:
- Replace `requireRole('admin', 'owner')` with `requirePermission('<module>', 'manage')` on every existing route, module by module
- Update `Team.jsx` to assign `custom_role_id` (currently still writes the legacy `users.role` enum)
- Add per-page route guards in `main.jsx` so e.g. `/cash-recon` redirects to dashboard if `permissions.cash_recon === 'none'` (currently the page renders and just gets an empty list)
- Surface a tooltip / disabled-button affordance on `view`-only access (currently hidden vs full-access; no middle-state UX yet)

**4. Customer hard delete**
Customers page currently supports anonymise (GDPR erasure by overwrite) but not hard delete.
A double-confirmation hard delete (for internal test/demo data cleanup) is still outstanding.
Must cascade-delete linked bookings or reassign them. Requires `requireRole('owner')` guard.

**5. Website builder — deployment pieces** *(feature shipped code-side, not yet deployed)*
- Nginx: wildcard server block for `*.macaroonie.com` (proxy_pass → Fastify API).
- DNS: wildcard A record `* → <Lightsail-IP>` on Lightsail DNS. Wildcard does NOT
  shadow more-specific records (regular `api`, `app`, etc. take precedence as expected).
- SSL: wildcard cert via Certbot DNS-01. **DNS lives on AWS Lightsail** (NOT Route53,
  NOT Cloudflare), so there's no off-the-shelf Certbot plugin. Use AWS CLI hook
  scripts with `certbot --manual --preferred-challenges dns-01` — see the runbook in
  the "Lightsail wildcard cert setup" gotcha below for the full path including IAM
  policy, hook scripts, and the renewal cron entry.
- Custom domains: per-tenant SSL provisioning (Caddy on-demand TLS or Certbot per
  domain). The app only resolves the Host header; cert provisioning is out of process.
- Migrations 025 + 026 + 027 + 035 + 036 + 037 + 038 + 039 + 040 + 041 + 042 are applied automatically by the migrate runner
  (or auto-baselined on first run via `AUTO_BASELINE_UP_TO=024`).
- Optional: set `STORAGE_DRIVER=s3` + `S3_*` env vars to use S3 / DO Spaces / R2
  instead of local disk for uploads.

**6. Email system loose ends**
- `nodemailer` is not in `api/package.json` — the SMTP email provider will crash on first use.
  Add `"nodemailer": "^6.9.0"` if any tenant selects SMTP.
- Guest manage page `POST /modify` directly UPDATEs `starts_at` without checking
  `get_available_slots()` — a guest could move into a slot that's already full or outside
  any sitting. Add slot validation before applying.
- Stripe webhook confirmation path (`payments.js handlePaymentSucceeded`) does not
  schedule reminder emails; only the free-booking path does. Wire `scheduleReminder()`
  into the webhook path too.
- After deploy, clear stale BullMQ jobs in Redis — the `notificationQueue` job name
  changed to `'booking_email'` (with required `venueId` field), so old-format jobs
  enqueued before the change will fail forever.
- Legacy `src/services/notificationSvc.js` is dead code — safe to delete.

**7. Docs.jsx update for platform/team**
Help.jsx has a Team Management section; Docs.jsx does not yet have a corresponding
technical section for the platform-admin routes, RBAC matrix, or `platform_admins` table.

**8. Test suite**
No tests exist yet. Recommended approach:
- API: Vitest + supertest for route integration tests
- DB: Use a test database with migrations applied, reset between test runs
- Priority test targets: `confirm_hold()` race condition, slot resolver output, booking flow end-to-end, admin-override booking creation, schedule exception resolution

**9. More `/mobile` modules**
The `/mobile` shell + `MOBILE_MODULES` registry (see Key files) now has five modules:
H&S Dashboard, Expenses, Order Sheets, Cash Up, and Wages. Remaining candidates, in the
order they'd likely get used on a phone away from the host-stand tablet: Food safety
quick-log (temp/hold/cooking entry only, no equipment management), a booking lookup/manage
view, the H&S Action Log standalone.
When adding one: reuse the desktop page's data hooks/mutations/body components (not its
layout), register it in `src/mobile/registry.js`, and check whether its body components
already reflow at phone width before assuming they do (see the Common-mistakes entry on
canvas/SSR parity for the general shape of this risk — a shared body component drifting
between two renderers).
**10. `/mobile` device testing**
Built and verified via production build (`npm run build`) only — this environment has no
way to run a real browser against a live API/DB to click through the installed-PWA flow
end-to-end (Add to Home Screen on an actual iPhone/Android device, confirm the swapped
icon/name/manifest actually take effect, confirm `100dvh`/safe-area handling on a notched
device). Worth a real-device pass before treating this as fully verified.

**11. `/mobile` PWA install identity on Android — confirmed broken on real device, needs a subdomain**
Real-device testing (item 10) surfaced that Android Chrome does not treat `/mobile` as a
separately-installable PWA the way iOS Safari does. Chrome ties "already installed?" to
the *origin*, not to which manifest is currently linked — since a WebAPK is already
installed for `macaroonie.com` (the main app, `scope: "/"`), Chrome's install flow offers
to open that existing app instead of creating a distinct "Macaroonie Ops" icon, regardless
of `MobileShell`'s manifest/link-tag swap. The only reliable fix is a genuinely different
origin for `/mobile` (e.g. `ops.macaroonie.com`), since PWA install identity on Android is
origin-scoped. **Pick this up together with the office/ops subdomain work discussed earlier**
— fold the `/mobile` PWA identity fix into that same subdomain rollout rather than doing a
one-off DNS/Nginx change just for this. Until then, "Create shortcut" (Chrome's fallback
option) is the only way to get a distinct icon/name on Android, and it's a browser
shortcut, not a standalone WebAPK.

**12. Desktop Wages — submitted-report inputs still not disabled, and no "Paid" checkbox**
Mobile Wages (`MobileWages.jsx`) now disables "To be paid"/add/remove while a report is
submitted (with an explanatory note) and marks staff "Paid" via a checkbox tied to a
dedicated `PATCH .../entries/:entryId/paid` endpoint that works regardless of submitted
status. Desktop `WagesView` (`CashRecon.jsx`) still has neither: its amount/notes inputs
and add/remove controls are not gated on `isSubmitted` at all, so editing them on an
already-submitted week still silently 422s into a bare "Save failed" (see the Common-mistakes
entry on this); and "Cash paid" is still a raw amount field, not a checkbox. Bring desktop
in line with the mobile page — disable inputs when submitted, and consider whether desktop
should adopt the same paid-checkbox model or keep partial-cash-amount entry (it may
legitimately want the extra precision a checkbox doesn't offer, since desktop is used at a
desk with time to enter an exact split between cash and bank transfer).

**13. Audit remaining pages for the sticky-header-hides-hamburger bug**
The `z-40` bump on `AppShell`'s hamburger button (see Common-mistakes) makes it clickable
on every page regardless of that page's own header z-index, so nothing is currently broken.
But pages whose own `position: sticky` top-of-page header sits at `z-10` or higher still
visually paint UNDER the (now-topmost) hamburger without the `pl-14 lg:pl-4` spacer, which
looks scruffy rather than actually blocking navigation. `CashRecon.jsx`'s 5 views got the
`pl-14` treatment as part of this fix; still to check (grep `sticky top-0 z-1\|sticky top-0
z-2` across `admin/src/pages`): `Menus.jsx`, `OrderSheetTemplates.jsx`, `Website.jsx`,
`Timeline.jsx` — confirm each one's header is actually the page's own top-of-viewport
element (not a nested table `<thead>` or in-panel section header, which aren't at risk)
before adding `pl-14`.

---

## Environment variables

Both `.env.example` files document all required vars.
The API will refuse to start if any required var is missing (Zod validation in `src/config/env.js`).

Key vars to set before running:
- `DATABASE_URL` — pre-filled by setup.sh
- `REDIS_URL` — update password to match Redis config
- `AUTH0_DOMAIN` / `AUTH0_AUDIENCE` — from Auth0 dashboard
- `STRIPE_SECRET_KEY` / `STRIPE_WEBHOOK_SECRET` — from Stripe dashboard
- Admin portal: `VITE_AUTH0_DOMAIN` / `VITE_AUTH0_CLIENT_ID` / `VITE_AUTH0_AUDIENCE`

---

## Common mistakes to avoid

> **This section is append-only.** Never delete an entry — only correct one if it turns out to be wrong. This is the most-read section of the doc; every "subtle why didn't that work" bug gets a new entry.

- **Never query tenant tables without `withTenant()`** — RLS silently returns empty rows, not an error.
- **Never trust client for payment confirmation** — booking only becomes permanent via Stripe webhook, not client-side resolve.
- **Never store slots** — they are always computed. Do not add a `slots` table.
- **`slot_duration_mins` ≠ `slot_interval_mins`** — duration is how long a booking lasts, interval is how often a new slot starts. Both live on different tables.
- **`max_covers = 0` on a slot cap ≠ fully booked** — it means intentionally blocked. The `zero_cap_display` venue setting controls whether it shows as hidden or unavailable.
- **Hold TTL is configurable per venue** — `booking_rules.hold_ttl_secs`. Default 300 (5 min). Widget countdown is driven by `expires_at` from the hold response, not a local timer.
- **pg_cron is a fallback** — the primary hold release is the explicit `DELETE /holds/:id` call. Don't rely on the sweep as the main path.
- **`slot_time` from DB is `HH:MM:SS`** — PostgreSQL `TIME` columns serialise to `HH:MM:SS` in JSON, but the API validation regex expects `HH:MM`. Always call `.slice(0, 5)` on slot_time values before using them as keys or sending them back to the API. **CAVEAT:** this only applies to columns of type `time` (e.g. `sitting_slot_caps.slot_time`, `venue_sittings.opens_at`). The composite type returned by `get_available_slots()` had its `slot_time` field changed from `time` to `timestamptz` in migration 020, and postgres.js deserialises that to a JS `Date` object — `.slice()` on it throws `s.slot_time.slice is not a function`. For slot_result rows, convert to the venue-local `HH:MM` via `Intl.DateTimeFormat('en-GB', { timeZone: venue.timezone, hour: '2-digit', minute: '2-digit', hour12: false }).format(date)` (see `widgetApi.js`). The admin `/api/slots` route passes the Date through and the consumers handle it — the widget API materialises HH:MM strings because the public widget JS interpolates them directly into a date string.
- **`combination_id` must be copied from hold to booking** — `POST /bookings` (free-booking path) copies it; `confirm_hold()` PG function does not yet — see Outstanding items. Never assume the booking record has `combination_id` set just because the hold had it (on the Stripe webhook path).
- **`guest_notes` was silently dropped on paid bookings** — now fixed. The Stripe webhook INSERT previously omitted both `combination_id` and `guest_notes`. Both are now copied from the hold record.
- **`/relocate` throws 422 when no combo exists for expanded table set** — step 4 no longer auto-creates combinations. If adjacency expansion finds T8+T9+T10 but no pre-configured combination exists for those tables, the endpoint returns 422 and the drag snaps back. Operators must create the combination in the Tables page first.
- **`v_exc_template` in `get_available_slots()`** — when no schedule exception covers the date, the record variable is never assigned by SELECT INTO. Accessing `.id` on an uninitialized PL/pgSQL RECORD throws PG 55000 'not assigned yet'. Always use a scalar UUID variable (`v_exc_template_id uuid := NULL`) and assign it only when the record IS found.
- **Admin override `starts_at` is server-local time** — `POST /bookings/admin-override` receives `YYYY-MM-DDTHH:MM:SS` without a timezone offset and passes it to `new Date()`, which interprets it as server-local time. If the server and venue are in different timezones, the stored `starts_at` will be offset. Future work: pass the venue's IANA timezone and convert in the server.
- **Timeline grey strips are per-column, not per-row** — grey = outside any sitting OR slot cap explicitly set to 0 (reason='unavailable'). Fully-booked slots (reason='full') are NOT greyed — they stay white. Secondary combo rows clip grey strips to the booking's own time window only; grey IS shown outside the booking's time.
- **`table_combination_members` has no `tenant_id`** — RLS on that table is enforced via a subquery on `table_combinations`. Never pass `tenant_id` to INSERT INTO `table_combination_members`.
- **`notificationQueue.add()` must never be awaited in request path** — Redis may be unavailable. Always use fire-and-forget: `.catch(e => req.log.warn(...))`. The booking is created and the response sent; the email is best-effort.
- **Customer anonymise never deletes** — GDPR right to erasure is implemented by overwriting PII fields with placeholder values and setting `is_anonymised=true`. The row is kept for audit/reporting. Never DELETE a customer row.
- **`api.download()` in the frontend** — use `api.download(path, filename)` to fetch auth-protected binary responses (JSON exports, etc.). It handles the Auth0 token injection and creates a temporary object URL for the browser Save dialog.
- **deploy.sh requires root but builds as ubuntu** — the script requires `sudo` to talk to PM2, but all `npm install` / `npm run build` steps must run as the `ubuntu` user to keep `dist/` and `node_modules/` owned by ubuntu. If `dist/` becomes root-owned, run `sudo chown -R ubuntu:ubuntu /home/ubuntu/app/admin/dist` then redeploy.
- **`slot_result` type requires DROP + CREATE** — PostgreSQL composite types used in functions cannot be altered. Any migration that adds fields to `slot_result` must `DROP FUNCTION get_available_slots` first, then `DROP TYPE slot_result`, then recreate both. See migration 020.
- **slot warning in booking modal uses local time** — `new Date(slot_time).getHours()` interprets the UTC timestamp in the browser's local timezone. If the server timezone and browser timezone differ, warning thresholds may be slightly off. Fix: use `slotsRes.timezone` to convert properly (outstanding).
- **`checked_out` is NOT the same as `completed`** — `completed` was renamed to `seated` in migration 017. `checked_out` is a new separate status (after seated). Do not confuse the two in queries or UI labels.
- **Secondary combo row canvas at z=4** — secondary combo rows (where the spanning card from the primary row above paints over them) must have `z-index: 4` AND `pointer-events: none` on their canvas div. z=4 ensures their clipped gradient background paints above the primary's z=3 stacking context. `pointer-events: none` lets click and drag events pass through to the primary row's spanning card. Without z=4, the gradient is invisible (the primary's z=3 stacking context covers T3B entirely).
- **`handleCustSearch` in BookingDrawer takes a single field value** — pass only the value of the field that just changed, not all three fields. The previous multi-field priority logic meant editing name while email was already filled always searched by email, so name-only changes never triggered search.
- **`ColourPickerRow` has local `hexInput` state** — the component uses `useState(value)` for the colour wheel and hex text input, which only initialises once. If the context value changes externally (e.g. an external swatch button calls `setTimelineBg(hex)`), the picker will show a stale colour unless synced via `useEffect(() => setHexInput(value), [value])`. Always include this effect when using `ColourPickerRow` alongside external swatch buttons that update the same context value.
- **`hourWidth` must be threaded everywhere in Timeline** — `HOUR_WIDTH = 80` is the module-level constant used as the default param in utility functions. The runtime `hourWidth` (80 or 120 from `wideColumns`) must be passed as a prop to `TableRow`, `BookingCard`, and `TimelineHeader`, and used in all pixel calculations (drag/resize deltas, `nowX`, `unavailableStrips`, gradient offsets, `handleCanvasClick`). Using the module constant directly in any of these bypasses the wide-columns setting.
- **`wideColumns` dep in useMemos** — `unavailableStrips` useMemo depends on `[sittingsForDate, slotsOverlay, hourWidth, totalWidth]`. If `hourWidth` is omitted from the dep array, the grey strips will not reposition when the wide-columns toggle changes.
- **`hourWidth`/`totalWidth`/`rowHeight` must be declared before `nowX` useMemo** — these are `const` declarations inside the component function body. `nowX` references `hourWidth` and `totalWidth` inside its callback. Because `useMemo` runs synchronously on first render, accessing a `const` that appears later in the function body throws a Temporal Dead Zone (TDZ) `ReferenceError`. Always keep `hourWidth`/`totalWidth`/`rowHeight` above any useMemo that references them.
- **`headerBgStrips` removes `bg-background` from `TimelineHeader` outer div** — when `headerBgStrips` is true, `backgroundStyle` is applied as inline style to the header's outer div, which overrides the Tailwind `bg-background` class (inline styles win). The sticky label cell inside still carries its own `bg-background` to mask scrolling content behind it. If you add any new wrapper classes to `TimelineHeader` that set a background, they will be silently overridden when `headerBgStrips` is on.
- **`showStartLine` colour picker only mounts when line is enabled** — in Settings, the `ColourPickerRow` for `startLineColour` is conditionally rendered inside `{showStartLine && ...}`. This means the picker's local state is reset each time the toggle is switched on. This is intentional (avoids a stale picker) but means the hex input always initialises from the context value when re-shown.
- **`sessionStartXs` replaces `firstOpenX` in Timeline** — the opening-hour line is now drawn once per sitting (not just the first sitting of the day). The useMemo returns an array; render site maps over it. Never revert to a single scalar — venues with lunch + dinner sessions need both lines.
- **Drawer table override routes single vs multi** — single table → `PATCH /relocate` (cascade displacement); multi-table → `PATCH /tables` (direct assignment, no displacement); unallocated table → `PATCH /tables` (never /relocate — the relocate endpoint rejects the unallocated table ID). Check `handleTableSave` branching logic before modifying.
- **`tables` query in BookingDrawer is non-lazy** — it was previously `enabled: editMode === 'table'`. It now fetches immediately on drawer open so `unallocatedTable` and `hasCapacityIssue` are available in view mode. Do not re-add the `enabled` gate.
- **Sitting `name` field is nullable text** — `SittingBody` validates it as `z.string().max(100).nullable().optional()`. In the frontend, always fall back to the time-range string: `sitting.name ?? \`${sitting.opens_at.slice(0,5)}–${sitting.closes_at.slice(0,5)}\``.
- **`sidebarExpandedDefault` only controls initial state** — it initialises the `open` state in AppShell's `useState`. Subsequent manual toggles work normally per-session. Mobile always starts collapsed regardless of the setting.
- **`sittingStats` useMemo uses local browser time for HH:MM comparison** — booking start times are UTC ISO strings; `new Date(b.starts_at).getHours()` returns local time. If the server timezone differs from the browser, bookings near sitting boundaries may be assigned to the wrong sitting in the stats display (cosmetic only — not used for any business logic).
- **Migration 022 uses `CREATE OR REPLACE FUNCTION`** — unlike migration 020 which had to `DROP TYPE slot_result` before recreation, migration 022 does not change the return type so `CREATE OR REPLACE` is safe. If you ever need to change `slot_result` again, revert to the DROP + CREATE pattern.
- **Overlap detection is frontend-only** — `overlappingIds` is computed from the in-memory `bookingsRes` array. It does not call the API. If a booking is created by another user and the timeline hasn't refreshed, an overlap may not be detected until the next WebSocket push or 60-second refetch.
- **`startHour` must be threaded the same way as `hourWidth`** — `timeToX(iso, hw, sh)` and `sittingTimeToX(t, hw, sh)` both accept `sh` (startHour) as a third optional param defaulting to `START_HOUR`. In the Timeline component, `startHour` is derived from `timelineStart` (context). Pass `startHour` as a prop to `TableRow`, `BookingCard`, and `TimelineHeader` — exactly the same threading pattern as `hourWidth`. All useMemos calling these functions must include `startHour` in their dep arrays. Using the module constant `START_HOUR` directly anywhere in the component body bypasses the timeline hour range setting.
- **`table_locked` is checked in `/relocate` only** — the lock prevents cascade displacement when an operator drags a booking onto tables occupied by a locked booking. It does NOT prevent the operator from directly reassigning via `PATCH /tables` (multi-table path) or `POST /bookings/admin-override`. The lock is a hint to the relocation engine, not a hard DB constraint.
- **Website CMS: subdomain slug is a GLOBAL namespace** — `website_config.subdomain_slug UNIQUE` across all tenants. Separate from `tenants.slug` (Auth0 lookup). `GET /api/website/slug-available?slug=...` must be used before POSTing a new config; the endpoint is authenticated but does NOT use `withTenant()` because it needs to see other tenants' rows.
- **Website CMS: custom_domain is the SSR lookup key, not the SSL provisioning key** — the app resolves the Host header against `website_config.custom_domain`. It does NOT issue certificates. SSL is terminated at Nginx (or Caddy on-demand TLS); the tenant-facing flow is: (a) tenant sets `custom_domain`, (b) tenant points DNS at our IP, (c) tenant clicks "Verify DNS" → `POST /api/website/verify-domain` (checks A + CNAME records), (d) an out-of-band process provisions a cert. Setting the domain clears `custom_domain_verified` so the site stays offline until verified.
- **Website CMS: `theme` JSONB is column-overwrite, not deep-merge** — PATCHing `theme: {...}` replaces the whole column. The admin page's `ThemeSection` holds the FULL merged theme in local state and PATCHes the entire object. Don't try to PATCH a partial theme object — the missing keys become null, not defaults.
- **Website CMS: pluggable storage via `STORAGE_DRIVER`** — `local` writes to `UPLOAD_DIR` and serves through `@fastify/static` at `/uploads/*`. `s3` lazy-imports `@aws-sdk/client-s3` (listed as `optionalDependencies` so local installs don't pay the download cost). Switching drivers mid-flight will ORPHAN existing upload URLs — run a migration script to copy files between backends before switching.
- **Website CMS: templates share CSS variables via `views/site/shared/head.eta`** — the shared head computes every `--c-*`, `--f-*`, `--r-*` CSS variable from `config.theme` (merged over defaults). When adding a new template, consume those variables; don't add a per-template CSS variable schema. When adding a new theme knob, update `ThemeSchema` in `routes/website.js`, the default constant in `admin/src/pages/Website.jsx`, and the CSS-variable emit in `shared/head.eta` together.
- **Website CMS: reserved subdomains** — `api, www, admin, app, mail, static, assets, cdn, ws, stripe, webhook, webhooks` never match as tenant sites, regardless of DB content. Extend `RESERVED_SUBDOMAINS` in `siteRenderer.js` before using any new infrastructure subdomain.
- **Website CMS: single-venue tenants skip the location picker** — `loadTenantBundle` hydrates the sole venue's hours/address/PDF menus/allergens onto the home bundle, `/locations` 302s to `/locations/{slug}`, `/menu` 302s to `/locations/{slug}/menu`, and the header drops a `/locations` nav item. Do NOT special-case this with a `site_mode` flag — venue count is the source of truth. Adding a second venue automatically restores the multi-location hub. Hours are loaded independently of `show_find_us` (they used to blank together).
- **Website hours default to the venue schedule** — `website_config.opening_hours_source` defaults to `venue` (sittings on `venue_schedule_templates`). `manual` is only for operators who want advertised hours that differ from booking hours. Empty `website_opening_hours` is not a source of truth. GET `/website/opening-hours` uses the same `loadOpeningHours` helper as SSR so the canvas preview matches the live site. Single-venue tenants get an **Opening hours** item on the Tenant site nav (and an Edit hours button on the block inspector); switching to Manual seeds the form from the current schedule.
- **Menu variant groups are tenant-wide libraries** — `menu_variant_groups` + `menu_variant_options` hold reusable option sets (Protein, Size). Items attach groups via `menu_item_variant_groups`; optional `menu_item_variant_prices` override a single option's price on that dish. Ad-hoc `menu_item_variants` remain for one-off options. Do not copy group options onto items — keep the live link. Item photos live on `menu_items.image_url`.
- **Inbox sender logos are BIMI, not an email header** — `tenant_site.bimi_svg` is served at `GET /bimi.svg` (and `/.well-known/bimi.svg`) on the tenant site. The operator adds `default._bimi.<from-domain> TXT "v=BIMI1; l=https://<host>/bimi.svg;"`. Gmail/Apple also need a VMC; Yahoo/Fastmail do not. Platform logo is `admin/public/bimi.svg` for `noreply@macaroonie.com`.
- **Every website block has an Anchor ID** — stored as `data.anchor_id`, edited in the shared BlockInspector (not per-type editors). SSR wraps the block in `<div id="…">`. Fallbacks: reservations_widget → `reservations`, opening_hours → `hours`, find_us → `find-us`. Do not hardcode those ids on the partials.
- **Extra pages are `website_pages.kind` `page` | `modal`** — standalone at `/p/{slug}`, modals opened via `#modal/{slug}` (optional `:anchor`). Built with the same PageBuilder (`blocks` jsonb). LinkPicker in the inspector lists this-page anchors, other pages ± anchors, and modals. Do not store structured link objects — href strings only. Modal markup is injected from `views/site/shared/modals.eta`.
- **Website CMS: `loadSiteBundle()` reloads on every request** — no caching. Cheap (~1 round trip for the config + a single parallel query for everything else) but not free. If traffic becomes a concern, add a short TTL cache keyed on `(tenant_id, updated_at)` inside the service, invalidated on any admin mutation. Don't cache by slug alone — would leak between tenants if slugs are ever reused.
- **Website CMS: `GET /api/site/:slug` short-circuits on `is_published = false`** — returns 404 for the public JSON AND the SSR renderer. Admin preview must go through the admin portal's authenticated path (or pass `includeUnpublished: true` in a future preview endpoint). Never expose the `includeUnpublished` flag on the public route.
- **Website CMS: `custom_domain_verified` is cleared on every mutation of `custom_domain`** — the PATCH handler explicitly sets `custom_domain_verified = false` whenever the body contains `custom_domain`. Refetch the config on the client after PATCHing so the UI shows the unverified state and the "Verify DNS" button.
- **Website CMS: Eta includes are path-relative to the `views` root** — the templates use `include('../../shared/head', it)` because they live 3 levels deep (`views/site/templates/{key}/index.eta`). Don't change the directory layout without updating every include path. Tests catch this only by rendering each template.
- **`timelineStart`/`timelineEnd` are integers, not strings** — stored in localStorage as numbers. When reading from `tlSettings`, do arithmetic directly: `const totalHours = endHour - startHour`. The `<select>` dropdowns in Settings use `Number(e.target.value)` on `onChange` to ensure the stored value is always a number, not a string.
- **React hook stability** — always return stable references from custom hooks. Any object/function used in a `useEffect` deps array must be wrapped in `useMemo` / `useCallback` or you get an infinite render loop. This applies to every `useContext`-returned value, every query result object, and every callback passed as a prop.
- **Migration idempotency** — wrap migrations in `BEGIN`/`COMMIT` (our runner does this automatically via `sql.begin()`). All seeds should use `ON CONFLICT DO NOTHING` / `WHERE NOT EXISTS`. Re-running `scripts/migrate.js` must be safe.
- **Trust but verify (session summaries)** — never reference session-summary items as "still open" without checking the actual code. Session summaries go stale between runs. Always grep / check live code before claiming something is pending.
- **File ownership on server** — if git throws "dubious ownership" on the Lightsail server, fix once with `git config --global --add safe.directory /home/ubuntu/app` rather than chowning files. The deploy workflow uses `git reset --hard` which needs this.
- **`manage_token` is the ONLY auth for the guest manage page** — the UUID in the URL IS the authentication. Do not add rate-limiting or IP-blocking that would break email link clicks. Do not expose manage_token in any admin API list response (it would let any admin-portal user access any guest's manage page).
- **`notificationQueue.add()` job name changed to `'booking_email'`** — the old `'confirmation'` / `'cancellation'` job names are no longer used. The worker now calls `processEmailJob` from `emailWorker.js` instead of the legacy `notificationSvc.sendNotification()`.
- **Reminder jobs use `jobId: 'reminder-{bookingId}'`** — BullMQ deduplicates by jobId, so re-confirming a booking doesn't create duplicate reminders. If the booking is cancelled, the reminder job stays in the queue but `processEmailJob` skips it (checks `reminder_sent_at` and terminal status).
- **Email template `theme` JSONB is column-overwrite** — same as website_config.theme. The admin page's ThemeSection holds the FULL merged theme and PATCHes the entire object. Never PATCH a partial theme.
- **SC `included_in_income` XOR variance** — the two SC flags (`included_in_income` + `included_in_takings`) are independent booleans. The variance formula is `totalIncome + scAdjustment - totalTakings` where scAdjustment is the signed XOR sum. When both flags are on, the adjustment is 0 (not doubled). When only income is on, the SC is subtracted from the expected-till figure. When only takings is on, it's added. **Superseded by migration 096** (kept for history, per the append-only rule) — these two booleans were replaced by `takings_effect`/`income_effect` (`'none' | 'add' | 'subtract'` each), since a fixed always-add-on-takings/always-subtract-on-income direction couldn't express every scenario an operator needed (e.g. deducting from takings). See the newer entry below and Docs.jsx's Cash Reconciliation section.
- **Migration 027 number collision** — both `027_cash_reconciliation.sql` and `027_per_venue_websites.sql` exist. They have different version strings in `schema_migrations` so the runner applies both. Confusing but not broken. Do not renumber — would cause re-application on servers that already ran them.
- **`platform_admins` table has NO RLS** — it's a global table queried during auth before any tenant context is set. Never add RLS to it. Access is gated in the API middleware (`requirePlatformAdmin`), not at the DB level.
- **`requireRole()` lets platform admins through automatically** — platform admins bypass all role checks. If you need to restrict something to ONLY tenant-level owners (not platform admins), you need a custom guard that explicitly checks `req.user.role === 'owner' && !req.isPlatformAdmin`.
- **`VITE_AUTH0_ORG_ID` is now optional** — when omitted, Auth0 prompts the user to select an org on login (for multi-tenant users). When set, it forces that org (existing single-tenant behaviour). Removing this env var from an existing deployment changes login UX.
- **Org switcher triggers a full re-authentication** — `loginWithRedirect({ organization: auth0_org_id })` navigates away from the SPA. All local state is lost. This is by design — Auth0 issues a new JWT with the new org's claims. TanStack Query cache is rebuilt on return. **Superseded** — this described the original org-scoped-login design. Everyday tenant switching no longer does this at all: it's a client-side `X-Tenant-Id` swap (`setSelectedTenant()` + page reload), no second Auth0 login, see the "Tenant selection" Completed item. This mechanism is now used only for Auth0 invitation-link acceptance.
- **Self-protection in team management** — `PATCH /team/:userId` and `DELETE /team/:userId` reject operations where the target `auth0_user_id` matches `req.user.sub`. Cannot demote yourself, cannot deactivate yourself. Enforced API-side (not just UI).
- **Auth0 Mgmt API is OPTIONAL** — when `AUTH0_MGMT_CLIENT_ID`/`AUTH0_MGMT_CLIENT_SECRET`/`AUTH0_INVITE_CLIENT_ID` are unset, `auth0MgmtSvc.isConfigured()` returns false and `/team/invite` falls back to creating a local row only (logged warning, Team UI shows an amber banner). This is intentional for local dev. In production all three MUST be set.
- **When the user pastes a string and errors follow, RECHECK THE STRING first** — don't dive into schema, validation, or framework debugging until the pasted value has been verified end-to-end. Bit us hard with Postmark (2026-05-02): an invisible character from the original clipboard paste produced a 401 "invalid token" response, and I spent multiple turns chasing schema bugs, CHECK constraints, and POST upsert behaviour — while the actual fix was the user re-copying the token to the clipboard. **The fastest sanity check** is to call the external API directly (e.g. `curl -H 'X-Postmark-Server-Token: <pasted-value>' ...`) with the same string and see if the third party rejects it. If THEY reject the raw value, it's the string. If they accept it, it's the app. Don't reflexively add `.trim()` calls to mask possible whitespace — that hides the real issue and tomorrow's bug looks identical.
- **`systemctl reload nginx` doesn't always pick up new `location` modifiers** — adding `^~`, `~*`, or other matching modifiers to existing locations sometimes leaves the running workers using the old matching table even after a clean reload (config tested OK, no error). Symptom: the new behaviour is in `nginx -T` output but requests still hit the old block. `systemctl restart nginx` (full process restart, ~50ms downtime) clears it. For routing-modifier changes, prefer restart over reload.
- **Eta `<% ... %>` JS blocks containing `//` line comments break the parser** — production Eta sometimes fails to detect the closing `%>` when the JS body has line comments (especially comments that contain backticks or em-dashes). Symptom: 500 with "Bad template syntax: Unexpected token '>'", and the compiled JS has literal `<%` / `%>` tokens appearing as raw source where Eta failed to compile them. Bit us on `views/site/shared/head.eta` (May 2026) where I'd added a multi-line `// comment` block above some `const` declarations. **Workaround**: use `/* ... */` block comments inside `<% %>` JS blocks (the rest of the file already does this — match the convention). Single-line `//` comments are usually fine if they don't sit between `<%` and the next `%>` boundary, but block comments are universally safe.
- **Eta `<%= %>` interpolation breaks on certain `.filter(...).join(...)` chains** — production Eta sometimes fails to emit the closing `)` of the wrapping `__eta.e(...)` when the inline expression is a chained array method ending in `.join(' ')` or `.join(' · ')`. Symptom: 500 with "Bad template syntax: Unexpected token '>'", and the compiled JS shown in the error has the source line `<div><%= [a, b].filter(Boolean).join(' ') %></div>` followed directly by `__eta.res+='</div>...'` without the matching `)`. The expression itself is valid JS — Eta just trips on the closing-tag detection in this exact shape. Bit us on `templates/classic/partials/footer.eta` (May 2026). **Workaround**: hoist the join into a `<% const x = [a,b].filter(Boolean).join(' ') %>` statement above the markup, then interpolate the variable: `<%= x %>`. Apply this to every venue card and address line — there are ~15 places across `templates/{classic,modern,onethai}/{fallback_home,fallback_location,locations,menu_hub,partials/footer}.eta`. Do NOT inline-chain `.filter(Boolean).join(...)` inside `<%= %>`.
- **Eta JS blocks must NOT start with `(` after a text/HTML line** — JavaScript's ASI (automatic semicolon insertion) does NOT insert a semicolon before a line starting with `(`, so the previous line's value gets called as a function. Symptom: `"<main>\n\n" is not a function` 500 from Eta. Bit us once when an `index.eta` had `<main>\n\n<% (it.config.home_blocks || []).forEach(...) %>` — the literal `<main>\n\n` ends up being called. Fix: assign the expression to a `const` first, OR use a `for…of` loop, OR put a leading semicolon. Same trap applies to any `<% (...)... %>` after HTML text.
- **Block-based page composition lives in `home_blocks` JSONB** — `website_config.home_blocks` is the only source of truth for the home-page layout. Both SSR templates (classic + modern) iterate `home_blocks` via `views/site/blocks/renderer.eta`. An empty array renders an empty page — there is no legacy flat-layout fallback (removed 2026-05-03 since we're pre-prod). Legacy admin nav entries (Hero, About) were also removed; their content lives inside the page-builder blocks. Legacy columns (`hero_*`, `about_text`, `about_html`) remain in the schema for now but are not read by any template — drop in a future migration when convenient.
- **Block dispatch is a single `<%~ include('./renderer', { ...it, block }) %>`** — the renderer.eta switches on `it.block.type` and includes the right per-block partial. Adding a new block type means: (1) entry in `blockRegistry.js` with editor component + default data, (2) new partial under `views/site/blocks/<type>.eta`, (3) line in `renderer.eta` for the dispatch. The frontend block list is the contract — server treats `data` as opaque JSONB.
- **Data blocks (gallery, hours, contact, etc.) don't store content on the block** — they just store display options like `heading`. Content comes from the existing per-section data (website_config fields, website_gallery_images rows, etc.). This is intentional — a single content source feeds both the legacy flat layout and any block instance, so users editing one place see the change everywhere.
- **All website-CMS image uploads mirror into `media_items`** — `/website/upload` does the storage put AND inserts a media_items row when `kind === 'images'` so the asset is visible in the Media library. PDF kinds (`menus`, `docs`) DON'T mirror because they're tracked via `website_menu_documents`. The `media_item_id` is returned in the response for callers that want to link.
- **`about_html` is the new rich-text source of truth** — templates check `it.config.about_html` first (rendered raw via `<%~ %>`) and fall back to `it.config.about_text` (escaped, with `white-space: pre-wrap`). The admin section uses TipTap and writes to `about_html`. The legacy column stays so existing data keeps rendering. Both Eta `about` blocks (classic + modern) need updating in lockstep when the about layout changes.
- **Theme presets are partial themes — deep-merged onto the current theme** — so a preset that only specifies colors leaves the user's typography tweaks intact. To curate a new preset, add to `THEME_PRESETS` in `Website.jsx`. Don't make presets mutable — they're keyed by `key` and rendered as fixed buttons.
- **Gallery layout is template-side, not CSS-side** — the Eta template branches on `it.config.gallery_style`. The `gallery_size` value maps to two numbers (`thumbMin`, `thumbHt`) at the top of the gallery block — keep them in sync if you change the size enum.
- **Opening hours `source = 'venue'` derives from `venue_schedule_templates` + `venue_sittings`** — `siteDataSvc.loadSiteBundle()` runs a separate aggregate query (`MIN(opens_at), MAX(closes_at)` per `day_of_week`). Result rows are normalised to the same shape `website_opening_hours` produces (`HH:MM` slices, `is_closed` boolean), so the templates don't branch on source. When the user switches source from manual → venue, manual rows in `website_opening_hours` are NOT deleted — they're just ignored in render. Switch back to keep them.
- **Apex Nginx `location /uploads/` must use `^~` prefix-priority** — the apex `macaroonie.com` server block has a regex `location ~* \.(js|css|png|jpg|...)$` that serves static admin-portal assets from `/home/ubuntu/app/admin/dist`. In Nginx, regex locations take precedence over plain prefix locations, so a plain `location /uploads/` is shadowed by the extension regex for `.png`/`.jpg`/etc. and the request gets served (with 404) from the wrong root. Fix by writing `location ^~ /uploads/ { proxy_pass ...; }` — the `^~` modifier tells Nginx to stop after a prefix match without checking regex locations. Same trap applies to any new path serving binary uploads on the apex.
- **PATCH `/website/config` and POST `/website/verify-domain` MUST scope by venue_id** — both endpoints used to filter UPDATE/SELECT only by `tenant_id`, which silently overwrote every venue's website_config under the tenant. A hero image saved for "One Thai Cafe in Ware" propagated to every venue site of the same master franchisee. Fixed (2026-05-03) by including `id = ${existing.id}` (looked up via the existing `ensureConfig(tenant_id, venue_id)` helper) on UPDATE, and by requiring `venue_id` query/body param on `verify-domain`. When adding new endpoints that mutate `website_config`, ALWAYS scope by `venue_id`. The `ensureConfig` helper exists for exactly this — use it.
- **`POST /api/email-templates/settings/:venueId` upserts ALL body fields** — used to be a thin "create with email_provider only" handler that silently dropped `provider_api_key`, `from_name`, etc. on first save. The frontend would call POST when no row existed, get a 201 back, and think the credentials had been saved when they hadn't. Symptom: switching to Postmark, saving, then trying to send a test → "Request does not contain a valid Server token" because the token never made it to the DB. Fixed by routing POST through the same upsert helper as PATCH (insert-or-noop, then dynamic UPDATE of supplied fields). Always test first-save flow when adding a settings page, not just edits.
- **`provider_domain` field is overloaded across email providers** — the column stores different things depending on `email_provider`: Mailgun uses it for the sending domain (`mg.macaroonie.com`); Postmark uses it for the message stream name (`outbound`); SES re-uses `provider_region` for the AWS region. The UI gates input fields by provider so users never see the overloading. If you add a new provider that needs ANOTHER field type, prefer adding `provider_extra` JSONB or another typed column rather than further overloading. Don't rename `provider_domain` — too many migrations + worker + test-send paths reference it.
- **Auth0 Mgmt API requires a Login Action that reads `app_metadata.role`** — role sync (`PATCH /team/:userId` calling `updateUserAppMetadata`) writes the role to `app_metadata.role` on the Auth0 user. The Auth0 Login Action then injects it as a JWT claim (`https://{AUTH0_DOMAIN}/claims/role`). Without that Action in place, the JWT keeps the old role until the user re-invites or you manually set it in the Auth0 dashboard. Verify the Action exists in Auth0 → Actions → Library before relying on role sync.
- **`assign_membership_on_login: true` lets anyone into the org — auth middleware must enforce users row** — `provisionTenantOrg()` enables Auth0 connections with `assign_membership_on_login: true`, which means any Google/email account that logs in through the org's Auth0 connection is automatically added as an org member and receives the org's `tenant_id` claim. Without the `userNotFound` guard in `requireAuth`, those users pass auth with the JWT role default (`operator`) and can see the app. The fix (June 2026): after the reconcile try-catch, if `userNotFound === true` (reconcile ran cleanly but found no users row), return 403 "not invited". Do NOT set `userNotFound` on DB exceptions — that would block legitimate users during infra failures.
- **Auth0 invitation roll-back on failure** — `POST /team/invite` inserts the local row first, then calls Auth0. If Auth0 returns non-2xx the local row is DELETEd before responding 502. Re-invites work cleanly because no orphan row remains. Do not change the order — calling Auth0 first leaves no audit trail when the local insert fails on a unique conflict.
- **First-login user reconciliation** — `requireAuth` in `auth.js` reconciles Auth0 sub ↔ local user every request. Three cases: (1) already linked → bump `last_login_at`, use local `users.role` as authoritative; (2) `auth0_user_id` is NULL but email matches an invited row → link sub, bump login; (3) `is_active=false` locally → reject with 403 even if Auth0 still trusts the user. The local DB is the source of truth for `req.user.role`, not the JWT — so role changes take effect without waiting for the next Auth0 token refresh.
- **`AUTH0_INVITE_CLIENT_ID` is the SPA client_id, not the M2M one** — the value is the same as the admin portal's `VITE_AUTH0_CLIENT_ID`. It's where users land after accepting their invitation. Confusing because the password reset endpoint also uses it, but the variable name is shared deliberately so there's only one SPA-client value to configure.
- **No conditional `useQuery` between an early return and another hook** — React error #310 (`Rendered more hooks than during the previous render`) fires when a component returns early before some hooks, then on a later render reaches those hooks. Pattern that broke `/platform`: `const me = useQuery(...)` → `if (!me?.is_platform_admin) return <Denied/>` → `const tenants = useQuery(...)`. First render `me` is undefined, only 1 hook runs; once `me` loads with `is_platform_admin: true`, 2 hooks run → React aborts. Fix: declare ALL hooks unconditionally at the top, gate the secondary query with `enabled: condition`, and move the early `return` AFTER every hook call.
- **Auth0 invitation links require `invitation` + `organization` to be forwarded to `/authorize`** — when an invited user clicks the email link they land on `https://<app>/?invitation=X&organization=org_Y`. `RequireAuth` in `main.jsx` MUST detect those query params and pass them as `authorizationParams` on `loginWithRedirect`. Without this, Auth0 runs a normal login → user authenticates with their existing Google/email account → Auth0 rejects with "user X is not part of org_Y" because the invitation acceptance step never ran. The check must run BEFORE the `isAuthenticated` gate too — an already-logged-in user clicking an invitation for a different org still needs the acceptance flow.
- **Auth0 invitation requires `Application Login URI` set on the SPA app** — Auth0 returns 400 `A default login route is required to generate the invitation url` from `POST /api/v2/organizations/{orgId}/invitations` when the SPA application has no Application Login URI. Set it under Auth0 → Applications → Macaroonie Admin → Settings → Application URIs → Application Login URI to the SPA root (e.g. `https://macaroonie.com`). Without this, every invite call returns 400 and the local row is rolled back.
- **Custom error status to dodge Nginx error pages** — Nginx by default replaces upstream JSON 502/504 bodies with its own HTML "Bad Gateway" / "Gateway Timeout" pages, so the admin UI sees no `body.error` and shows the literal HTTP status text. For "I caught the upstream failure cleanly" cases (e.g. Auth0 invitation rejected), throw `httpError(422, ...)` instead of 502 — Nginx leaves 4xx bodies alone, so the actual error message reaches the operator.
- **Auth0 invitation setup is 8 steps and Auth0's own docs are stale** — the actual current dashboard requires: (1) M2M app authorised against Management API with the 6 scopes; (2) `AUTH0_INVITE_CLIENT_ID` = SPA client_id; (3) Application Login URI set on the SPA app (Settings → Application URIs); (4) Allowed Callback/Logout/Web Origins all include the SPA root; (5) Connections enabled at the **Application** level (Application → Connections tab); (6) Connections added at the **Organization** level (Organization → Connections tab); (7) **per-connection Auto-Membership = Enable** inside Org → Connection → Authentication section (THIS IS THE STEP THAT'S LEAST OBVIOUS — without it Google login loops back to the picker with no error); (8) Login Action that propagates `app_metadata.role` to a JWT claim. Help.jsx + Docs.jsx have the operator-facing version. If invitations fail, the order to debug is: pm2 logs (real Auth0 error) → Auth0 → Monitoring → Logs (per-attempt cause) → walk this checklist.
- **`provisionTenantOrg` skips steps 6 + 7 of the manual checklist** — when auto-provision is on, the API creates the org and enables Username-Password + Google connections with `assign_membership_on_login: true` in one call. Steps 1–5 + 8 (M2M scopes, env vars, SPA URIs, app-level connection toggles, role-claim Login Action) are still one-time tenant-wide setup that the platform admin does before the first auto-provisioned tenant. Auto-provision is enabled by default in the Platform → New Tenant card; uncheck if you want to manually paste an existing `org_…`.
- **Auth middleware reconcile uses a 3s Promise.race timeout** — the user-row lookup in `requireAuth` races against `setTimeout(reject, 3000)`. If the DB hangs, the timeout rejects, the catch logs a warning, and the request continues with the JWT role — never produces ERR_EMPTY_RESPONSE for the upstream client. The `last_login_at` UPDATE is fire-and-forget (`.catch(...)` only), so the request never blocks on a write that nobody reads in this request.
- **Fastify preHandlers in arrays must be async or use `done` callback** — a sync hook without a `done` parameter (e.g. `function requirePlatformAdmin(req, reply) { ... }`) chained inside a `preHandler: [requireAuth, requirePlatformAdmin]` array silently breaks the chain — the route handler never runs and the request hangs forever with no error. Always declare guards as `async function` (Fastify awaits the returned Promise). Symptom that took hours to find: logs show every preHandler running, the last one's "pass" log fires, then nothing — no route handler log, no `request completed`. Diagnostic was adding a log line at the very top of the route handler and seeing it never fire.
- **Module registry is single source of truth** — `api/src/config/modules.js` lists every module the platform knows about. To add a new one: (1) add it to `MODULES` with a `group` (or `core: true` for always-on) and per-built-in `default` permission map, (2) if it's part of an existing group, append its key to `MODULE_GROUPS[i].moduleKeys`; if it's its own product, add a new entry to `MODULE_GROUPS`, (3) write a migration to `INSERT INTO tenant_modules (tenant_id, module_key, is_enabled) SELECT t.id, '<new_key>', true FROM tenants t ON CONFLICT DO NOTHING`, (4) wire `requirePermission('<new_key>', 'manage')` on the relevant routes, (5) add a `module: '<new_key>'` field to the relevant `NAV` entries in `AppShell.jsx`. Don't hardcode module keys outside this file.
- **Site model is one-per-TENANT** — `tenant_site` (singleton per tenant, renamed from `tenant_brand_defaults` in migration 043) owns subdomain, custom domain, brand identity, home_blocks, SEO, banner, locations-index settings. `website_config` (one per venue) owns location-page content only: hero, gallery, hours, address, contact, menus, allergens, plus a `page_blocks` jsonb for the per-venue page layout. Don't put subdomain/SEO/home_blocks on `website_config` — they're tenant-level now. Conversely don't put address/hours on `tenant_site` — those are per-location. The renderer merges them at request time via `mergeLocationConfig()` in `siteDataSvc.js`.
- **Routes**: `/` → tenant home (renders `tenant_site.home_blocks`, falls back to `fallback_home.eta`). `/locations` → list of all venues (404 if `hide_locations_index`). `/locations/:venueSlug` → location page (`website_config.page_blocks`, falls back to `fallback_location.eta`). `/locations/:venueSlug/menu[/{id}]` → location menu(s). `/menu` → tenant menu hub (lists venues with menus). `/p/:slug` → tenant-level custom page. `/locations/:venueSlug/p/:slug` → venue-level custom page. `/widget/:venueId` → venue-direct widget (deep link). `/widget/tenant/:tenantId[?venue=ID]` → tenant widget with location picker.
- **`website_pages` venue_id is nullable** — `venue_id IS NULL` means a tenant-level page (visible in tenant-home nav); non-NULL means a location-specific page (visible only in that location's nav). Slug uniqueness is enforced by two partial unique indexes (one for tenant-level, one per venue). The admin `/website/pages?venue_id=tenant` flag selects tenant-level; a UUID selects venue-level.
- **`brand-defaults` admin endpoints alias `tenant-site`** — kept temporarily so the `BrandIdentitySection`/`BrandThemeSection`/`BrandAnalyticsSection`/`BrandBannerSection` admin components keep working without rewrite. Future cleanup: rename the JSX components and switch to `/tenant-site` directly. Both routes write to the same row.
- **`/website/config` POST + PATCH no longer accept subdomain/custom_domain/is_published/meta_title/meta_description** — they were dropped from `website_config` in migration 043. The Zod `VenueConfigBody` rejects these keys; submitting them returns a 400. Move them onto the `/website/tenant-site` PATCH instead.
- **`PageBuilder` accepts `blocksField`/`saveEndpoint`/`invalidateKey` props** — defaults to tenant home (`home_blocks`, `/website/tenant-site`, `['tenant-site']`) but the `<VenueActiveSection>` dispatcher passes `page_blocks`, `/website/config?venue_id=X`, `['website-config', venueId]` for location pages. Don't hardcode `home_blocks` in any new builder code — always read the field from props.
- **`booking_widget` block has 4 fallback levels** — block-data `venue_id` → location's `it.venue.id` → `tenant_site.default_widget_venue_id` → `/widget/tenant/:tenantId` (location picker). On a tenant home page with multiple venues and no default venue set, it renders the picker. Setting `default_widget_venue_id` on the tenant skips the picker for the home embed.
- **Module groups are the unit of tenant-level toggling, NOT modules** — bookings/venues/tables/schedule/rules/customers/widget_test all belong to the `bookings` group and toggle together (they're operationally one product). The `/access` UI shows one switch per group; `PATCH /access/module-groups/:key` upserts every member module's `tenant_modules` row in one transaction. Don't add per-individual-module toggles to the Modules tab — that's a deliberate design choice. The per-module endpoint (`PATCH /access/modules/:key`) is kept for internal use only.
- **Lightsail wildcard cert setup runbook** — Lightsail DNS has no Certbot plugin, so wildcard issuance uses `certbot --manual --preferred-challenges dns-01` with AWS CLI hook scripts. The full sequence is documented in the chat history (this session, May 2026). Key pieces: (1) IAM user with policy `lightsail:GetDomain`, `lightsail:CreateDomainEntry`, `lightsail:DeleteDomainEntry` on the domain, (2) AWS CLI v2 + credentials on the server, (3) `/etc/letsencrypt/lightsail-auth.sh` and `/etc/letsencrypt/lightsail-cleanup.sh` hook scripts, (4) `certbot certonly --manual --manual-auth-hook ... --manual-cleanup-hook ...`, (5) renewal cron at `/etc/cron.d/certbot` running every Monday + Nginx reload hook. **Don't switch to Route53 or Cloudflare** — the user's preference is Lightsail-everything; the hook-script path keeps that. **Don't switch to Caddy on-demand TLS** for the wildcard either — saved for a future round if custom-domain provisioning becomes high-traffic. Re-running `certbot renew` is idempotent; the hook scripts handle the dance with Lightsail DNS each time.
- **Built-in roles are never deleted, but their permissions can be edited** — `tenant_roles.is_builtin = true` for the four seeded roles. The DELETE endpoint refuses to delete them (422). The PATCH endpoint allows full permission edits (so a tenant can take, say, `email_templates: manage` away from their admin role). The role's `key` is immutable in code (used to map legacy `users.role` enum) — the editor doesn't show a key field for built-ins.
- **Permission gate ordering: tenant module switch wins, then role permission** — `requirePermission()` first checks `tenant_modules.is_enabled` for the tenant; if disabled, even an owner is rejected with 403. Only if the module IS enabled does it check the user's role permission level. Same logic in `/api/me` when computing the `permissions` map. So an owner CAN turn off their own access by disabling a module — that's intentional, gives a single switch to neutralise a feature platform-wide for the tenant.
- **Custom roles bypass the legacy `users.role` enum** — when `users.custom_role_id` is set, the auth path resolves permissions from `tenant_roles.permissions`. When it's NULL (default), it maps `users.role` enum → `tenant_roles.key` (matching one of the 4 built-ins) and uses those. The legacy enum stays for backwards compat — don't remove the column.
- **`/api/me` is the SPA's nav-gating contract** — frontend reads `me.permissions[module]` to decide whether to render a nav entry, route guard, or button. After ANY mutation that affects modules or roles (PATCH module toggle, role permission edit), invalidate `['me']` so the UI refreshes. The TanStack Query stale time is 60s by default — without manual invalidation, changes only show up after a minute.
- **Nginx `client_max_body_size` defaults to 1 MB and silently breaks PNG uploads** — the API allows 30 MB via `@fastify/multipart`, but Nginx in front of it caps the request body at 1 MB by default. JPEGs are usually < 1 MB so they slip through; PNGs are typically 2–10 MB and get rejected with 413 before they reach Fastify. Symptom from the operator's perspective: "I can't upload PNG files" (sometimes works for tiny PNGs, never for normal photos). Fix: every server block that proxies to the API must include `client_max_body_size 30M;` (or higher if the API ceiling is raised). `setup.sh` now sets it on the apex block; the wildcard `*.macaroonie.com` server block must have it too. Same trap applies to PDF menu uploads (25 MB API limit) and receipt uploads in cash-recon.
- **Google Fonts v2 returns HTTP 400 for the WHOLE stylesheet if any one requested weight is missing for any one font** — `head.eta` and `FontPicker.jsx` both maintain a `FONT_WEIGHTS` map of which weights each Google Font ships with, and the URL builders request only those weights per-font. Earlier code requested `wght@300;400;500;600;700;800` for every font, which broke whenever a picky font (Caveat, DM Serif Display, Libre Baskerville, etc.) ended up in the list — the entire CSS file failed and ALL fonts (not just the picky one) silently fell back to system defaults. When adding a new font to the picker: add it to BOTH `FONT_WEIGHTS` maps with its actual supported weight set (look up on fonts.google.com under "Styles"). Don't request a weight a font doesn't have.
- **Page builder block partials should use a unique scoped class for any layout that needs media queries** — inline styles can't carry `@media` rules, so a fixed `display:grid; grid-template-columns:repeat(3,1fr)` inline style means the block stays 3-col forever and ends up wider than the viewport on phones, producing a horizontal scroll strip on the right. Pattern used by `menu_inline.eta`, `dish_list.eta`, `reviews_band.eta`, `story_with_stamp.eta`: derive a `_blockId` from `it.block.id`, emit a `<style>` tag with the desktop layout AND `@media` collapse rules keyed on that class, then put the class on the wrapper. Always pair this with `overflow-x: hidden; max-width: 100vw` on `html, body` (in `head.eta`) as the defensive top-level guard so any future block that overflows doesn't leak the strip.
- **Header block has separate desktop + mobile visibility flags** — `mobile_show_logo`, `mobile_show_brand`, `mobile_show_subtitle`, `mobile_show_links`, `mobile_show_cta`, `mobile_breakpoint`. Defaults: logo + brand + CTA on, subtitle + links off, breakpoint 768px. Each emits a `display:none` rule inside `@media (max-width: ${mobile_breakpoint}px)`. Don't conflate `show_logo` (desktop master) with `mobile_show_logo` — both must pass for the logo to render on mobile.
- **Eta JS blocks must NOT contain regex literals with quote chars OR template literals with `${...}`** — Eta's parser tracks quote state naively to find the closing `%>`. A regex like `/^["']|["']$/g` opens a `"` and a `'` that the tracker counts as live string state, never closes them, never finds the matching `%>`, and consumes the rest of the file as JS. Symptom: a 500 with "unclosed string at line N col M" pointing at a JS line FAR below the broken block — usually inside a `<script>` tag that has nothing to do with the cause. Same trap with template literals like `` `family=${x}&display=swap` `` — Eta misreads the `${` as Eta interpolation territory in some configs, or simply gets confused by the quote-balance asymmetry. Fix: use plain `String(x).replace(/^#/, '')` for non-quote regexes, define helper functions outside the block if you need quote-bearing regexes, and use string concatenation (`'a=' + x + '&b=' + y`) instead of template literals inside `<% %>` blocks. Bit us in `widget.eta` (May 2026) — the new top-of-file `<%` block had `replace(/^["']|["']$/g, '')` which broke widget rendering with an error that pointed 170 lines below into the public JS. Reserve template literals for code OUTSIDE Eta blocks (i.e. inside script tags meant for the browser).
- **Eta's `autoEscape: true` HTML-escapes `"` to `&quot;` in `<%= %>` output — silently breaks CSS font-family strings** — if you build a font-family value in JS that contains the quotes (e.g. `const x = '"Poppins", system-ui, sans-serif'`) and emit it via `<%= x %>`, the rendered CSS becomes `font-family: &quot;Poppins&quot;, system-ui, ...` which is invalid. The browser silently falls through to the next font in the stack (usually `system-ui` which looks plausibly correct on most OSes), so every font picker on the Reservations widget LOOKED like it did nothing for weeks. **Fix: use `<%~ %>` (raw, no escape) when the JS value already contains literal CSS-safe quotes.** Affected files were `reservations_widget.eta` at `--r-font-cal`, `--r-font-slot`, and the body `font-family`. The safer alternative pattern (matches what `head.eta` already does) is to keep quotes LITERAL in the template — `font-family: "<%= bareFontName %>", system-ui, ...;` — so escaping only touches the bare font name. Any time you're tempted to build a quoted CSS value in JS and `<%=` it, stop and use `<%~` instead.
- **Eta auto-declares `include`, `includeAsync`, `__eta` AND `layout` at the top of every compiled template** — any `const/let/var layout = ...` (or the same with the other names) inside a block's `<% %>` produces `SyntaxError: Identifier 'layout' has already been declared` and the whole page 500s. Bit us in `gallery.eta` (May 2026) where `const layout = d.layout || 'grid'` collided with Eta's own `function layout(path, data)`. Fix: rename your variable (`layoutMode` is fine), but more generally avoid `layout` / `include` / `includeAsync` / `__eta` as identifiers inside Eta partials. The compiled output is visible in the 500 response body — look for `function layout(path, data)` near the top and your conflicting `const` below.
- **`setAttribute('disabled', false)` STILL DISABLES the button** — browsers treat the presence of the `disabled` attribute as disabled regardless of the value (`'false'` as a string is truthy from HTML's POV). Bit us hard in the reservations widget's `el()` helper (May 2026): an `else if (v != null) setAttribute(k, v)` catch-all ran for `disabled: false`, silently disabling every calendar cell and slot button for hours. The fix: handle `disabled` (and other boolean HTML attributes like `checked`, `readonly`, `required`) explicitly — set the attribute ONLY when the value is truthy. Same idea: in any DOM-builder helper, the catch-all branch must skip falsy values (`v != null && v !== false`) or the boolean-presence trap will catch you again.
- **`/relocate` step 3b does NOT apply the non-adjacent rule to pre-configured combos** — the adjacency check only governs step 3c (automatic expansion). Operator-configured combinations are always eligible as relocation targets regardless of sort-order gaps. If you add a new step that filters combinations before step 3b's break, do not re-introduce the `isAdjacentSet` check on that path.
- **Widget `tryDisplaceCombo` must check free combos as well as free individual tables** — the SQL slot-availability subquery and the JS displacement logic must stay in sync. If you add a new displacement destination type, update both in `widgetApi.js` or slots will show as available but holds will return 409.
- **Do not diagnose 422 errors on `/relocate` as "missing combination" without checking the error message text first** — there are multiple distinct 422 paths (no allocation found, no pre-configured combo, locked booking). The red banner in the Timeline shows the specific message. Jumping to "create a combination" as the fix is only correct for the "No table combination is configured…" message.
- **`overlapPeerMap` (and any other value from a late-declared useMemo) must be bridged via `useRef` if used in a `useCallback` earlier in the component** — TDZ applies to `const` declarations inside React component functions just like anywhere else. Pattern: `const peerMapRef = useRef(new Map())` at the top, `peerMapRef.current = overlapPeerMap` immediately after the useMemo, read `.current` inside the callback. Never add the useMemo's return value to a `useCallback` dep array when the memo is declared later.
- **`manual_mode: true` on `/relocate` skips conflict resolution but still runs allocation (steps 1-4)** — the booking is placed on the best available single table or pre-configured combo for the target. It does NOT skip the "no combination configured" 422 (step 4). To place onto an arbitrary set of tables ignoring all rules, use `PATCH /bookings/:id/tables` directly.
- **The Edit tool can introduce curly/typographic quote characters (U+2018 `'`, U+2019 `'`) as string delimiters** — these are NOT valid JavaScript string delimiters. They cause two distinct failure modes: (a) in JSX/JS files, esbuild throws `Unexpected "'"` at build time; (b) in Eta `<% %>` server-side blocks, V8 throws `Invalid or unexpected token` at render time producing a 500 on every request. In the `<script>` HTML section of Eta templates they don't break server-side compilation but DO break browser-side JS execution. Diagnosis: `python3 -c "data=open('f','rb').read(); [(print(i,l)) for i,l in enumerate(data.split(b'\n'),1) if b'\xe2\x80\x98' in l or b'\xe2\x80\x99' in l]"` — U+2018 is `\xe2\x80\x98`, U+2019 is `\xe2\x80\x99`. Fix pattern for JSX: use double-quoted strings. Fix pattern for Eta templates: Python byte-level global replacement — `data = data.replace(b'\xe2\x80\x98', b"'").replace(b'\xe2\x80\x99', b"'")`. Do NOT use `content.replace(b'\xe2\x80\x98\xe2\x80\x99', ...)` (replaces adjacent pairs only). Replace each byte sequence independently. Bit us in `ReservationsWidget.jsx` (build-time, May 2026) and `reservations_widget.eta` (runtime 500, June 2026) — 153 curly quotes introduced during confirmation-page feature work.
- **`backlog_items` has NO RLS — it is a global platform table, not tenant-scoped** — all backlog operations use raw `sql` (no `withTenant()`), and the route is gated by `requirePlatformAdmin`. Same pattern as `platform_admins`. Never add RLS to it. In contrast, `issue_log`, `feature_requests`, `feature_request_upvotes`, and `changelog_entries` are all tenant-scoped with RLS and must go through `withTenant()` on routes that read/write them.
- **Apify scrape jobs are fire-and-forget BullMQ** — `POST /api/reviews/scrape` enqueues via `reviewScrapeWorker.js` and returns immediately. The worker polls Apify asynchronously. If `APIFY_API_TOKEN` is missing the job fails silently (no crash). Check `GET /api/reviews/scrape-jobs` to see job statuses.
- **JSX attribute strings follow HTML rules, not JavaScript string rules — `\'` is NOT a valid escape** — in `hint='You\'re done'`, the `\` is a literal backslash, `'` terminates the attribute, and the parser sees unexpected tokens. esbuild error: `Expected ">" but found "\"`. Fix: use double-quoted attributes (`hint="You're done"`) or JSX expressions (`hint={'You\'re done'}`). Double-quoted attributes handle apostrophes with no escaping needed. Bit us in `ReservationsWidget.jsx` and `SiteBlockEditors.jsx` (May 2026) on `hint='Override "You\'re booked"...'`.
- **Auth0 access tokens (RS256, custom API audience) do NOT contain `email`** — only the ID token does. `payload.email` in `auth.js` is always `undefined` unless the Login Action explicitly adds it via `api.accessToken.setCustomClaim(ns + 'email', event.user.email)`. Without it, the email-link UPDATE that links an invited user's `auth0_user_id` on first login never fires → `userNotFound = true` → 403 forever. The fix is in the Login Action (add the claim) + auth.js (read `payload[CLAIM_NS + 'email'] ?? payload.email`). Bit us badly (June 2026) — took many sessions to find.
- **`auth.js` user reconciliation MUST use `withTenant()`, not bare `sql`** — the `users` table has RLS enabled. The middleware runs outside any request-scoped tenant context, so a bare `sql` query against `users` returns 0 rows (RLS silently blocks all reads). The email-link UPDATE (`SET auth0_user_id = ... WHERE lower(email) = lower(...)`) also requires `withTenant` or it matches 0 rows even when the invited user row exists. Symptom: every first-login attempt gets "Your account has not been set up for this organisation" (userNotFound = true) despite the row being present in the DB. Bit us (June 2026). Fix: wrap all SELECT and UPDATE against `users` inside `requireAuth` in `withTenant(tenantId, tx => tx\`...\`)`.
- **`req.auth0OrgId` is the CALLING USER's JWT org — not the target tenant's org** — when a platform admin acts on a tenant, their JWT carries the platform org ID (e.g. `org_FgM9cXJbMwp9jAwE` = macaroonie), NOT the tenant's org. Using `req.auth0OrgId` in `team.js` for `inviteUserToOrg` / `removeUserFromOrg` / `sendPasswordResetEmail` sends those Auth0 operations to the platform org, not the tenant. Invited users then get a JWT with the wrong `org_id`, which maps to no DB tenant → "Tenant not found" 403. Fix: always fetch the tenant's Auth0 org from the DB — `getTenantAuth0OrgId(tenantId)` queries `SELECT auth0_org_id FROM tenants WHERE id = $1`. Same applies to any service that acts on behalf of a tenant via Auth0 Management API. Bit us hard (June 2026).
- **`tenants.nav_style = 'launcher'` hides the ENTIRE sidebar tenant-wide, and `nav_items.show_in_launcher` defaults to `false` for every item** — switching a tenant into Launcher mode before an admin has flagged at least one nav item as a quick-access tile leaves every user (including the admin) with no sidebar. The Nav Designer's Sidebar/Launcher toggle is the way back. **Correction (same day):** the escape hatch was first added only to `/launcher`'s empty state, which meant a tenant with even ONE tile configured had no way back — reproduced live in production. Fixed by moving "Back to full menu" into the page header, always visible whenever `nav_style === 'launcher'` and the viewer has `nav_designer:manage`, regardless of tile count. Don't gate a nav escape hatch on any content-dependent condition — it needs to be unconditionally reachable or it isn't an escape hatch.
- **Dashboard's `SHORTCUT_OPTIONS` (the per-user Quick access widget on `/`) is a SEPARATE feature from the Navigation designer's Launcher** — they're both called "quick access" in normal conversation, which is confusing. `SHORTCUT_OPTIONS` is a hardcoded catalog in `Dashboard.jsx` that every user picks their own subset from, always available regardless of `nav_style`. The Launcher (`/launcher`) only exists when `nav_style = 'launcher'`, only shows links an admin flagged in the Nav Designer, and replaces the sidebar rather than living inside the Dashboard page. When a new top-level module ships, remember to add it to `Dashboard.jsx`'s `SHORTCUT_OPTIONS` — this was missed for Food safety, Checklists, and H&S Dashboard for a long stretch.
- **Eta `<% %>` JS string literals need the same apostrophe-escaping as any other single-quoted JS string, but this also applies inside `DataTable`/table-row arrays in Help.jsx and Docs.jsx** — a straight apostrophe inside a single-quoted array-literal string (e.g. `'this week's list'`) terminates the string early and breaks the esbuild parse with a confusing "Expected ']' but found ..." error pointing at the apostrophe, not the actual mismatched quote. Fix: escape it (`'this week\'s list'`) — plain JSX text content (inside `<P>`/`<H3>` etc., not a JS string literal) does NOT need escaping, only literal JS string values do.
- **"Who completed this" fields are plain `text` storing an email, not a `uuid` FK to `users(id)`** — `checklist_instances.completed_by` set the precedent (`req.user?.email`), and `hs_action_log.completed_by`/`created_by` follow it. This was nearly gotten wrong building the Action Log (drafted as a `users(id)` FK before checking the existing convention) — a FK would also be wrong in practice since not every JWT-bearing caller (e.g. a platform admin with no local `users` row) can be resolved to a local user id, while `req.user.email` always exists. Check `checklist_instances` before inventing a new "who did this" column shape.
- **`MediaLibraryModal`'s `onPick` in picker mode passes `(url, item)` — the full media_items row is the second argument, not just the URL** — most existing callers (hero images, gallery pickers) only destructure the first arg because they store a plain URL string on their own record. Any new feature that needs a real FK to `media_items(id)` (rather than a denormalised URL) must take the second argument — see `hs_action_log.attachment_media_id` in `hsActionLog/shared.jsx` for the pattern.
- **postgres.js deserialises `date` columns to JS `Date` objects, not strings — this breaks map-key lookups built from date fields, silently** — `dashboardTiles.js`'s hs-status aggregation builds a lookup key `` `${template_id}|${period_start}` `` from a `checklist_instances` query; without `period_start::text` in the SELECT, the key becomes `` `${template_id}|Mon Sep 21 2026 00:00:00 GMT+0000 (Coordinated Universal Time)` `` instead of `` `${template_id}|2026-09-21` ``, so the lookup against the JS-string period computed by `periodStartFor()` never matches and a genuinely-completed weekly/monthly checklist always shows as incomplete. No error, no crash — just wrong data. This is the same family of gotcha as the existing `slot_time` entry above (composite-type `timestamptz` → JS `Date`), but for plain `date` columns feeding a string-keyed `Map`. Always cast date/timestamp columns to `::text` in the SQL when the JS value is going to be used as a map key or compared to a plain date string — don't rely on the driver's default deserialisation matching what you expect. Caught by writing a direct test against a disposable Postgres before shipping (see the food-safety migration-testing pattern below) — worth doing for any new cross-table date aggregation.
- **`cash_wage_entries.total` is the full wage cost; `cash_wage_entries.cash_amount` is only the cash-paid portion** — the two are independent and legitimately differ (part or all of a wage paid by bank transfer instead of till cash). Any net-cash-position calculation in cash recon must subtract `cash_amount`/`wages_cash_total`, never `total`/`wages_total` — using the full total silently overstates the cash shortfall whenever staff are paid other than in cash. Both wage-total SQL queries in `cashRecon.js` (`week-detail` and `week` routes) return both sums; don't add a third query that only selects one.
- **`cash_wage_defaults` only stores entries with a `staff_id`** — ad-hoc wage entries (typed name, no `cash_staff` row) have no stable identity across weeks, so "Set as default" silently drops them from the saved list. This is intentional, not a bug — surfaced via a `title` tooltip on the button rather than a popup per Standard Design Rule #4. If you ever need ad-hoc entries to persist as defaults too, you'd have to give them a durable identity first (e.g. auto-promote to a `cash_staff` row), not just store the free-text name.
- **`/mobile` is a second route tree, not a responsive mode of the desktop pages** — `AppShell.jsx` (sidebar, ≥1015px tablet chrome) and `MobileShell.jsx` (header-only, phone-portrait) are two separate layouts mounted as sibling top-level `<Route>` trees in `main.jsx`, both inside the same `RequireAuth`/`TenantGate`. Don't try to make an existing desktop page "responsive down" into the mobile experience — build a dedicated component under `src/pages/mobile/` (reusing the desktop page's data hooks/mutations/body components, not its layout) and register it in `src/mobile/registry.js`. `MobileHSDashboard.jsx` is the reference example.
- **The `/mobile` PWA identity swap is a runtime `<link>`/`<meta>` rewrite, not a second build** — there is one `index.html` and one `vite-plugin-pwa` instance (main manifest, scope `/`). `admin/public/mobile.webmanifest` is a hand-written static file (scope `/mobile/`) that ships in every build regardless of which routes a given visitor uses; `MobileShell`'s mount/unmount effect is what actually makes `/mobile` install under a different name/icon than `/`. If you add a new manifest field that needs to differ per surface (e.g. `orientation`), edit the static file directly — there's no shared generator between the two manifests.
- **iOS "Add to Home Screen" reads `<link rel="apple-touch-icon">` + `<meta name="apple-mobile-web-app-title">` from the current page, not the manifest** — pre-17.4 Safari (and many users are still on it) ignores the Web App Manifest for icon/name purposes. `MobileShell`'s manifest-swap effect must also rewrite those two tags, not just `<link rel="manifest">`, or the installed mobile icon/name on iPhone silently stays "Macaroonie" instead of "Macaroonie Ops".
- **A resolved `null` from `useQuery` is not the same as `undefined` — don't collapse them in a guard clause** — `data` is `undefined` while a query hasn't resolved yet, but becomes `null` once it resolves successfully if the API genuinely returns `null` (e.g. `GET .../wages/:week_start` returns `null`, not 404, when no report exists yet for that week). A guard like `if (!wagesData || !config) return` treats "confirmed empty" the same as "still loading" — so logic meant to run on confirmed-empty (auto-populate defaults, show an empty state) never fires. Check `wagesData === undefined` specifically when the success value can legitimately be `null`. Bit us in the Wages page's auto-populate effect.
- **Canvas components under `admin/src/components/website-builder/canvas/` are a second, independent implementation of their matching SSR `.eta` partial — there's no shared renderer** — any conditional-display flag added to a block (a menu-level toggle, a per-block option, a venue setting) must be ported into the canvas version too, or the page-builder's live preview silently drifts from what actually publishes. Caught with `MenuInlineCanvas` (`dataBlocks.jsx`) ignoring `hide_zero_priced_variants`/`hide_unpriced_variants` for a while after `menu_inline.eta` already had them. When adding a new toggle to any menu/website block, grep both the block's `.eta` partial AND its canvas counterpart before considering the work done.
- **Some touch devices never surface their on-screen keyboard for a text/number input — this is a platform limitation, not something `inputMode` can fix** — confirmed on at least one Linux/Chrome tablet tapping a `type="number" inputMode="decimal"` field. Desktop Linux has no standardised "show virtual keyboard on touch focus" contract the way iOS/Android/ChromeOS do, and Chromium's own virtual-keyboard support on Linux is a long-standing known gap. Don't chase OS-specific workarounds — sidestep it with the same custom on-screen keypad pattern already used for covers entry (`NewBookingModal.jsx`'s `IS_TOUCH` + `CoversInput`/`NumericKeypad`) and now temperature entry (`foodSafety/shared.jsx`'s `TempKeypadModal`): render a button instead of a native input on touch devices, open a full-app keypad UI on tap.
- **`boxedLayout.js`'s current tenant step values are a module-level variable, not React Context** — `currentBoxedSteps`, set once per render by `themeResolver.js`'s `setBoxedSteps()`. Deliberate: there is only ever one `<ThemeFrame>` mounted at a time in the admin (the page-builder canvas), so there's no risk of two different tenants' step values colliding. If a second concurrent canvas is ever added, this must become a Context instead.
- **`Menus.jsx`'s save-payload builder whitelists fields explicitly — it does not spread `...item`/`...section`** — the read-side draft hydration DOES spread (`{...s}`, `{...it}`), so a newly-added field flows into local editor state and displays/edits fine, giving the false impression that wiring is complete. But the PATCH-time payload builder (`(draft.sections || []).map(...)` in the save mutation) lists fields one by one; a field left out of that list is silently dropped on every save even though the UI showed it correctly right up until the page reload. Whenever you add a field to `menu_items`/`menu_sections` (or any similarly-shaped whole-tree-upsert page), grep for the explicit save-payload mapping specifically — don't assume the read-side spread means the field is already wired end-to-end. Bit us building `calories`/section `image_url` (caught before shipping, not in production).
- **A structured-data feature can ship years of iteration with zero Help.jsx/Docs.jsx coverage if nobody added a section for it at the time** — the entire Menus feature (migration 048 onward: sections, dishes, variant groups, dietary tags, print, the `menu_inline` website block) had no entry in either file until the `calories`/section-image change added the first one. Don't assume "no doc gap flagged" means "no doc gap exists" — if you're about to document a small change to a feature and can't find its existing section, that's a sign to add a baseline section, not skip documenting because "there's nowhere to put it."
- **A component's own margin and a parent's `space-y-*` are two competing spacing mechanisms — don't use both for the same gap** — `CashRecon.jsx`'s daily-declaration page wrapped its `SectionCard`s in `space-y-0` while `SectionCard` itself carried `mb-4`, apparently on the assumption the two wouldn't conflict. In production the visible gap between stacked cards was zero (colour-matched header/footer accent bars touching directly, no seam) — whatever the exact cause, the practical lesson holds regardless of the precise CSS mechanics: don't split responsibility for one visual gap across a child's own margin AND a parent's spacing utility. Pick one (usually the parent's `space-y-*`, since it's visible at the call site and doesn't require reading the child's internals) and remove the other.
- **`hideHeader` mode on a shared panel can silently drop functionality the full-page mode already has** — `ChecklistRunPanel`'s `hideHeader` mode (used by the H&S Dashboard's checklist widget) re-derives its own compact header affordance from `onStateChange`, which only exposed `markComplete`, not `reopen` — even though the full-page mode's own footer already had a complete, working double-confirm reopen flow. When adding a `hideHeader`/compact-mode branch to a shared component, diff its exposed capability against the full mode's — a host-rendered compact header is easy to under-build relative to what the component itself can already do.
- **`POST` endpoints that require an id the frontend doesn't have in scope will 400 forever, silently, if the catch block is empty** — `POST /:venueId/cash-recon/expenses` required `report_id`, but its only caller (`ExpensesSection.handleAdd()`, which only has `venueId`/`date` as props) never had one to send and sent `date` instead. Zod validation failed on every call; the `catch {}` around it meant no operator ever saw an error, so "Add expense" just silently did nothing from the button's perspective while the row visibly appeared to have been added optimistically nowhere (it wasn't — `setExpenses` inside the `try` block never ran). Caught only when a second, independent caller (the mobile Expenses page) was built against the same endpoint and needed to understand its actual contract. Never wrap a mutating API call in a bare `catch {}` in new code — log the error at minimum, ideally surface it, so a broken contract like this doesn't go unnoticed indefinitely.
- **A reconciliation variance's sign convention should match the standard accounting one (Actual − Expected), and it's easy to accidentally write the reverse** — Cash Recon's variance was computed as Income (expected) − Takings (actual) in every one of its three independent implementations (`SpreadsheetView`'s `variance(date)`, the day-view's `const variance`, and `WeekView`'s per-day-card recompute). That reads backwards from what an operator expects: taking in MORE cash than declared showed as a NEGATIVE number, and less showed positive. When adding any "did the actual figure match the expected one" calculation, write it as `actual − expected` from the start and sanity-check the sign against a simple story ("I took in more than I declared — should this be positive or negative?") rather than just whichever order felt natural to type.
- **A value duplicated across 3 independent call sites can each drift differently — `WeekView`'s day-card variance referenced a field, `total_sc_in_takings`, that was never actually in its API response** — `parseNum(undefined)` silently returns `0`, so the missing field just meant "no SC adjustment ever applied here," not a crash or a visible bug — it took a deliberate read of the `/week/:week_start` route (which already computed its own, correctly-signed `variance` field) to notice the client was ignoring it and recomputing its own, wrongly-signed version from partly-nonexistent inputs. When a figure is computed in more than one place from what should be the same inputs, check whether the backend already computed it correctly before re-deriving it client-side — the duplication itself is usually the bug, independent of which copy turns out to have the wrong sign.
- **`cash_payment_channels.type` (`'cash'|'card'|'voucher'|'online'|'other'`) was never actually used to decide what counts toward Net Cash — it only drove the tooltip/icon selection** — every active channel fed `Net Cash`/`Cash to bank` regardless of type, so a venue with a "Card machine" channel had card takings silently inflating its cash total. Fixed with an explicit `counts_as_cash` boolean (migration 097) rather than inferring it from `type` at read time, because an operator may want a channel's cash-contribution to diverge from its display type (e.g. a "Cash app" channel that's technically `type: 'online'` but should still count as cash). Don't assume an existing categorical field already gates a calculation just because the categories would make sense for it — grep the actual formula.
- **Two functions in the same file can share a name with radically different implementations if one is dead code from an old refactor** — `Website.jsx` has both a `ThemeSection({ config })` (~line 1628, unreferenced — no `<ThemeSection` anywhere in the file) and a `BrandThemeSection()` (~line 3413, rendered under the `tenant-brand` nav case) that both edit `theme.colors.*`. Adding a new theme field to the wrong one (the plausible-looking, alphabetically-earlier `ThemeSection`) compiles fine and looks correct in a diff, but has zero effect on the live app. Before editing a form/section component, grep for where it's actually rendered (`<ComponentName`) — don't assume a component that looks like "the" theme editor is the one wired into the nav switch.
- **The 5 shared `--boxed-step-N` CSS variables in `head.eta` are the ONE place the mobile-portrait breakpoint (`max-width: 600px` and `orientation: portrait`) is defined for boxed-inset spacing** — both the theme-level default (`--boxed-pad`) and every per-block `boxed_step` override resolve through these vars, so adding per-step `mobile_value`/`mobile_unit` support once here made every existing per-block override mobile-responsive automatically, with no changes needed in any of the ~19 block partials that reference `boxed_step`. When a per-step-number setting needs viewport-dependent behaviour, prefer resolving through a small set of shared CSS custom properties over recomputing a literal value at each call site — the literal-value approach (what this codebase used before) means every consumer needs its own media query.
- **A form field can display and save correctly for months while being completely unread at render time — always trace the actual render path, not just the save path, before trusting a field is "working"** — the venue-level Identity page's `site_name`/`tagline` fields round-tripped through the API fine (real columns, accepted by Zod, returned on GET) and displayed whatever was typed, giving every appearance of working. But `mergeLocationConfig()` never included them in what it inherited per-venue — `head.eta`'s `it.config.site_name` on a location page always read the TENANT's value, never the venue's own. Caught only by explicitly grepping every reader of `config.site_name`/`config.tagline` after a user asked "why do we need this page" — a save button that works and a field that renders correctly in the admin are necessary but not sufficient evidence that a value actually reaches the public site. When in doubt about whether a per-entity override field does anything, grep its readers, not just its writers.
- **A "defensive" hack that forces one side of a merge to always win can be the more serious bug, not the fix** — `brandTheme.js`'s `mergeThemes()` deep-merged a venue's theme under the tenant's, then explicitly overwrote `colors`/`typography` back to the tenant's values regardless, to stop a stale venue-level theme from silently overriding a template's fonts/colours on the live site (a real bug it was patching). But this meant a venue's theme override was ALWAYS partially ignored (colours/typography) and partially applied (spacing/radii/etc) with no way for an operator to tell which was which from the admin UI — a confusing, half-working state that outlived the original bug it patched. Migration 098's `use_brand_override` boolean replaced both the original bug and its patch with one explicit binary switch (tenant's values in full, or the venue's own in full) — when a per-field "inheritance" model needs a special-case override to stop a specific field from behaving badly, that's usually a sign the per-field model itself is wrong, not that it needs one more exception.
- **A `bg-*/NN` (opacity-suffixed) Tailwind background on a `position: sticky` element lets whatever scrolls underneath it bleed through, visibly** — `TempChecksTable`/`HoldChecksTable`'s sticky Equipment/Station header cell used `bg-muted/50` (50% opacity). On a phone-width viewport, with the table's remaining columns wide enough to require horizontal scroll, the next header's text scrolled to sit partially underneath the sticky cell and showed through as garbled overlapping text ("EquipmentRange"). A sticky element meant to mask content scrolling behind it needs a fully opaque background — reserve opacity-suffixed backgrounds for elements nothing will ever scroll underneath. The sticky *body* cells in the same tables already used solid `bg-background` and never had this problem, which is why the bug only showed in the header row.
- **Auth0's "Types of Users" application setting (Login Experience tab) has three options, and picking the wrong one either reopens public signup or reintroduces the org-picker screen the admin portal's client-side `TenantGate` was built to replace** — `RequireAuth` (`main.jsx`) deliberately omits `organization` from `loginWithRedirect()` so tenant selection happens in our own `TenantGate`/`TenantSwitcherModal`, not via a second Auth0 screen. But whether Auth0 honours that depends entirely on this dashboard setting, which is easy to get wrong because two of the three options look similar: **"Individuals"** disables organization support for the app entirely (`organization_usage: deny`) — this breaks Auth0 organization invitations outright (confirmed live: sending an invite to an email that had signed up under this setting returned a bare "Unprocessable Entity" from Auth0's invitation API). **"Both"** ("users may sign up with a personal account and be affiliated with one or more organizations", `allow`) does support invitations, but also means literally anyone with a Google/email account can complete a normal login with zero organization — they land harmlessly on `TenantGate`'s "No restaurant yet" screen, but a real, standalone Auth0 identity now exists for that email regardless of whether anyone ever invited them. Confirmed live: a fresh, never-invited Gmail account completed a full login and reached the app under "Both". **"Business Users"** (`require`) is the correct setting for this app's invite-only model — an email with zero organization memberships cannot complete a login at all, Auth0 rejects it before the app is ever reached, and normal invitation acceptance (which always supplies an explicit `organization` param from the invite link) is unaffected. The one accepted tradeoff: an account that's a genuine member of 2+ organizations and logs in with no stored tenant preference will see Auth0's own "choose an organization / continue with personal account" hosted screen — Auth0 has no way to resolve the ambiguity itself when we deliberately don't pass `organization`, and there's no dashboard setting to suppress that specific screen while keeping "Require". In practice this only affects platform-admin/multi-tenant test accounts (single-tenant restaurant staff — the overwhelming majority of real users — never hit it, since Auth0 resolves an unambiguous single membership silently). Closing that gap fully would require the app itself to remember the last-used tenant's `auth0_org_id` and pass it as a login hint — deliberately not built, since a stale/revoked hint risks Auth0 hard-rejecting a login that should otherwise succeed, which is a worse failure mode than an occasional picker screen for a handful of internal accounts. Revisit only if a real (non-test) customer account ever legitimately needs 2+ tenant memberships.
- **A page under `AppShell` must reserve left-side space for the fixed collapsed-sidebar hamburger button, or its own title overlaps it below `lg`** — `AppShell.jsx` renders `<button className="fixed top-3.5 left-3.5 z-10 ... lg:hidden">` whenever the sidebar is collapsed (the normal state on any viewport under `lg`, and always on mobile). A page's own header needs `pl-14 lg:pl-<normal>` (see `OrderSheets.jsx`'s `pl-14 lg:pl-4`) so its title doesn't render directly underneath that icon. `Dashboard.jsx`'s Overview header (`px-6 h-14 border-b`, no left-padding override) had this bug for a long stretch — invisible on tablet/desktop where the sidebar is normally expanded, only visible once someone actually collapsed the sidebar or opened the page on a phone. When adding a new top-level page header under `AppShell`, check it against a collapsed sidebar at phone width before considering it done — this is the same class of bug as the sticky-header-opacity entry above (correct on the common path, silently wrong on a less-common one).
- **A tile/card grid built with a fixed `style={{ gridTemplateColumns: 'repeat(N, minmax(Xpx, 1fr))' }}` doesn't degrade at phone width — it either forces `N * X`px minimum (horizontal scroll) or squeezes below the `minmax` floor** — `Dashboard.jsx`'s Overview tile grid had this shape (4 columns, 240px minimum each = 960px+ before anything actually reflows). Fixed by switching to Tailwind breakpoint classes (`grid-cols-1 sm:grid-cols-2 lg:grid-cols-4`) with each tile's configured "span" (1-4 quarters) mapped through a `SPAN_CLASSES` lookup so a 4-wide tile is `col-span-1` on a 1-column phone grid, `col-span-2` (full width) on a 2-column tablet grid, and `col-span-4` only once the grid actually has 4 columns at `lg`. Any admin page with a resizable/spannable tile or card grid should use this same breakpoint-keyed span-class pattern rather than a fixed pixel-based `grid-template-columns`.
- **`MobileSuggestModal` (one-time nudge) and `MobileViewToggle` (persistent floating button) solve different problems — don't conflate them** — the suggestion modal fires once per browser on first phone-portrait detection and is then gone forever (`maca_mobile_prompt_dismissed`); it was never meant to be a recurring way back into `/mobile`, and before `MobileViewToggle` shipped there was no way at all to get from `/mobile/*` back to the full desktop admin short of manually editing the URL (the `MobileShell` header's back-arrow only goes to the mobile hub, not out of `/mobile` entirely). `MobileViewToggle` is deliberately simpler in its detection (`IS_TOUCH && matchMedia('(max-width: 700px)')`, no portrait requirement, no localStorage dismissal) since it needs to be reliably present on every visit, not just once.
- **`DayView` in `CashRecon.jsx` was already mobile-ready — no rework needed to reuse it in `MobileCashUp.jsx`** — its inputs are `flex-1 min-w-0` labels next to fixed `w-28` amount fields (never a wide multi-column grid), it already has its own sticky header with a back button (`onBack` prop), and `ExpensesSection` inside it is the same component `MobileExpenses.jsx` already reuses independently. This is the same "check whether the desktop body already reflows before rebuilding it" step the Order Sheets mobile module needed — DayView is the second confirmed case (Order Sheets was the first). Contrast with `WagesView` in the same file, which does NOT reflow: its 8-column staff grid (Staff/Type/Hours/Rate/Total/Cash paid/Notes/Remove) only survives narrow viewports via `overflow-x-auto` + `min-w-[760px]`, i.e. horizontal scrolling, not real responsiveness — so `MobileWages.jsx` is a genuine rebuild (card-per-entry, Name/Total/Cash-paid only) rather than a reuse, unlike `DayView`. Check both possibilities before assuming either.
- **A narrowed mobile form must still round-trip every field the desktop form has, even ones it never shows** — `MobileWages.jsx` only lets an operator edit `total` and `cash_amount` per staff entry, but its save payload still includes `entry_type`/`hours`/`rate`/`notes` read straight from the loaded entry, unchanged. Omitting a field entirely from the PUT body (rather than passing its current value through) would have silently zeroed it out server-side the next time a mobile save fired — e.g. an hourly entry's `hours`/`rate` would revert to blank the moment someone glanced at Wages on their phone, even though they never touched those fields. Any "focused" mobile rebuild of a richer desktop form needs to pass every field the API expects, not just the ones the mobile UI exposes for editing.
- **A `position: sticky` page header with `z-10`+ can fully hide (and swallow taps on) `AppShell`'s fixed collapsed-sidebar hamburger button, not just visually overlap it** — the earlier "page title sits under the hamburger" bug (Dashboard.jsx, fixed by adding `pl-14`) was cosmetic: both elements were visible, just crowded together, because Dashboard's header had no competing z-index. `CashRecon.jsx`'s page headers are `position: sticky` with their own `z-10`/`z-20`, which (a) creates a new stacking context and (b) ties or beats the hamburger's `z-10`, so the header paints fully OVER the button — it disappears completely and the tap lands on the page's own header instead. This is a strictly worse failure mode than the cosmetic overlap: the user has no way to open the sidebar at all, not even an ugly one. Fixed by raising the hamburger to `z-40` (safely above every in-page header currently in the app) rather than relying on every page to keep its own header below `z-10` forever — a single high z-index on the one thing that must always be reachable is more robust than auditing every page's stacking choices. When adding a new `position: sticky` page header, keep it below `z-40` or the hamburger fix stops covering that page.
- **A pre-existing native `<select>`-styled venue/venue picker on a content page is easy to mistake for a sidebar redesign, especially when the sidebar itself is unreachable** — `CashRecon.jsx`'s `VenueSelector` (an actual `<select>` element, styled to look like a button, switching between VENUES within the current tenant for reconciliation purposes) is completely unrelated to `AppShell`'s tenant-name-in-header switcher (added the same day, switching TENANTS). A user who can't open the sidebar (see the entry above) and then spots "Restaurant Name ⌄" at the top of Cash Recon has no way to tell the two apart from a screenshot alone — worth explicitly asking "which page is this dropdown on?" before assuming a bug report about "the sidebar dropdown" is describing the actual sidebar.
- **A mobile page's venue selector should match its own desktop page's show/hide rule, not a different mobile page's** — `MobileCashUp.jsx`/`MobileWages.jsx`/`MobileExpenses.jsx` all originally gated their venue `<select>` on `venues.length > 1`, copied from the pattern in `MobileHSDashboard.jsx` (which correctly mirrors `HSDashboard.jsx`'s own `venues.length > 1` gate). But `CashRecon.jsx`'s desktop `VenueSelector` is unconditional — it always renders, even for a single-venue tenant. Copying a gating rule from one mobile module to another without checking THAT module's own desktop counterpart produced a real inconsistency: a single-venue tenant saw a venue selector on desktop Cash Recon but not on the matching mobile pages. Fixed by changing the three Cash Recon-family mobile pages to `venues.length > 0` (always show once venues have loaded). `MobileHSDashboard.jsx` was correctly left alone — it already matches its own desktop page. When building a new mobile module, check that specific page's desktop show/hide rules, not another mobile module's.
- **Reusing a desktop component that renders its own header inside a `/mobile` full-page-detail view produces a double header — `MobileShell` renders its own, then the reused component renders its own underneath** — `MobileCashUp.jsx` reused `CashRecon.jsx`'s `DayView` verbatim for the day-detail drill-in, which looked fine on desktop (no competing header there) but on mobile stacked `MobileShell`'s "← Cash Up [logout]" bar directly above `DayView`'s own "← Monday 21 September 2026 … Submit Declaration" bar — two back arrows, two bars, wasted vertical space. Fixed with a small, deliberately minimal mechanism in `MobileShell.jsx`: `useHideMobileHeader()` (a hook the detail view calls to suppress the shell header for as long as it's mounted) plus an exported `MobileLogoutButton` (so the replacement header can fold sign-out back in) — and a matching `hideHeader`/`onStateChange` prop pair added to `DayView` itself (same shape as `ChecklistRunPanel`'s existing `hideHeader`/`onStateChange` pattern), so the reused component's internal status/save/submit state flows up into the host's own single header instead of rendering its own. **`MobileOrderSheets.jsx`'s `OrderDetail` drill-in has the same shape (a reused desktop component with its own header row, no back button of its own, shown under `MobileShell`'s header) and was not audited or fixed as part of this change** — worth checking whether it reads as a double header too before assuming it's fine because it wasn't reported.
- **When passing render-time state from a reused child component up to a host header via a callback prop, pass the state setter directly rather than wrapping it inline** — `DayView`'s `onStateChange` prop is called from a `useEffect` whose dependency array includes `onStateChange` itself; `MobileCashUp.jsx` passes its raw `setDayState` (a `useState` setter, referentially stable across renders) rather than an inline arrow function, so the effect's dependency array doesn't see a "new" callback every render and doesn't refire needlessly. Passing `state => setDayState(state)` instead would have created a new function on every render of the host, causing DayView's effect to refire every render too — not an infinite loop here (the object it computes doesn't feed back into its own inputs), but pure waste. The same applies to `useHideMobileHeader()`'s internal effect: it depends on `setHidden` pulled from a `useMemo`-stabilised context value in `MobileShell.jsx`, not a fresh object literal each render, for the same reason.
- **Wages inputs are never disabled once a report is `submitted`, so any edit there autosaves straight into a 422 and shows "Save failed" with no explanation** — the whole-tree `PUT /:venueId/cash-recon/wages/:week_start` in `cashRecon.js` rejects any save once `report.status === 'submitted'` ("unsubmit first"), but neither `CashRecon.jsx`'s desktop `WagesView` nor (until the mobile "Paid" checkbox work) `MobileWages.jsx` ever disabled their inputs to reflect that — an operator could type into "Cash paid" on an already-submitted week, watch the debounced autosave silently fail, and lose the edit with only a small "Save failed" text as a clue. Fixed on the mobile page: `RowAmountField`/add/remove are disabled and hidden respectively while `isSubmitted`, with a one-line explanation ("Amounts and staff are locked while submitted — unsubmit to change them"). **Desktop `WagesView` still has the original bug** — its `AmountInput`/`TextInput` fields for hours/rate/total/cash_amount/notes and the add/remove controls are not gated on `isSubmitted` at all; see Outstanding items. If you see "Save failed" anywhere in Cash Recon with no other explanation, check whether the report/day is already submitted before assuming it's a network or validation bug.
- **Marking a wage entry "Paid" is a different lifecycle step from editing its amount, and locking both the same way breaks the normal workflow** — submitting a wage report is meant to finalise the computed totals (hours × rate, fixed amounts), but an operator typically ticks staff off as actually paid in cash AFTER that point, sometimes days later. Locking `cash_amount` behind the same submitted-report guard as `total`/`hours`/`rate` (the original behaviour, still true of the desktop `PUT`) meant there was no way to record "paid" once submitted without unsubmitting first — which also re-exposes the locked amount fields to accidental edits. Fixed via a narrow, purpose-built escape hatch (`PATCH .../entries/:entryId/paid` in `cashRecon.js`) that updates only `cash_amount` and explicitly does not check submitted status, rather than loosening the whole-tree `PUT`'s guard. When a "mark as done" action is conceptually decoupled from the data it's attached to, prefer a small dedicated endpoint for it over relaxing a broader lock — the broader lock is usually protecting something else (here, the computed amounts) that should stay protected.

---

## Deployment — CI/CD via GitHub Actions

**Deployment is fully automated on push.** A GitHub Actions workflow (`.yml`) handles build and
deploy automatically when changes are pushed from the developer's laptop. **Never suggest running
`deploy.sh` manually** — it is not needed and should not be referenced at the end of responses.

- **To deploy:** `git push` from the local laptop. The Actions workflow does the rest.
- **Migrations run automatically** via `api/scripts/migrate.js`, invoked from both the GitHub
  Actions workflow and `deploy.sh`. The runner tracks applied migrations in a
  `schema_migrations` table and only applies new files in order. Each migration runs in a
  transaction — if it fails, the deploy aborts before the API restarts.
- **First-time baseline** — on a server that already had migrations applied manually via
  `psql`, SSH in once and run:
  ```bash
  cd /home/ubuntu/app/api && set -a; source .env; set +a
  node scripts/migrate.js --baseline-up-to 024
  ```
  This records 001–024 as applied without running them. Subsequent deploys then apply
  025, 026, and anything new.
- **OS user is `ubuntu`** — app lives at `/home/ubuntu/app`.
- **deploy.sh** exists on the server as a fallback but is not part of the normal workflow.

---

## Running locally

```bash
# Prerequisites: Postgres 16, Redis 7, Node 22

# API
cd api
cp .env.example .env   # fill in values
psql $DATABASE_URL -f ../migrations/001_tenants_users.sql
# ... run 002-010 in order
psql $DATABASE_URL -f ../migrations/011_doors_close_time.sql
psql $DATABASE_URL -f ../migrations/012_reconfirmed_status.sql
psql $DATABASE_URL -f ../migrations/013_doors_close_per_sitting.sql
psql $DATABASE_URL -f ../migrations/014_schedule_exceptions.sql
psql $DATABASE_URL -f ../migrations/015_fix_get_available_slots.sql
psql $DATABASE_URL -f ../migrations/016_noslot_noshow_cancelled.sql
psql $DATABASE_URL -f ../migrations/017_seated_checked_out.sql
psql $DATABASE_URL -f ../migrations/018_customers.sql
psql $DATABASE_URL -f ../migrations/019_customer_visit_count.sql
psql $DATABASE_URL -f ../migrations/020_slot_start_filter.sql
psql $DATABASE_URL -f ../migrations/021_enable_arrived_status.sql
psql $DATABASE_URL -f ../migrations/022_slot_inclusive_last_order.sql
psql $DATABASE_URL -f ../migrations/023_sitting_names.sql
npm install
npm run dev            # starts on :3000 with --watch

# Admin portal (separate terminal)
cd admin
cp .env.example .env   # fill in VITE_ values
npm install
npm run dev            # starts on :5173, proxies /api to :3000
```

Visit `http://localhost:5173`. Auth0 login redirects to the admin portal.
Use `/widget-test` to test the full booking flow without deploying anything.

---

## Doc maintenance

- **Update CLAUDE.md as part of EOS, every session.** If you skip a session, the doc rots.
- **"Common mistakes to avoid" is append-only.** Never delete an entry — only correct one if it turns out to be wrong. This section beats every other section in long-term value.
- **Keep the TOC synced.** When adding or renaming a section, update the Table of Contents.
- **Don't let the doc grow past ~3,000 lines.** Once a module section exceeds ~200 lines, move it to `docs/<MODULE>.md` and link from CLAUDE.md.
- **Don't duplicate what's already in code** (table schemas exposed via API, route lists). Reference instead.
- **Don't include credentials, tokens, or secret values** — only their names and where they live.
- **Don't include time-sensitive status** ("in progress this sprint") — that's what Outstanding items is for.
- **Don't write planning / decision documents inline.** Decisions go in commit messages or PR descriptions.
- **Cross-link with docs/.** CLAUDE.md is the index; deep-dives live elsewhere.
- **Companion files worth setting up:**

| File | Purpose |
|------|---------|
| `MEMORY.md` (in `~/.claude/projects/<repo>/memory/`) | Persistent AI memory — short notes that survive across sessions |
| `docs/<MODULE>.md` | Deep dives that don't belong in CLAUDE.md |
| `CHANGE.md` | Living changelog — prepend on every deploy |

